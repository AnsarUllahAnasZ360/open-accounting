# Epic 0 — Defect-to-Epic Map (verified against current code)

Date: 2026-06-13. Read-only audit by a 6-agent workflow. Every anchor the redesign report cites was re-checked against the current code; defects are tagged with the owning epic. Companion to the baseline screenshots in `docs/finishing/evidence/baseline/`.

## Summary

- Clusters audited: **6** (all 12 surfaces)
- Report anchors re-verified: **151** — drifted: **4**, missing: **1** (rest valid)
- Concrete defects catalogued: **132**
- Design-system token violations: **76**

## Defects by owning epic

### Epic 1 (1)

| Severity | Issue | Current ref | Surface |
|---|---|---|---|
| high | Pre-req for the Income/Expenses red-routing fixes: the app token layer is missing the --negative / --negative-surface semantic token (globals.css defines only destructive/positive/warning/info/chart-1..5). text-negative/bg-negative will not compile until Epic 1 wires it. Build epics cannot 'route reds through --negative' until this lands. | `apps/web/src/app/globals.css:69-80` | Income + Expenses |

### Epic 2 (27)

| Severity | Issue | Current ref | Surface |
|---|---|---|---|
| high | Docked AI panel is a width-stealing 380px flex sibling (aside, shrink-0) of the max-w-1200 main, with no overlay/scrim and no narrow-column fallback; opening it forces inner horizontal scrollbars on P&L/GL/Trial-Balance/Aging money grids at common laptop widths. Structural fix (overlay drawer or resizable split keeping content legible) not applied. | `apps/web/src/components/openbooks/AppShell.tsx:467-469 (aside w-[380px] shrink-0 lg:flex); main apps/web/src/components/openbooks/AppShell.tsx:463` | Reports + Settings |
| high | Provider/vendor name 'Bedrock active' leaks to the owner in the chat header Badge (quiet-AI / plain-English violation). Active state should show no status chrome. | `apps/web/src/components/openbooks/OpenBooksAIChat.tsx:757-759` | Ask AI + Shell |
| high | Provider leak in the full-page header CategoryChip ('Bedrock active'/'Degraded mode'). | `apps/web/src/components/openbooks/AskAIScreen.tsx:45` | Ask AI + Shell |
| high | ai.ts label 'Bedrock provider is configured' is rendered directly to the user in the status card when no thread is active (4th leak site). Relabel to provider-agnostic ('AI is on'/'AI is off'). | `apps/web/src/lib/openbooks/ai.ts:143 (rendered at OpenBooksAIChat.tsx:804)` | Ask AI + Shell |
| high | Raw native <select> thread switcher (hand-classed, not shadcn Select) plus a parallel page-only ThreadRail — two inconsistent, non-command-driven switchers. Both sm/lg-gated so neither is reachable on the mobile sheet/page. | `apps/web/src/components/openbooks/OpenBooksAIChat.tsx:762-774 (select) and :476-538 (ThreadRail)` | Ask AI + Shell |
| high | Docked AI panel is a fixed w-[380px] flex SIBLING of the max-w-[1200px] main column: opening it on a dense page compresses content to ~820px (at 1440) rather than overlaying. No overlay/resizable option, no scrim, no Escape-to-close on desktop. | `apps/web/src/components/openbooks/AppShell.tsx:466-480 (sibling of :463)` | Ask AI + Shell |
| high | pendingPrompt nonce uses '::' separator (AppShell appends ::Date.now(), chat strips via split('::')[0]). Any prompt containing '::' (time range, ratio) is silently truncated before send. Replace with a structured {prompt, nonce} payload. | `apps/web/src/components/openbooks/AppShell.tsx:226 and OpenBooksAIChat.tsx:644` | Ask AI + Shell |
| high | Mobile sheet header right-hand controls (thread <select>, new-chat, maximize) are all sm:-gated, so on the phone bottom-sheet thread switching is entirely unreachable. Same applies to /ask-ai page on a phone (ThreadRail lg:flex, artifacts xl:block both hidden). | `apps/web/src/components/openbooks/OpenBooksAIChat.tsx:757,762-774,777,786 (sm-gated)` | Ask AI + Shell |
| high | Mobile bottom-sheet is a raw translate-y aside that is ALWAYS mounted (outside the aiOpen guard) and uses direct document.body.style.overflow mutation for scroll-lock instead of a managed shadcn Drawer/Sheet. When aiOpen on lg, OpenBooksAIChat is double-mounted (desktop + mobile) with identical props (pendingPrompt fires into both, two live Convex chat subscriptions + thread queries). | `apps/web/src/components/openbooks/AppShell.tsx:466-480 (desktop) + :492-507 (mobile, always mounted) + :233-246 (scroll-lock)` | Ask AI + Shell |
| medium | 'Bedrock active' / 'Degraded mode' vendor badge leaks the AI provider name to owners; report mandates replacing it with a quiet capability state (no vendor wording). Two further vendor leaks: 'Bedrock is the v1 target when env is present' and the receipt extractionLabel 'Bedrock'. | `apps/web/src/components/openbooks/ModuleScreens.tsx:1568 (badge), :1580 (copy), :525 (extractionLabel)` | Reports + Settings |
| medium | Status-card copy names the backend framework: 'Answers stream from the Convex Agent...'. Drop framework naming. | `apps/web/src/components/openbooks/OpenBooksAIChat.tsx:806-808` | Ask AI + Shell |
| medium | Hand-rolled regex markdown engine (InlineMarkdown/MarkdownBlocks/isSeparatorLine/parseTableRow) reimplements tables/lists/links — maintenance + XSS/format risk. Replace with AI Elements Response (streamdown). | `apps/web/src/components/openbooks/OpenBooksAIChat.tsx:154-314` | Ask AI + Shell |
| medium | Tool-call disclosure on native <details>/<summary> instead of shadcn Collapsible / AI Elements Tool primitive. | `apps/web/src/components/openbooks/OpenBooksAIChat.tsx:325-342` | Ask AI + Shell |
| medium | Docked desktop panel is lg:flex only — tablet band (768-1023px) gets NO docked panel, only the mobile bottom-sheet (lg:hidden), even in landscape on a wide tablet. | `apps/web/src/components/openbooks/AppShell.tsx:469` | Ask AI + Shell |
| medium | Assistant table money alignment is index-based not semantic: only cellIndex>0 gets money-figures text-right; a leftmost money column or label-bearing 2nd column renders wrong. And <th> headers are always text-left font-medium with no money-figures, so a money header is left-aligned proportional over right-aligned tabular body cells. | `apps/web/src/components/openbooks/OpenBooksAIChat.tsx:226 (header) and :238 (body cell)` | Ask AI + Shell |
| medium | Assistant inline table wrapper uses overflow-hidden, clipping wide money tables at 380px with no horizontal-scroll affordance. Needs overflow-x-auto. | `apps/web/src/components/openbooks/OpenBooksAIChat.tsx:221` | Ask AI + Shell |
| medium | No compact/collapsed Ask AI affordance: CollapsedRail (AppShell.tsx:717-820) renders only appRoutes + settingsRoute and has NO Ask AI trigger, so when the sidebar is iconified the only entry point is the header pill. Add a Sparkles trigger to CollapsedRail. | `apps/web/src/components/openbooks/AppShell.tsx:717-820` | Ask AI + Shell |
| medium | Header clutter: hardcoded literal 'Jun 2026' month chip (static, goes stale after 2026-06, sm-gated) and a dedicated global search trigger that vanishes below md while the mobile bottom nav has no search entry — phones have no UI path to the command palette. Strip the month chip; surface search in mobile chrome. | `apps/web/src/components/openbooks/AppShell.tsx:444-446 (month chip) and :430-441 (search, md:flex)` | Ask AI + Shell |
| medium | Ask AI is a permanent full text+icon+shortcut header pill, not the icon-only/floating assistant control Ansar asked for. Convert to icon-only Sparkles + tooltip + ⌘J on the far right. | `apps/web/src/components/openbooks/AppShell.tsx:447-459` | Ask AI + Shell |
| medium | answerOpenBooksQuestion (ai.ts) is a full deterministic local Q&A engine (tables, AR aging, payroll, proposals) that is NOT wired into the chat — the chat only calls api.aiThreads.sendMessage, so in degraded mode it cannot fall back to this local answerer. The rich local engine is orphaned dead code relative to the chat. Decide: wire it as the degraded-mode fallback or remove it. | `apps/web/src/lib/openbooks/ai.ts:235-439` | Ask AI + Shell |
| medium | Only Reports dispatches a scoped createAiRequestEvent; Income/Expenses/Transactions/Bills never push entity+date-range+filter context, so 'Viewing: {label}' is a coarse route name and reportPack is year-wide (2026-01-01..2026-12-31 pinned in AppShell.tsx:135-141). Have each workbench dispatch its active scope. (Launch wiring lands per-surface in Epics 3/4/5; the receiving widget contract is Epic 2.) | `dispatch only at apps/web/src/components/openbooks/ReportsScreen.tsx:1269; receiver AppShell.tsx:221-231; year-wide pack AppShell.tsx:132-146` | Ask AI + Shell |
| medium | Entity name re-announced per page: AppScreen PageHeader eyebrow=activeEntity.name duplicates the sidebar EntitySwitcher trigger; activeEntityName even falls back to reportPack.entity.name. Kill the per-page eyebrow duplication and route.summary boilerplate. | `apps/web/src/components/openbooks/AppScreen.tsx:50-55; AppShell.tsx:255,846` | Ask AI + Shell |
| low | SUGGESTIONS prompt list is duplicated verbatim in OpenBooksAIChat.tsx:33-39 and ai.ts:121-127 (aiSuggestedPrompts). Two sources of truth; chat ignores the lib export. Collapse to the single ai.ts export. | `apps/web/src/components/openbooks/OpenBooksAIChat.tsx:33-39 vs apps/web/src/lib/openbooks/ai.ts:121-127` | Ask AI + Shell |
| low | Footer suggestion chips use overflow-x-auto inside the 380px panel, forcing a horizontal scrollbar instead of wrapping (prototype wraps). Switch to flex-wrap / AI Elements Suggestion. | `apps/web/src/components/openbooks/OpenBooksAIChat.tsx:871` | Ask AI + Shell |
| low | Page mode (/ask-ai) renders a redundant outer PageHeader (eyebrow=entity + 'Ask AI' title + provider chip) above the chat that owns its own header — double header. Drop the outer PageHeader and the provider chip; let the widget own its chrome. | `apps/web/src/components/openbooks/AskAIScreen.tsx:41-46` | Ask AI + Shell |
| low | Static 'Pinned artifacts' aside on the full page is dead placeholder copy with no functionality; should become a real Sources/artifacts canvas or be removed. | `apps/web/src/components/openbooks/OpenBooksAIChat.tsx:907-915` | Ask AI + Shell |
| low | 'Demo entity' body chip rendered in the page header on every demo screen — body-level demo marker Ansar wants removed in favor of a single subtle env indicator near the workspace name. (Demo chip is shell/AppScreen-owned.) | `apps/web/src/components/openbooks/AppScreen.tsx:54` | Ask AI + Shell |

### Epic 3 (27)

| Severity | Issue | Current ref | Surface |
|---|---|---|---|
| blocker | No Approve action anywhere — no per-row inline Approve, no bulk Approve, no Approve-&-post in the detail panel. The only state-changing actions are Recategorize (hardwired to the 'other income' acct 4200 via otherIncomeCategoryId, lines 761-764/1125), Split (postSplit, 900), and Exclude (bulk 946 + panel 1172). An owner literally cannot confirm a needs_review item from this screen, breaking the mandatory v1 loop (uncertain -> review -> confirm). | `apps/web/src/components/openbooks/CoreScreens.tsx:946 (only bulk action is Exclude selected); 1121-1129 (panel 'Recategorize' hardwired to otherIncomeCategoryId, not an approve)` | Transactions |
| blocker | Detail panel auto-opens on first paint with no row clicked. `selected` falls back to data.rows[0] (746) and the 380px aside (1048) renders whenever selected is truthy, so a 0-balance Accounting view / Receipt / Activity / Split / Exclude stack shows before any selection. Row detail must be CLOSED by default; replace the permanent aside with a shadcn Sheet/DetailSheet opening only on click. | `apps/web/src/components/openbooks/CoreScreens.tsx:746 (?? data.rows[0]); 980 (permanent grid); 1048 (always-present aside)` | Transactions |
| high | Inbox backend hardcodes negative on document totals (-document.totalMinor) so receipts always render negative and money-in (transfers/payouts) never shows +; Amount is never passed signed/tone, so direction is invisible. Correctness bug baked into the view layer (R3/D7). | `convex/coreViews.ts:327` | Dashboard + Inbox |
| high | Inbox detail has only 2 layouts (receipt vs generic categorize). The four other backend kinds — transfer, payout_mismatch, connection, question — all fall into the categorize detail showing a meaningless category dropdown, no evidence rows, no ledger-impact copy, no provenance. Backend inbox query returns none of the per-kind evidence (transfer pairs, payout breakdown, connection state, question candidates). | `apps/web/src/components/openbooks/CoreScreens.tsx:593-669 / convex/coreViews.ts:312-360` | Dashboard + Inbox |
| high | Static lg:grid-cols-[360px_1fr] never collapses to a drawer. On mobile both panes stack: the entire list renders first, the detail sits far below, and selecting a row gives no visible feedback (no scroll, no Sheet) — effectively unusable as a triage queue. The 88dvh bottom-sheet pattern at AppShell.tsx:495 is not reused. | `apps/web/src/components/openbooks/CoreScreens.tsx:503` | Dashboard + Inbox |
| high | Table wrapper is overflow-hidden, which CLIPS instead of scrolls; below xl the [1fr_380px] grid collapses to one column with no horizontal-scroll wrapper and no mobile card/list reflow, so the dense 7-col table renders squeezed on phones. Violates the 'real responsive surface, not a squeezed desktop table' rule. | `apps/web/src/components/openbooks/CoreScreens.tsx:981 (overflow-hidden); 980 (xl-only split, no md card fallback)` | Transactions |
| high | Filters incomplete: only 5 review-enum Button pills (934) + one merchant/memo search Input (942). Missing AccountMultiSelect, DateRangeControl, amount-direction, type, category, contact, source, receipt has/none, AI-status+confidence band, and a Needs-attention toggle. Server query accepts only review+search (coreViews.ts:375-379) and must be extended. | `apps/web/src/components/openbooks/CoreScreens.tsx:934-943; convex/coreViews.ts:375-379` | Transactions |
| high | Columns incomplete vs prototype: no Contact column, no receipt/paperclip indicator, no quiet green AiInsightBadge (Sparkles) popover exposing reasoning/decidedBy/confidence, no per-row confidence, no ledger/reconciliation state dot, no inline Approve. Merchant subline is bare `{source} - {decidedBy ?? 'review'}` text (1016). | `apps/web/src/components/openbooks/CoreScreens.tsx:985-991 (TableHead set); 1016 (bare subline)` | Transactions |
| high | Row shape (coreViews.ts:464-503) does not return contactId/contactName or reasoning, though both exist on the transactions doc (schema.ts:354 contactId, :370 reasoning). The new FilterBar/table Contact column + AiInsightBadge reasoning popover require the query to add: contactId, contactName, reasoning, and a receipt-present boolean (currently only a full receipt sub-object is returned; hasInboxItem IS already returned at 478). | `convex/coreViews.ts:464-503 (row shape); convex/schema.ts:354,370 (fields exist on doc)` | Transactions |
| high | No PageHeader/title/subtitle and no header actions. Prototype wants title 'Transactions' + subtitle 'Every account, one register · {entity}' and right-aligned Import / ExportMenu / primary green 'Add transaction'. Manual add (1195-1205) and CSV import (1207-1230) are buried as cards in the right aside. | `apps/web/src/components/openbooks/CoreScreens.tsx:929-957 (filter card, no header); 1195,1207 (buried entry points)` | Transactions |
| high | No Export affordance of any kind (no CSV all/filtered/selected/audit). ExportMenu must be wired into the header. | `apps/web/src/components/openbooks/CoreScreens.tsx:717-1236 (grep: zero export controls in TransactionsScreen)` | Transactions |
| high | No attach-receipt + ask-AI-to-categorize -> review-before-post flow on this surface. Panel has only a read-only receipt preview (1088-1102); Recategorize (updateCategory 798) silently reverses+reposts with no confirm gate; categorizePendingTransactions fires only as a CSV-import side effect (865), never from a selected row. | `apps/web/src/components/openbooks/CoreScreens.tsx:1088-1102 (read-only preview); 798-812 (no review-before-post); 865 (AI only via CSV)` | Transactions |
| high | N+1 backing-query cost: coreViews.transactions runs one journalLines query per row in a loop (429-436) and re-scans up to 1000 fetched journalEntries per row to assemble entryIds for activity (445-453). Quadratic-ish at the 120-row cap; richer AI affordances / larger registers from the redesign amplify it. Batch the journalLine fetch (group by entryId) and index/precompute entry linkage. | `convex/coreViews.ts:429-453` | Transactions |
| medium | Inbox list is a single flat inbox.items.map() with no work-type grouping; the prototype's groupOrder (Needs category / Receipts / Possible transfers / Payout issues / Connections / AI questions) was never ported. No progress strip and no confidence-aware 'Confirm all >=0.90' batch banner. | `apps/web/src/components/openbooks/CoreScreens.tsx:506-564` | Dashboard + Inbox |
| medium | Receipt compare card (md:grid-cols-2) lives inside the 1fr track; vendor/candidate.merchant/category values use right-aligned font-medium spans with no truncate/min-w-0, so long values wrap and collide with their labels at ~1024-1280px. Only fileName truncates. | `apps/web/src/components/openbooks/CoreScreens.tsx:603,628,640` | Dashboard + Inbox |
| medium | selected auto-falls-back to inbox.items[0]; selectedId is never reset when the selected item is resolved/removed. After confirm/exclude the list re-fetches, find() returns undefined, focus silently jumps to the top of the list instead of advancing to the next card — a triage-flow regression. Row detail is therefore never CLOSED by default either. | `apps/web/src/components/openbooks/CoreScreens.tsx:360` | Dashboard + Inbox |
| medium | merchant falls back to the raw item.kind string for kindless items, so the detail <h2> renders literal 'connection'/'question' and the checkbox aria-label becomes 'Select connection' — a copy + accessibility regression. categoryLabel only de-underscores in the subline; the title/aria use the raw string. | `convex/coreViews.ts:325` | Dashboard + Inbox |
| medium | confirmBatch awaits confirmTransaction sequentially with no error isolation; a single rejected mutation throws and aborts the remaining items, the success message never shows, and there is no per-item status (R6). | `apps/web/src/components/openbooks/CoreScreens.tsx:442-460` | Dashboard + Inbox |
| medium | Inline per-row category Select forced to min-w-44 crushes Merchant/Account columns and can overflow on tablet widths. Drop the hard min so columns compress (Section 8.2). | `apps/web/src/components/openbooks/CoreScreens.tsx:1021` | Transactions |
| medium | Bulk actions incomplete: only 'Exclude selected' (946); selection state is wired (checkedTransactionIds, 737) but there is no bulk Approve/confirm, no bulk Recategorize, and no Clear-selection control — and it is a static button row, not a contextual green bulk bar that explains confident items already auto-posted. | `apps/web/src/components/openbooks/CoreScreens.tsx:945-951` | Transactions |
| medium | Reconciliation tile shows static bankAccounts[0] regardless of any (absent) account filter, so its Ledger/Synced/difference figures can contradict the visible register; no reconnect action, just a Matched/Needs-review chip. (Connect/reconnect itself belongs to Settings/Connections per IA, but the tile must at least scope to the filtered account.) | `apps/web/src/components/openbooks/CoreScreens.tsx:779; 959-978` | Transactions |
| medium | CSV import duplicate-detection is cosmetic: it computes duplicateCsvCount (778) and surfaces 'N duplicate-looking rows' (1218) but imports every row anyway via routeTransaction with externalId `csv:{date}:{description}:{amount}` (859), so two genuinely distinct same-day same-merchant same-amount transactions collide on externalId. Real correctness gap on a money surface when import becomes a primary header action. | `apps/web/src/components/openbooks/CoreScreens.tsx:778,848-863,1218` | Transactions |
| medium | Comments and 'Create rule from this' capabilities fully absent. The prototype drawer has a Comments thread (composer + posts) and a Create-rule footer action; the current panel has only read-only Activity history (1103-1120). No comment composer, no rule-creation affordance. | `apps/web/src/components/openbooks/CoreScreens.tsx:1103-1120 (Activity only); 1172-1190 (footer is Exclude only, no Create-rule/Approve)` | Transactions |
| low | Inbox list has no internal scroll region; with take(2000) open items the list grows the page unbounded instead of an internal overflow-y like the prototype. Action button row (flex-wrap) and message banner push detail height unpredictably with no sticky action bar. | `apps/web/src/components/openbooks/CoreScreens.tsx:516,675 / convex/coreViews.ts:298` | Dashboard + Inbox |
| low | bankAccountName falls back to literal 'OpenBooks' when no bankAccount resolves, surfacing a placeholder string as real account data in the detail Account/Candidate column. | `convex/coreViews.ts:332,353` | Dashboard + Inbox |
| low | No threshold-aware approval explanation. Prototype shows a 'How approval works' note tied to the balanced=0.90 autonomy constant; current screen gives the owner zero explanation of why some rows posted automatically and others wait — a trust/transparency gap on the review loop. | `apps/web/src/components/openbooks/CoreScreens.tsx:929-957 (filter/bulk card has no approval note)` | Transactions |
| low | Developer/raw copy on a money surface: button labels 'Add through pipeline' (1203), 'Import through the pipeline' messaging (834), 'Manual transaction imported through the pipeline.' (834), and raw enum Status chip (1033, via categoryLabel underscore-swap only). Replace with owner-plain English. | `apps/web/src/components/openbooks/CoreScreens.tsx:834,1033,1203` | Transactions |

### Epic 4 (17)

| Severity | Issue | Current ref | Surface |
|---|---|---|---|
| high | Income Payments rows are inert: PaymentRow carries txn._id (incomeViews.ts:113) but the table row has no onClick/href, so a payment cannot drill into the SAME record in Transactions. The IA 'Income is a lens, not a silo' contract is unmet at the UI layer — user cannot tell Income and Transactions show the same money. | `apps/web/src/components/openbooks/IncomeScreen.tsx:161-174; convex/incomeViews.ts:113` | Income + Expenses |
| high | Income KPIs and payments are frozen to MONTH_START/TODAY constants with no DateRangeControl; the overview query takes only entityId and cannot accept an explicit range, so the lens cannot re-scope to match Reports/Dashboard period filters (number-consistency rule). | `convex/incomeViews.ts:8-9,95,141; apps/web/src/components/openbooks/IncomeScreen.tsx:48-51` | Income + Expenses |
| high | Income/Receivables/Payments tables have no horizontal-scroll wrapper or mobile card fallback; the 7-col Invoices table and 6-col aging grid sit inside Card overflow-hidden and will CLIP money columns on a ~380px viewport — a squeezed-desktop-table the DS bans. No sticky headers on any table. | `apps/web/src/components/openbooks/IncomeScreen.tsx:226-260,290,306,326` | Income + Expenses |
| high | Expenses has NO full expense transaction table (the headline missing feature): CategoryTable is a category-aggregate CSS grid with no per-transaction rows, date/merchant/account/evidence columns, search, or row-detail DetailSheet. Contract wants a table-first lens mirroring Transactions with row-detail CLOSED by default. | `apps/web/src/components/openbooks/ExpensesScreen.tsx:85-146` | Income + Expenses |
| high | Expenses category table is a hardcoded 7-col CSS grid grid-cols-[18px_1.4fr_70px_1fr_90px_110px_14px] with no responsive variant and Card overflow-hidden; fixed 70/90/110px tracks plus share bar cannot fit ~380px mobile and clip rather than reflow. Recurring/vendor rows carry min-w-[90px]/min-w-[110px] minimums that overflow phone width. | `apps/web/src/components/openbooks/ExpensesScreen.tsx:95,107,128-130,157-165` | Income + Expenses |
| medium | Missing Customers and Streams tabs and the Recurring-revenue/MRR KPI required by the revenue-lens contract; only Payments/Invoices/Receivables exist. No revenue-by-stream / by-customer / recurring-vs-onetime / AR-aging charts (no BarChart/Sparkline imported in IncomeScreen). | `apps/web/src/components/openbooks/IncomeScreen.tsx:82,102-123` | Income + Expenses |
| medium | Bespoke segmented tab control instead of shadcn Tabs; a THIRD bespoke tab style (the invoice sub-filter underline buttons) coexists. No role=tablist/aria-selected/arrow-key nav — a11y regression vs the shadcn Tabs they bypass (tabs.tsx exists, unused). | `apps/web/src/components/openbooks/IncomeScreen.tsx:81-95,211-224` | Income + Expenses |
| medium | Payments hard-capped at 40 (incomeViews.ts:217) with no 'showing 40 of N' / load-more, while 'Received this month' KPI sums the full pre-slice set (incomeViews.ts:141-142). On a >40-payment month the KPI total will not foot to the visible list — a reconciliation/trust defect. | `convex/incomeViews.ts:141-142,217` | Income + Expenses |
| medium | Receivables matrix has only Current/1-30/31-60/61-90 and NO 90+ bucket; money aged past 90 days is folded or dropped by buildAgingRows — a potential reconciliation gap with the Reports AR aging the lens claims to share. | `apps/web/src/components/openbooks/IncomeScreen.tsx:290-296; convex/incomeViews.ts:221-228` | Income + Expenses |
| medium | Missing Expenses KPIs uncategorizedCount, missingEvidenceCount, topVendor — and backend expensesViews.overview does not compute them; only spent/recurring/biggest-mover exist. These need new backend fields. | `apps/web/src/components/openbooks/ExpensesScreen.tsx:67-83; convex/expensesViews.ts:266-274` | Income + Expenses |
| medium | No shadcn Tabs and no shared FilterBar/DateRangeControl/AccountMultiSelect on Expenses; only a 2-segment period toggle bound to hardcoded PERIODS. tabs.tsx exists but is unused. Required Tabs: Transactions/Categories/Vendors/Recurring/Evidence Needed. | `apps/web/src/components/openbooks/ExpensesScreen.tsx:50-65; convex/expensesViews.ts:12-15` | Income + Expenses |
| medium | Expenses period math is pinned to demo TODAY=2026-06-11 (and PERIODS Jun/May 2026, recurring window, next-date projection). For any real 'today' other than mid-June 2026 the surface silently shows stale/empty periods — a correctness/limits gap. | `convex/expensesViews.ts:7-15,110,142-146` | Income + Expenses |
| low | Raw <select> for invoice Terms in the composer instead of shadcn Select (select.tsx exists). | `apps/web/src/components/openbooks/IncomeScreen.tsx:508-510` | Income + Expenses |
| low | Dropped prototype 'Download PDF' invoice action — present in Income.dc.html detail drawer, absent from InvoiceDetailDrawer footer (only Finalize/Send reminder/Void/Close). | `apps/web/src/components/openbooks/IncomeScreen.tsx:657-669` | Income + Expenses |
| low | Avg-days-to-pay is net-terms (issue->due) across paid invoices, not realized days-to-pay; a Net-30 customer who always pays on day 5 still shows 30. Effectively a constant of the terms mix, mislabeled as a behavioral KPI. | `convex/incomeViews.ts:196-202` | Income + Expenses |
| low | InvoiceComposer subtotal uses float dollar math Math.round((Number(rate))*100)*(Number(qty)) on free-text decimal input; >2-decimal entry can produce off-by-one-cent minor units before the mutation. Borderline against 'never floats for stored financial amounts'; relies on invoices.saveDraft re-validating server-side. | `apps/web/src/components/openbooks/IncomeScreen.tsx:368,494` | Income + Expenses |
| low | Expenses category disclosure rows are <button> wrapping a CSS grid with no aria-expanded/aria-controls and a chevron with no accessible label; a screen reader cannot tell the row is expandable or its state. | `apps/web/src/components/openbooks/ExpensesScreen.tsx:103-121` | Income + Expenses |

### Epic 5 (20)

| Severity | Issue | Current ref | Surface |
|---|---|---|---|
| high | Bills register is rendered as four stacked group Cards of bespoke div-grid rows (md:grid-cols-[1fr_auto_auto_auto]) instead of one shadcn Table/DataTable — no column headers, no search, no filter, no sort, no export. Table is imported (:49-56) and used elsewhere in the same file but not for bills. | `apps/web/src/components/openbooks/ModuleScreens.tsx:733-767` | Bills + Contacts + Payroll |
| high | Persistent half-empty 'Selected bill' side panel renders 'No bill selected' + two boilerplate paragraphs by default, consuming ~45% width; violates closed-by-default row-detail requirement. Should be a DetailSheet opened on row click. | `apps/web/src/components/openbooks/ModuleScreens.tsx:770-798` | Bills + Contacts + Payroll |
| high | Contacts always renders a two-column split (directory 1.2fr + profile 0.8fr) and the backend defaults selectedProfile to contactRows[0], so the profile pane is populated before any click — directory is cramped and never full-width. Contract wants full-width directory + closed-by-default Sheet. | `apps/web/src/components/openbooks/ModuleScreens.tsx:179,248; convex/moduleViews.ts:263-264` | Bills + Contacts + Payroll |
| high | Combined open-balance cell nets AR against AP (openReceivableMinor - openPayableMinor) into one number — actively misleading for a contact who is both customer and vendor (R3 correctness bug). Should show separate Money-owed-to-you / You-owe columns. | `apps/web/src/components/openbooks/ModuleScreens.tsx:236` | Bills + Contacts + Payroll |
| high | 'This year' column sums invoice revenue + bill totals + abs(transaction amounts) into one opaque totalThisYearMinor (revenue and spend added together), semantically meaningless per role (R3). Prototype splits Received-YTD vs Paid-YTD. | `convex/moduleViews.ts:223-232` | Bills + Contacts + Payroll |
| high | Contacts schema has no archived flag and there is no contacts.ts archive/delete mutation; the resolved soft-archive decision (11.0 / D1) requires a net-new schema field + an 'Archived' filter before the UI affordance can exist. | `convex/schema.ts:231-240` | Bills + Contacts + Payroll |
| medium | Raw HTML <select> for the document type picker instead of shadcn Select. | `apps/web/src/components/openbooks/ModuleScreens.tsx:625-632` | Bills + Contacts + Payroll |
| medium | Bill rows render the raw dueDate string and statusLabel subtext with no overdue/due-soon semantic token; an overdue bill reads identically neutral to an open one. daysUntilDue is already on the row (moduleViews.ts:339) but unused, so no 'in 3d' / 'X days overdue' humanization and no warning color. | `apps/web/src/components/openbooks/ModuleScreens.tsx:747-760` | Bills + Contacts + Payroll |
| medium | Per-bill evidence link (BillRow.document) is fully plumbed (moduleViews.ts:330,340-342; typed module-helpers.ts:151) but rendered nowhere — selecting a bill shows no evidence; documents live on a physically separate upload Card. | `apps/web/src/components/openbooks/ModuleScreens.tsx:769-798 (never reads selectedBill.document)` | Bills + Contacts + Payroll |
| medium | KPI strip shows only Open total / Due this week / Overdue; the contract's six-tile strip (add Paid this period, Missing evidence, Avg days to pay) is missing and the three new metrics are not computed in moduleViews. | `apps/web/src/components/openbooks/ModuleScreens.tsx:602-606; convex/moduleViews.ts:500-510` | Bills + Contacts + Payroll |
| medium | AddBillModal is a single manual form, not the prototype's 2-path 'Upload PDF (AI extract) vs Type it in' chooser; its DialogDescription is factually stale — says 'PDF text extraction lands in a later epic' even though the upload path already runs a pdf_text Bedrock branch (ModuleScreens.tsx:524-527) and moduleViews.ts:515 advertises 'PDF text metadata'. | `apps/web/src/components/openbooks/ModuleScreens.tsx:875-935 (stale copy at :909)` | Bills + Contacts + Payroll |
| medium | 'Add contact' button has no onClick — non-functional primary affordance. | `apps/web/src/components/openbooks/ModuleScreens.tsx:172-176` | Bills + Contacts + Payroll |
| medium | Permanent empty 'No contact selected' EmptyState pane occupies the 0.8fr column as dead space whenever nothing is selected. | `apps/web/src/components/openbooks/ModuleScreens.tsx:261-268` | Bills + Contacts + Payroll |
| medium | Static 'Default category as rule' Sparkles AI card is always rendered even when no category is set (label falls back to 'Set a default category for X'), surfacing a hollow green AI affordance. | `apps/web/src/components/openbooks/ModuleScreens.tsx:293-301; convex/moduleViews.ts:257` | Bills + Contacts + Payroll |
| medium | Merge-duplicates card is static/non-functional (mergeFlow is a backend placeholder); no merge mutation and no archive action exist. | `apps/web/src/components/openbooks/ModuleScreens.tsx:303-313; convex/moduleViews.ts:295-301` | Bills + Contacts + Payroll |
| low | Unnecessary explanatory text: long ModuleIntro description plus marketing boilerplate ('true accrual books without the homework'; 'Partial payments are out of scope in this version'; upload reason paragraph). | `apps/web/src/components/openbooks/ModuleScreens.tsx:586-588,794,796,619` | Bills + Contacts + Payroll |
| low | Backend-computed lastActivity (moduleViews.ts:233-238) is returned on the row (:249) but dropped from the ContactRow type (module-helpers.ts:115-126) and never shown as a column. | `apps/web/src/components/openbooks/module-helpers.ts:115-126` | Bills + Contacts + Payroll |
| low | Role chips show no role-aware color (CategoryChip only has active/inactive) and Open-balance carries no overdue/aging signal — flat neutral, losing the prototype's AR-overdue health cue. | `apps/web/src/components/openbooks/ModuleScreens.tsx:232,236; primitives.tsx:194-211` | Bills + Contacts + Payroll |
| low | Row click sets selectedId only; it never pushes the contact id into the URL/searchParams, so deep-link state and click-selection diverge and selection is lost on refresh. | `apps/web/src/components/openbooks/ModuleScreens.tsx:224` | Bills + Contacts + Payroll |
| low | Filtered directory has no empty-state row: when search + role filter match nothing the TableBody maps an empty array (no length check), leaving a bare header and blank card. | `apps/web/src/components/openbooks/ModuleScreens.tsx:218-243` | Bills + Contacts + Payroll |

### Epic 6 (40)

| Severity | Issue | Current ref | Surface |
|---|---|---|---|
| high | Payroll paid checkbox is a raw <input type=checkbox readOnly> — functionally dead: no onChange, so there is NO per-line mark-paid control even though payroll.ts exposes markLinePaid (:548); only run-level 'Mark all paid' exists. | `apps/web/src/components/openbooks/ModuleScreens.tsx:1327-1333` | Bills + Contacts + Payroll |
| high | PayrollRunDetail is a full-screen in-place swap that unmounts the runs list + KPI strip; contract requires the run detail to be CLOSED by default (DetailSheet/inline expansion) with the list and KPIs staying on screen. | `apps/web/src/components/openbooks/ModuleScreens.tsx:965-967,1119` | Bills + Contacts + Payroll |
| high | Approval banner concatenates raw localMinor/100 floats (e.g. 'PKR 2140000' / 'USD 6000') with no Amount component, no thousands separators, no mono — violates 'minor units, never floats / tabular figures'. | `apps/web/src/components/openbooks/ModuleScreens.tsx:1194` | Bills + Contacts + Payroll |
| high | Run-payroll button is hardcoded to literal period '2026-06' (runJune) with no period selector; there is no way to draft any other period or to scope Runs/Statements by period. | `apps/web/src/components/openbooks/ModuleScreens.tsx:952-963,1081` | Bills + Contacts + Payroll |
| high | run.headcount is computed from CURRENT active employees, not the run's own snapshot lines, so every historical run shows the same headcount regardless of who was actually on it. | `convex/moduleViews.ts:528` | Bills + Contacts + Payroll |
| high | No payroll auto-run / scheduler exists (only cron is Plaid sync) and the UI never states that runs are manually drafted; the resolved auto-run decision (11.0) is net-new backend (scheduled startRun / pay-schedule) and an honest 'Auto-run: off' affordance is missing. | `convex/crons.ts:7; convex/payroll.ts:253; apps/web/src/components/openbooks/ModuleScreens.tsx:1086-1113` | Bills + Contacts + Payroll |
| high | Reports drill-through is local-only: clicking a money cell opens a read-only in-page DrillSheet and never routes to the filtered register. No account=/category=/start=/end= params are read by the register (CoreScreens transactions reads only ?focus) or emitted by MoneyButton, so 'work the records' has no hand-off to Transactions/Income/Expenses per IA. | `apps/web/src/components/openbooks/ReportsScreen.tsx:430-456 (MoneyButton onDrill -> local sheet), :1191 (only router.push is /reports?report=); register param read at apps/web/src/components/openbooks/CoreScreens.tsx:720 (only ?focus)` | Reports + Settings |
| high | Settings desktop subnav is not sticky: <nav> has no sticky/top- class and AppShell main is the document scroll container with no height/overflow, so on long sections (Rules, Audit, Categories ~16 rows) the 190px subnav scrolls off-screen, forcing a scroll-to-top to switch sections. | `apps/web/src/components/openbooks/SettingsScreen.tsx:152-156 (nav, no sticky); scroll container apps/web/src/components/openbooks/AppShell.tsx:463` | Reports + Settings |
| high | Audit table uses a fixed grid-cols-[120px_120px_1fr] on both header and rows with no min-w-0/truncate; under the open AI panel the content column narrows and the Action column crowds/overlaps. Needs auto/auto/minmax(0,1fr) on desktop and a stacked label-value reflow on mobile (no horizontal scroll). | `apps/web/src/components/openbooks/settings/AuditSection.tsx:77 (header), :86 (row)` | Reports + Settings |
| high | Each Rules row packs ~9 inline controls (up arrow, down arrow, grip, #order, name+summary, AI-drafted badge, hit-count, toggle, edit, delete) in one no-wrap flex row; under squeeze these overlap. Edit/delete should move into a row-hover action cluster or overflow menu. | `apps/web/src/components/openbooks/settings/RulesSection.tsx:110-175 (row); controls at :117/:127/:138/:139/:148-156/:157/:166` | Reports + Settings |
| high | Connections shows two developer debug consoles by default instead of owner-facing connection cards. Plaid panel exposes Prepare Link / Open Plaid Link / Use sandbox bypass / Simulate relink + StepRow diagnostics; Stripe panel exposes Validate / Seed / Sync + a checklist grid + 'Integration notes for the main thread'. None of this is gated behind an Advanced/sandbox disclosure. | `apps/web/src/components/openbooks/settings/ConnectionsSection.tsx:93,96; apps/web/src/components/openbooks/PlaidConnectionPanel.tsx:405/422/446/463 (console buttons), :155 (StepRow); apps/web/src/components/openbooks/StripeConnectionPanel.tsx:368-393 (Validate/Seed/Sync), :543-551 (integration notes)` | Reports + Settings |
| medium | Dashboard renders competing page titles: shared PageHeader title 'Dashboard' immediately followed by card h2 'Operating snapshot' plus up to ~10 more section h2s, plus the un-prototyped onboarding checklist card (100-138) and fresh-entity card (140-155) that push the cash metric down the page. Header should collapse to one title; delete the Operating snapshot card. | `apps/web/src/components/openbooks/CoreScreens.tsx:80,104,142` | Dashboard + Inbox |
| medium | Period control is a body-buried month-only shadcn Select inside the 'Operating snapshot' card with no presets (This month/Last month/Last 3 months/YTD) and no custom range; it scrolls away because it is not header-anchored. Should become a header-right DateRangeControl with a future-disabled calendar. | `apps/web/src/components/openbooks/CoreScreens.tsx:83-97` | Dashboard + Inbox |
| medium | Dashboard card actionability/deep-link gaps: only inner figures link, not whole cards; income-by-customer links to /contacts (not Income lens), AR/AP links to generic /invoices, and several drill-throughs do not carry the active period into the target (e.g. /transactions, /bills). IA rule: every tile is a single click target into the matching workbench carrying the period. | `apps/web/src/components/openbooks/CoreScreens.tsx:181,190,308` | Dashboard + Inbox |
| medium | Tabs are a Button group, not shadcn Tabs / the prototype green-underline pattern (tabs.tsx primitive exists but is unused on this surface). | `apps/web/src/components/openbooks/ModuleScreens.tsx:976-981` | Bills + Contacts + Payroll |
| medium | 'Add employee' button is a dead control (no onClick / no modal); prototype opens an add-employee modal. | `apps/web/src/components/openbooks/ModuleScreens.tsx:1023-1026` | Bills + Contacts + Payroll |
| medium | approveRun and markRunPaid post immutable ledger entries directly from single buttons with only a busy guard — no AlertDialog confirm, although alert-dialog.tsx exists (R6 safety gap on irreversible posting). | `apps/web/src/components/openbooks/ModuleScreens.tsx:1174,1179; convex/payroll.ts:379,594` | Bills + Contacts + Payroll |
| medium | Payroll expense and contractor pay are commingled in one ledger account ('Payroll & Contractors', number 5000) with no workerType field to split them — blocks any contractor-vs-employee breakdown. | `convex/payroll.ts:23,374-378,396` | Bills + Contacts + Payroll |
| medium | Run-detail 7-column editable grid is a single overflow-x-auto desktop table forcing ~720px min width on mobile (w-24 adjustment + w-20 FX + checkbox) — must become a responsive card-per-row stack, not a squeezed desktop table. | `apps/web/src/components/openbooks/ModuleScreens.tsx:1211-1238` | Bills + Contacts + Payroll |
| medium | Statement tab is roster-only (statementRows = current employees) with no period chooser; PayrollRunStatement is locked to one run — no way to view a past statement period. | `apps/web/src/components/openbooks/ModuleScreens.tsx:1401-1449; convex/moduleViews.ts:442-449` | Bills + Contacts + Payroll |
| medium | Generic statusChip (outline Badge, capitalize) drops semantic status color across Bills, Payroll runs and Invoices (Paid=green / Approved=info-blue / Draft=neutral / Overdue=negative) — all statuses look identical product-wide. | `apps/web/src/components/openbooks/ModuleScreens.tsx:94-96 (used at :443,:750,:1106)` | Bills + Contacts + Payroll |
| medium | Quarterly drill-down leaks the whole year: StatementTable filters drillDown by date.startsWith(columnKeyToPrefix(key)), and columnKeyToPrefix returns only the 4-digit year for quarter keys, so a quarter column's drill lists every line in the year, not that quarter. | `apps/web/src/components/openbooks/ReportsScreen.tsx:502 (filter), :521-524 (columnKeyToPrefix year-only for quarter keys)` | Reports + Settings |
| medium | Dashboard period= drill-through is silently ignored by Reports: ReportsScreen reads only start/end from the URL, never period=. CoreScreens emits /reports?report=profit-and-loss&period=<month> (and a bare /reports?period=<month> with no report=, which lands on the home grid carrying a dead param), so drill-throughs land on the wrong/default month. | `apps/web/src/components/openbooks/ReportsScreen.tsx:1135-1136 (no period= read); apps/web/src/components/openbooks/CoreScreens.tsx:172 (bare /reports?period=), :231/:243 (report=&period=)` | Reports + Settings |
| medium | Close-the-books banner + readiness checklist is absent from Reports home (prototype shows a green Close CTA / Locked pill gated on a 5-check flow). The only lock affordance lives outside Reports (a bare date Input in AccountingPanel), with no checklist, no reopen flow, no Reports-home entry point. | `apps/web/src/components/openbooks/ReportsScreen.tsx:1232-1238 (home renders only ReportsHome); lock affordance lives in AccountingPanel.tsx:269-276` | Reports + Settings |
| medium | Six report tables become horizontally-scrollable desktop tables on narrow/mobile widths (overflow-x-auto) instead of reflowing to stacked label/value cards; violates the real-mobile-surface rule. GL/Trial Balance/Journal especially dense. | `apps/web/src/components/openbooks/ReportsScreen.tsx:472,704,778,814,849,894` | Reports + Settings |
| medium | Stripe payout child table is wrapped in overflow-x-auto inside a Settings card (5-col Table), so it scrolls horizontally within the card on narrow widths instead of reflowing to rows below md. Summary header is a 5-col grid that also crowds. | `apps/web/src/components/openbooks/StripeConnectionPanel.tsx:236 (overflow-x-auto + Table), :215 (md:grid-cols-[1.2fr_1fr_1fr_1fr_auto] summary)` | Reports + Settings |
| medium | AI section default view includes verification-harness telemetry tables (Batch runs and Categorization eval with accuracy %, correctCount/evaluatedCount, providerMode) that are not owner config; they should be demoted to a collapsed Diagnostics disclosure or moved to Audit. | `apps/web/src/components/openbooks/settings/AiSection.tsx:210-228 (Batch runs), :230-252 (Categorization eval)` | Reports + Settings |
| medium | Heavy Plaid (655 LOC) and Stripe (555 LOC) multi-button consoles were never simplified for the ~390px mobile drill-in surface; they render as a wall of stacked buttons rather than a single-action-per-row layout. | `apps/web/src/components/openbooks/PlaidConnectionPanel.tsx (full); apps/web/src/components/openbooks/StripeConnectionPanel.tsx (full); mobile drill at apps/web/src/components/openbooks/SettingsScreen.tsx:134-148` | Reports + Settings |
| low | automationRate is labeled '{n}% reviewed' but computes share of transactions whose review !== needs_review, not a true automation rate — a metric/label mismatch presented to the owner. | `apps/web/src/components/openbooks/CoreScreens.tsx:185 / convex/coreViews.ts:153,224` | Dashboard + Inbox |
| low | Cash-flow BarChart plots netMinor only, collapsing the prototype's dual in/out bars into a single net bar and dropping the net-positive insight line; the expense BarChart truncates category names to the first word (item.name.split(' ')[0]) so 'Software & SaaS' becomes 'Software' — a data-fidelity regression. | `apps/web/src/components/openbooks/CoreScreens.tsx:338,249` | Dashboard + Inbox |
| low | Income-by-customer fallback silently mixes metrics: if no invoice was issued in the selected month it falls back to all-time amountPaidMinor instead of period totalMinor, with no label distinguishing issued-this-month from lifetime-paid. | `convex/coreViews.ts:172-183` | Dashboard + Inbox |
| low | Net income and AR/AP-net StatCards use plain <Amount> with default neutral tone and no sign signal; a negative net income or AR<AP net shows as an ordinary black figure with no visual cue of which side dominates. Tone/sign policy should derive from semantic role (D7). | `apps/web/src/components/openbooks/CoreScreens.tsx:170-177,187-195` | Dashboard + Inbox |
| low | Activity feed renders {entry.date} - {entry.source} where source is the raw journalEntry.source enum, surfacing machine tokens to the owner (the inbox-kind path at least de-underscores via categoryLabel). | `apps/web/src/components/openbooks/CoreScreens.tsx:276 / convex/coreViews.ts:256` | Dashboard + Inbox |
| low | Dashboard responsive: StatCard hero figures use text-2xl with no min-w-0/truncate (a 9-digit cash figure overflows the 4-up grid column); 'Cash and credit' rows use grid-cols-[1fr_auto_auto] that squeezes on narrow widths (prototype flex-wraps); income-by-customer and activity name spans lack min-w-0 so long names push the money figure off-row. | `apps/web/src/components/openbooks/primitives.tsx:86 / apps/web/src/components/openbooks/CoreScreens.tsx:206,275,309` | Dashboard + Inbox |
| low | All-businesses / combined dashboard mode is entirely absent; DashboardScreen always scopes to one activeEntity via entityArg. The prototype's all-mode hero ($131,883.16) has no backend concept — flagged as needing the per-surface real/partial/mock map (Q1) before design. | `apps/web/src/components/openbooks/CoreScreens.tsx:55-60` | Dashboard + Inbox |
| low | KPI strip is per-currency totals + Headcount only; base currency is not pinned first (currencyTotals sorted alphabetically), and Next-run/taxes/contractors/unmatched tiles are absent. | `apps/web/src/components/openbooks/ModuleScreens.tsx:985-999; convex/moduleViews.ts:438-440` | Bills + Contacts + Payroll |
| low | Employees, Runs and global Statement tables are not wrapped in overflow-x-auto; right-aligned money columns clip rather than scroll on narrow viewports. | `apps/web/src/components/openbooks/ModuleScreens.tsx:1029,1087,1421` | Bills + Contacts + Payroll |
| low | Reports toolbar state is not sticky across report switches: the init effect hard-resets basis, compare and columnMode every time the selected report changes, so a chosen basis/compare/columns is lost when switching reports. | `apps/web/src/components/openbooks/ReportsScreen.tsx:1169-1172 (setColumnMode/setCompare/setBasis on every report change)` | Reports + Settings |
| low | Redundant per-section h2 in SectionBody re-prints the section label that the subnav already names, on every Settings section. | `apps/web/src/components/openbooks/SettingsScreen.tsx:210-214 (h2 + description block)` | Reports + Settings |
| low | Settings sits in the primary nav list (below a divider) and in ProfileMenu, not a quiet footer/utility cluster as Ansar requested. Move the Settings entry into the sidebar footer alongside Sync + Profile. | `apps/web/src/components/openbooks/AppShell.tsx:679-697, :787-804` | Ask AI + Shell |

## Anchor drift (report citation vs current code)

| Status | What | Report ref | Current ref | Note |
|---|---|---|---|---|
| drifted | CoreScreens.tsx total length (report stated 1235) | `CoreScreens.tsx:1235 LOC` | `apps/web/src/components/openbooks/CoreScreens.tsx:1236` | File is now 1236 lines (1-line drift). All Dashboard/Inbox anchors land <=716 and are unaffected. |
| drifted | Bills routed in AppScreen | `ModuleScreens.tsx:1918` | `apps/web/src/components/openbooks/ModuleScreens.tsx:1918 (ModuleScreens switch); actual AppScreen render at AppScreen.tsx:62` | Report's 2.6 'routed at :1918' points to the internal ModuleScreens switch (section==='bills' returns <BillsScreen/> at :1918), which is valid. The real route conditional is AppScreen.tsx:62. Not a defect, just two different routing layers. |
| missing | 'magenta avatar' claim in prompt | `prompt focus item: 'magenta avatar'` | `apps/web/src/components/openbooks/ModuleScreens.tsx:852 (BillMatchPicker avatar) and Contacts has none` | No magenta/purple avatar exists in current code. BillMatchPicker initials chip is bg-foreground text-background (neutral). Contacts directory and profile use NO avatar at all (only text). The 'magenta avatar' is a prototype-only artifact; the defect in current code is the opposite — Contacts is missing the avatar primitive (no avatar.tsx in ui/). |
| drifted | Docked AI panel is a 380px flex sibling that steals width (squeeze cause), NOT yet an overlay | `AppShell.tsx:463-469,466-479` | `apps/web/src/components/openbooks/AppShell.tsx:467-469 (aside: 'sticky top-0 hidden h-screen w-[380px] shrink-0 ... lg:flex'); main at :463 ('mx-auto w-full max-w-[1200px]')` | Line numbers shifted (aside now :467-469, not :466-479) but root cause unchanged: still a width-stealing flex sibling, no overlay/scrim, no narrow-column fallback. Structural fix not applied. |
| drifted | BusinessesSection magenta avatar #a4148c in avatarColor palette | `BusinessesSection.tsx:45` | `apps/web/src/components/openbooks/settings/BusinessesSection.tsx:46 (['#fce7f6', '#a4148c'] in palette), consumed :81/:90-92` | Line shifted from :45 to :46; #a4148c magenta confirmed, violates one-green + chart-token rule. |

## Design-system token violations

| Issue | Ref | Surface |
|---|---|---|
| ConfidenceRing hardcodes stroke-primary regardless of value, so a 58% ring is the same brand green as 96% — low confidence reads as high. Should be threshold-colored (green/amber/red) via a confidence-color helper. | `apps/web/src/components/openbooks/primitives.tsx:220` | Dashboard + Inbox |
| Native unstyled <input type=checkbox> instead of shadcn Checkbox (Checkbox is absent from the kit) — no DS focus ring, no brand accent. AGENTS.md mandates shadcn primitives before raw controls. | `apps/web/src/components/openbooks/CoreScreens.tsx:537-554` | Dashboard + Inbox |
| Inbox detail/list amounts use <Amount amountMinor> with default tone='neutral' and no signed flag; money-in transfers/payouts never receive the income green tone, and the backend negation (coreViews.ts:327) makes receipts render as unsigned-negative. No income/expense direction semantics. | `apps/web/src/components/openbooks/CoreScreens.tsx:558,584` | Dashboard + Inbox |
| Accounting/AR jargon in owner-facing copy: 'Operating snapshot', 'ledger-backed books', 'Ledger lines' chip, 'A/R and A/P' heading — DS/IA mandate plain English ('Money you're owed' / 'Money you owe'). | `apps/web/src/components/openbooks/CoreScreens.tsx:80,81,226,286` | Dashboard + Inbox |
| 'Demo entity' CategoryChip rendered into the PageHeader action slot — a build/state label leaking into the product surface; feedback says remove from the body (keep only a quiet header indicator per D6). | `apps/web/src/components/openbooks/AppScreen.tsx:54` | Dashboard + Inbox |
| Redundant entity label: entity name shown both as PageHeader eyebrow and inside the snapshot card subtitle with 'ledger-backed books' jargon. DS calls for one entity surface. | `apps/web/src/components/openbooks/AppScreen.tsx:51 + apps/web/src/components/openbooks/CoreScreens.tsx:81` | Dashboard + Inbox |
| Dashboard hero cash figure is not Geist Mono / tabular: StatCard value is a plain text-2xl div with no money-figures class, so the cash total renders proportional. Money must always be money-figures + tabular-nums. | `apps/web/src/components/openbooks/primitives.tsx:86` | Dashboard + Inbox |
| Inbox subline renders categoryLabel(item.kind) which only underscore-replaces the raw enum, so a row shows 'payout mismatch'/'connection' rather than owner-language section copy. | `apps/web/src/components/openbooks/CoreScreens.tsx:560` | Dashboard + Inbox |
| Unicode arrow '→' used as a functional glyph in the CSV column-mapper copy ('Date → column 1 · Description → column 2 · Amount → column 3'). DS bans unicode-as-icon ('no → as functional glyphs'); use a lucide icon or plain text. | `apps/web/src/components/openbooks/CoreScreens.tsx:1217` | Transactions |
| Unicode middle-dot '·' used as a separator glyph in the CSV mapper rows. Lower severity but still a non-lucide unicode glyph per Appendix A rules. | `apps/web/src/components/openbooks/CoreScreens.tsx:1217 and :1218` | Transactions |
| Unicode middle-dot '·' separator in the receipt-preview subline ({date} · {status}). | `apps/web/src/components/openbooks/CoreScreens.tsx:1096` | Transactions |
| Raw <input type="checkbox"> used instead of a shadcn Checkbox primitive (ui/checkbox is missing). Violates 'build on shadcn primitives before raw controls'. | `apps/web/src/components/openbooks/CoreScreens.tsx:998-1011` | Transactions |
| Raw <textarea> with hand-rolled focus-ring utility classes (focus-visible:ring-[3px] focus-visible:ring-ring/50) instead of a shadcn Textarea primitive. | `apps/web/src/components/openbooks/CoreScreens.tsx:1220-1225` | Transactions |
| Status column renders the bare review enum via CategoryChip with only underscore-swapped text ('needs review','auto'), not plain-English owner copy. Not a color violation (CategoryChip is brand-green/neutral only, no red), but a DS tone/copy violation. | `apps/web/src/components/openbooks/CoreScreens.tsx:1033 (chip) + :43-45 (categoryLabel)` | Transactions |
| Date column renders in the default UI font, not Geist Mono / tabular figures. DS requires Geist Mono + tabular-nums for ALL dates and account numbers; only <Amount> uses money-figures, dates do not. | `apps/web/src/components/openbooks/CoreScreens.tsx:1013 (<TableCell>{row.date}</TableCell>)` | Transactions |
| Money tone wiring missing: every register/panel Amount passes a blanket `signed` with no tone, so income never renders brand-green and there is no explicit neutral-expense tone (relies on Amount's default). Prototype mandates income green / expenses neutral; the call sites must pass tone=income/expense, not a blanket signed flag. | `apps/web/src/components/openbooks/CoreScreens.tsx:1034 and :1059` | Transactions |
| Overdue KPI amount uses raw Tailwind text-red-600 (twice: span class + Amount className) instead of a semantic negative token — alarm-red on money the DS warns against | `apps/web/src/components/openbooks/IncomeScreen.tsx:115` | Income + Expenses |
| Overdue invoice due-date cell uses raw text-red-600 conditional | `apps/web/src/components/openbooks/IncomeScreen.tsx:245` | Income + Expenses |
| Overdue + refunded status chips use raw bg-red-50/text-red-700 utilities | `apps/web/src/components/openbooks/IncomeScreen.tsx:28,31` | Income + Expenses |
| Overdue note banner in invoice detail uses raw bg-red-50 text-red-700 | `apps/web/src/components/openbooks/IncomeScreen.tsx:651` | Income + Expenses |
| Open + reconciled status chips use raw bg-blue-50/text-blue-700 — off the brand palette entirely; should route through --info, and a blue accent fights the one-green rule | `apps/web/src/components/openbooks/IncomeScreen.tsx:26,30` | Income + Expenses |
| Receivables heat cells hardcode inline orange rgba(247,144,9,a) ramp (#f79009) instead of --warning/--chart-3 amber token | `apps/web/src/components/openbooks/IncomeScreen.tsx:268` | Income + Expenses |
| Stripe blurple #635bff hardcoded as inline bg-[#635bff] (composer header badge and hosted-link badge) — defensible as a Stripe-only chip but bypasses any token | `apps/web/src/components/openbooks/IncomeScreen.tsx:476,614` | Income + Expenses |
| Raw HTML <select> with hand-rolled classes for invoice Terms instead of shadcn Select primitive | `apps/web/src/components/openbooks/IncomeScreen.tsx:508-510` | Income + Expenses |
| Bespoke segmented tab control + a second bespoke invoice sub-filter tab style instead of shadcn Tabs primitive (tabs.tsx exists) | `apps/web/src/components/openbooks/IncomeScreen.tsx:81-95,211-224` | Income + Expenses |
| tracking-wide (0.025em letter-spacing) on Receivables uppercase column header — violates DS 'keep implementation letter spacing at 0' (report cited this only for Expenses, missed this Income instance) | `apps/web/src/components/openbooks/IncomeScreen.tsx:290` | Income + Expenses |
| DOTS palette contains off-brand category swatches incl. Stripe blurple #635bff (idx 6) and plum #7a4a8c (idx 9), plus #1d6bb5 blue, used as generic category dots (:109) and share-bar fills (:116) instead of --chart-1..5 tokens. #7a4a8c plum does NOT appear in the prototype — a genuine off-palette addition | `apps/web/src/components/openbooks/ExpensesScreen.tsx:29` | Income + Expenses |
| Unicode geometric glyphs ▲/▼ used as functional trend icons (deltaLabel, Spent KPI detail, Biggest-movement value) — DS bans unicode-as-icon; should be lucide TrendingUp/TrendingDown | `apps/web/src/components/openbooks/ExpensesScreen.tsx:26,71,80` | Income + Expenses |
| Raw HTML <select> with hand-rolled classes in AddCategoryModal Group picker instead of shadcn Select (select.tsx exists, used in CoreScreens) | `apps/web/src/components/openbooks/ExpensesScreen.tsx:215-217` | Income + Expenses |
| tracking-wide (0.025em) on uppercase category-table header and 'Recurring' label — violates DS letter-spacing-0 rule | `apps/web/src/components/openbooks/ExpensesScreen.tsx:95,152` | Income + Expenses |
| Pervasive arbitrary px font sizes (text-[12.5px], text-[11.5px], text-[13px], text-[11px], text-[10.5px]) bypass the DS type scale (12 meta / 14 body / 16 card-title) | `apps/web/src/components/openbooks/ExpensesScreen.tsx:58,96,111,114,118,119,153,160,161,164,165` | Income + Expenses |
| Hardcoded brand hex on a raw checkbox accent instead of shadcn Checkbox + --primary token (no Checkbox primitive exists in ui/). This is the only raw #hex in the cluster. | `apps/web/src/components/openbooks/ModuleScreens.tsx:1331 (accent-[#2ca01c])` | Bills + Contacts + Payroll |
| Raw float money string in the approval banner (localMinor/100) — no tabular/mono figures, no Amount, no thousands separators; violates the money-formatting design rule. | `apps/web/src/components/openbooks/ModuleScreens.tsx:1194` | Bills + Contacts + Payroll |
| Raw HTML <select> with hand-rolled border/padding classes instead of the shadcn Select primitive (DS: build on shadcn primitives before raw controls). | `apps/web/src/components/openbooks/ModuleScreens.tsx:625-632` | Bills + Contacts + Payroll |
| Bills register uses bespoke div-grid rows rather than the shadcn Table the file already imports and uses for Invoices/Payroll — inconsistent with the DS dense-table pattern. | `apps/web/src/components/openbooks/ModuleScreens.tsx:741-761` | Bills + Contacts + Payroll |
| Payroll tabs and Contacts/Payroll filters are Button groups, not shadcn Tabs / a real FilterBar — diverges from the DS tab + filter affordance. | `apps/web/src/components/openbooks/ModuleScreens.tsx:976-981 (payroll tabs); :185-195 (contacts role pills)` | Bills + Contacts + Payroll |
| Role chips pass raw lowercase enum strings to CategoryChip with no Title-case label and no per-role color semantics. | `apps/web/src/components/openbooks/ModuleScreens.tsx:232; primitives.tsx:194-211` | Bills + Contacts + Payroll |
| Off-palette teal accent bg-teal-600 in ACCENT_BG (used by home-card PreviewViz + Cash Flow bridge bars) instead of a chart token | `apps/web/src/components/openbooks/ReportsScreen.tsx:141` | Reports + Settings |
| Off-palette amber accent bg-amber-500 in ACCENT_BG (used by PreviewViz + Cash Flow outflow bars) instead of a chart/negative token | `apps/web/src/components/openbooks/ReportsScreen.tsx:142` | Reports + Settings |
| Raw amber status chip bg-amber-100 text-amber-800 (Balance Sheet 'Needs review') instead of a semantic warning token | `apps/web/src/components/openbooks/ReportsScreen.tsx:591` | Reports + Settings |
| Raw amber status chip bg-amber-100 text-amber-800 (Trial Balance 'Off by N') instead of a semantic warning token | `apps/web/src/components/openbooks/ReportsScreen.tsx:888` | Reports + Settings |
| Unicode checkmark glyph used as status icon in Balance Sheet 'Balanced' chip (DS bans unicode-as-icon; use lucide Check/CircleCheck) | `apps/web/src/components/openbooks/ReportsScreen.tsx:593` | Reports + Settings |
| Unicode checkmark glyph used as status icon in Trial Balance 'Balanced' chip (use lucide Check/CircleCheck) | `apps/web/src/components/openbooks/ReportsScreen.tsx:889` | Reports + Settings |
| Unicode arrow glyph used in Monthly Review hero band ('You made X, spent Y -> Z') instead of lucide ArrowRight | `apps/web/src/components/openbooks/ReportsScreen.tsx:1002` | Reports + Settings |
| Unicode arrow in MrSection link labels (Income by Customer / Full P&L / AR Aging / AP Aging) instead of lucide ArrowRight | `apps/web/src/components/openbooks/ReportsScreen.tsx:1008,1020,1032,1041` | Reports + Settings |
| Inline raw amber rgba literal rgba(245,158,11,..) for AR/AP aging heat cells, applied as inline backgroundColor, off any chart/warning token | `apps/web/src/components/openbooks/ReportsScreen.tsx:686 (heat()), :721 (applied)` | Reports + Settings |
| BusinessesSection avatar palette includes magenta #a4148c (with #fce7f6), outside the one-green + chart-token rule | `apps/web/src/components/openbooks/settings/BusinessesSection.tsx:46` | Reports + Settings |
| Settings subnav active state hardcodes brand hex bg-[#f1f8ee] text-[#17540f] (and inactive text-[#454545]) instead of --primary/--muted-foreground tokens | `apps/web/src/components/openbooks/SettingsScreen.tsx:170,171` | Reports + Settings |
| AuditSection actor pills hardcode brand-green #f1f8ee/#1d6b12, info-blue #eff8ff/#175cd3, and grey #525252 instead of --primary/--info/--muted-foreground tokens | `apps/web/src/components/openbooks/settings/AuditSection.tsx:20-25` | Reports + Settings |
| AuditSection row action text hardcodes #3d3d3d | `apps/web/src/components/openbooks/settings/AuditSection.tsx:99` | Reports + Settings |
| RulesSection AI-suggestion banner and AI-drafted chip hardcode brand-green #f1f8ee/#17540f/#1d6b12 instead of --primary tokens | `apps/web/src/components/openbooks/settings/RulesSection.tsx:86,87,88,145` | Reports + Settings |
| RulesSection rule on/off control is a hand-rolled toggle pill (raw button h-[19px] w-[34px] rounded-full with sliding span) instead of shadcn Switch (switch.tsx exists in ui/ but is not imported) | `apps/web/src/components/openbooks/settings/RulesSection.tsx:148-156` | Reports + Settings |
| RulesSection auto-post uses a raw <input type=checkbox> with accent-[#2ca01c] hack instead of a Checkbox primitive (no checkbox.tsx exists in ui/) | `apps/web/src/components/openbooks/settings/RulesSection.tsx:335` | Reports + Settings |
| AiSection provider/key/connection chrome hardcodes brand-green #f1f8ee/#17540f and label grey #525252 instead of --primary/--muted-foreground tokens | `apps/web/src/components/openbooks/settings/AiSection.tsx:98,113,119,125,134` | Reports + Settings |
| NotificationsSection toggle is a hand-rolled pill (button h-[19px] w-[34px] rounded-full) instead of shadcn Switch | `apps/web/src/components/openbooks/settings/NotificationsSection.tsx:84` | Reports + Settings |
| CategoriesSection accountant-mode toggle is a hand-rolled pill (h-[19px] w-[34px] rounded-full) instead of shadcn Switch | `apps/web/src/components/openbooks/settings/CategoriesSection.tsx:89` | Reports + Settings |
| TeamSection avatar palette hardcodes off-token swatches: owner #17540f/#ffffff, pending warning #fffaeb/#b54708, grey #f0f0f0/#525252; plus 'Invite sent' chip #fffaeb/#b54708 and #525252 role chip and #17540f success text | `apps/web/src/components/openbooks/settings/TeamSection.tsx:29,43,57,61,145` | Reports + Settings |
| DataSection danger zone built from arbitrary hex/rgba: inline boxShadow rgba(217,45,32,0.25), text-[#b42318], border-[#f1c2bd], hover:bg-[#fef3f2] instead of --negative / --negative-surface tokens | `apps/web/src/components/openbooks/settings/DataSection.tsx:75,76,80` | Reports + Settings |
| PlaidConnectionPanel sandbox-bypass uses a raw <input type=checkbox> with accent-[#2ca01c] hack instead of a Checkbox primitive | `apps/web/src/components/openbooks/PlaidConnectionPanel.tsx:501,504` | Reports + Settings |
| 'Bedrock active' provider-name string leaked in owner-facing Badge (vendor wording banned; replace with quiet capability state) | `apps/web/src/components/openbooks/ModuleScreens.tsx:1568` | Reports + Settings |
| User-facing vendor/provider label 'Bedrock active' / 'Degraded mode' in chat header Badge — leaks infra naming (quiet-AI / plain-English violation). | `apps/web/src/components/openbooks/OpenBooksAIChat.tsx:758` | Ask AI + Shell |
| Vendor label 'Bedrock active'/'Degraded mode' in the full-page header CategoryChip. | `apps/web/src/components/openbooks/AskAIScreen.tsx:45` | Ask AI + Shell |
| Vendor label 'Bedrock active'/'Degraded mode' in Settings AI card Badge (plus 'Bedrock is the v1 target' copy at :1580). | `apps/web/src/components/openbooks/ModuleScreens.tsx:1568` | Ask AI + Shell |
| ai.ts status label hardcodes vendor 'Bedrock provider is configured', surfaced to the user via the status card. | `apps/web/src/lib/openbooks/ai.ts:143` | Ask AI + Shell |
| Status-card copy names the backend framework 'Convex Agent' to the owner. | `apps/web/src/components/openbooks/OpenBooksAIChat.tsx:807` | Ask AI + Shell |
| Off-token brand-green hex on inline AI link chip (border-[#bbe0a9] bg-[#f1f8ee] text-[#1d6b12] hover:bg-[#dcefd2]) instead of --primary/token. | `apps/web/src/components/openbooks/OpenBooksAIChat.tsx:185` | Ask AI + Shell |
| Off-token brand-green hex on active-thread state (border-[#bbe0a9] bg-[#f1f8ee]). | `apps/web/src/components/openbooks/OpenBooksAIChat.tsx:508` | Ask AI + Shell |
| Off-token brand-green hexes on the Ask AI header pill (border-[#bbe0a9] bg-[#f1f8ee] text-[#1d6b12] hover:bg-[#dcefd2] hover:text-[#1d6b12]; aiOpen border-[#92cc7a] bg-[#dcefd2]). | `apps/web/src/components/openbooks/AppShell.tsx:450-451` | Ask AI + Shell |
| Off-token green hex on the Ask AI pill ⌘J hint (text-[#63b347]). | `apps/web/src/components/openbooks/AppShell.tsx:458` | Ask AI + Shell |
| Off-token active nav-item hexes in ExpandedSidebar (bg-[#f1f8ee] font-semibold text-[#17540f]; inactive text-[#454545]). | `apps/web/src/components/openbooks/AppShell.tsx:668-669` | Ask AI + Shell |
| Off-token active Settings-link hexes in ExpandedSidebar (bg-[#f1f8ee] text-[#17540f]). | `apps/web/src/components/openbooks/AppShell.tsx:690-691` | Ask AI + Shell |
| Off-token divider hex bg-[#ececec] in nav (repeated at :759, :785). | `apps/web/src/components/openbooks/AppShell.tsx:681` | Ask AI + Shell |
| Off-token active nav/Settings hexes in CollapsedRail (bg-[#f1f8ee] text-[#17540f]; text-[#454545]). | `apps/web/src/components/openbooks/AppShell.tsx:773,796` | Ask AI + Shell |
| Off-token EntitySwitcher chip hexes (bg-[#dcefd2] text-[#17540f]) — repeated in the entity-option rows at :865. | `apps/web/src/components/openbooks/AppShell.tsx:843` | Ask AI + Shell |
| Money table <th> headers always text-left font-medium with no money-figures/tabular-nums — proportional left-aligned header over right-aligned tabular body cells. | `apps/web/src/components/openbooks/OpenBooksAIChat.tsx:226` | Ask AI + Shell |
| Index-based money alignment: only cellIndex>0 gets money-figures text-right (assumes every non-first column is money) — non-tabular money / wrong alignment for label-bearing columns. | `apps/web/src/components/openbooks/OpenBooksAIChat.tsx:238` | Ask AI + Shell |

## Per-cluster notes

### Dashboard + Inbox

Re-verification verdict: every report-cited anchor for the Dashboard + Inbox cluster is VALID against current code, with one cosmetic drift — CoreScreens.tsx is now 1236 lines (report said 1235); all cluster anchors land at or below line 716 and are unaffected. No anchor is missing. Cited report sections (2.1, 2.2, 6.1, 6.2, 8.2, 11 R3) describe the current code accurately.

Cross-cluster prerequisites confirmed: (1) The --negative / --negative-surface tokens the redesign relies on do NOT exist in apps/web/src/app/globals.css (only --positive/--warning/--info/--chart-1..5). text-negative/bg-negative would not compile today — the token layer (Epic 1 / D5) must land before any color fix on these surfaces. (2) The Inbox negative-on-documents bug (coreViews.ts:327) and the money sign/tone policy belong in the Amount primitive + view layer per D7; Epic 1 should make Amount tone derive from semantic role and Epic 3 should drop the backend negation.

Epic ownership rationale: Inbox surface defects (grouping, per-kind cards, drawer, sign correctness, batch isolation) are owned by Epic 3 (Transactions + Inbox). Dashboard defects (one-title header, header DateRangeControl, card actionability/deep-links, metric labels, chart fidelity, all-entities mode) are owned by Epic 6 (Payroll + Reports + Settings polish) per the plan's surface-to-epic mapping, since Dashboard is not assigned to Epic 3. Shared primitive fixes (ConfidenceRing thresholding, shadcn Checkbox, StatCard money-figures, Amount tone policy) are Epic 1. The Demo-entity chip and the eyebrow/title duplication live in shell/PageHeader (AppScreen.tsx) and are Epic 2 shell territory, but I left the chip listed as an Epic 6 Dashboard copy item where it manifests on this surface; treat AppScreen.tsx:51/54 as a shared shell fix coordinated between Epic 2 and the surface epics.

Two items need an Ansar/backend decision before layout (Q1): the all-businesses combined dashboard has no backend concept, and the per-kind Inbox evidence (transfer pairs, payout breakdown, connection state, question candidates) is not returned by coreViews.inbox today — Epic 3 must extend the query, not just the UI.

### Transactions

All cited report anchors for the Transactions cluster re-verified VALID against current code — no drift; the report's own correction (AppScreen mount is :59 not :60) holds. Key correction to the report's framing of R3: the Amount primitive (apps/web/src/components/openbooks/primitives.tsx:48-54) does NOT color negatives red and applies NO color at all unless tone is income/expense, so the on-screen claim 'expenses styled like income / alarm red' is imprecise — the real, verified defect is that NEITHER income (should be green) nor expense (should be explicit neutral) tone is wired; income gets only a leading '+'. CategoryChip (primitives.tsx:194-211) and ConfidenceRing (:213-233) use only brand-green/neutral tokens — no raw red on this surface, so needs_review does NOT read as alarm red. Confirmed for the new FilterBar/table: the transactions doc carries contactId (schema.ts:354), reasoning (:370), confidence (:369), decidedBy (:358-368), source (:352), review (:346-351), but the query row shape (coreViews.ts:464-503) currently returns only confidence/decidedBy/source/review/receipt/hasInboxItem and OMITS contactId/contactName + reasoning + a receipt-present boolean — Epic 3 must extend both coreViews.transactions args (currently entityId/review/search only) and the row shape. Owner of every Transactions defect = Epic 3 (Transactions + Inbox). Cross-surface dependencies: ExportMenu/FilterBar/DateRangeControl/AccountMultiSelect/DetailSheet/AiInsightBadge/AttentionState primitives come from Epic 1; shadcn Checkbox + Textarea + Sheet adoption also depend on Epic 1 primitives.

### Income + Expenses

Re-verified every report anchor for Income+Expenses against current code — ALL cited anchors are VALID (no drift; line numbers in incomeViews.ts/expensesViews.ts/IncomeScreen.tsx/ExpensesScreen.tsx still match exactly). Files are unchanged since the report was written.

LENS PROOF (the core IA question): The two views do NOT read the same table. Expenses IS a genuine journal-line lens — categoryTotals sums debitMinor-creditMinor over expense accounts from the journalLines table (expensesViews.ts:80-98,194), which is what makes category totals reconcile to the P&L. Income PAYMENTS, by contrast, derive from the transactions table + stripePayouts (incomeViews.ts:103-104,111-137), NOT journalLines; only Income's receivables/AR aging reuses buildAgingRows from reportViews (incomeViews.ts:173). So 'same transactions + journalLines' is half-true: Expenses=journalLines-backed, Income payments=transactions-backed, Income AR=invoices via shared aging math. Both are legitimate read-models, but Epic 4 must make this explicit and ensure Income payments drill into the SAME transaction record (the txn._id at incomeViews.ts:113 is already present but unused — the single biggest lens defect).

KPI GAPS confirmed missing in backend (not just UI): Income lacks recurringRevenue/MRR; Expenses lacks uncategorizedCount, missingEvidenceCount, topVendor (expensesViews.ts:266-274). These require new backend fields, not just UI work.

TOKEN-LAYER BLOCKER (Epic 1, not Epic 4): The report repeatedly prescribes --negative/--negative-surface as the fix for Income's raw reds, but those tokens do NOT exist in apps/web/src/app/globals.css (only --destructive/--positive/--warning/--info/--chart-1..5 at :69-80, no @theme --color-negative utility). text-negative/bg-negative will not compile today. The negative semantic token must be added in Epic 1 before any Income/Expenses red can be re-routed — sequencing dependency for the build epics.

EXPENSES-NEUTRAL rule is already honored (Amount tone='expense' -> text-muted-foreground at primitives.tsx:54; Expenses never uses red). Only IncomeScreen breaks the alarm-red rule. PROTOTYPE FIDELITY: most Expenses 'violations' (7-col grid, ▲/▼, raw select, px fonts, 9/10 dot colors) are faithful reproductions of Expenses.dc.html — the genuinely off-palette ADDITION beyond the prototype is the single plum #7a4a8c dot (DOTS index 9). The 'Stripe blurple leaked from Income' framing is partly wrong: #635bff and #1d6bb5 are both in the prototype too.

ONE ANCHOR OMISSION (not drift): the report flags tracking-wide only in ExpensesScreen; IncomeScreen.tsx:290 also uses tracking-wide on the receivables header — same DS letter-spacing-0 violation, missed by the report.

### Bills + Contacts + Payroll

Cluster Bills+Contacts+Payroll re-verified against current code at /Volumes/SSD/OpenBooks. Nearly every file:line the report cites is still VALID and exact — the cited surfaces have not drifted since the report was written. Drift/correction summary: (1) The prompt's 'magenta avatar' focus item is MISSING in current code — no magenta/purple avatar exists anywhere; BillMatchPicker's initials chip is neutral bg-foreground text-background (ModuleScreens.tsx:852) and Contacts uses NO avatar at all (no avatar.tsx primitive in ui/). The real defect is the opposite: Contacts lacks an avatar entirely. (2) Payroll route: the report's 2.8 prose says 'AppScreen.tsx:65' but the actual conditional render is AppScreen.tsx:64 — the report's own additional-issues note already flags this off-by-one, so treat as valid/self-corrected. (3) lastActivity is returned on the contact row (moduleViews.ts:249) as well as computed (:233-238) — it is dropped at the TYPE layer (module-helpers.ts ContactRow), not the query layer, so the fix is purely frontend type + column. CONFIRMED net-new backend prerequisites that block UI epics: contacts.archived schema field (schema.ts:231-240 has none; needed for D1 soft-archive); a payroll scheduler / pay-schedule (crons.ts:7 is the sole cron, Plaid only; startRun payroll.ts:253 is manual — needed for the 11.0 auto-run decision); run snapshot headcount (moduleViews.ts:528 reads live roster). Missing shadcn primitives confirmed for this cluster: checkbox, popover, scroll-area, avatar are ABSENT in apps/web/src/components/ui/ (tabs, sheet, alert-dialog ARE present and should be wired). Money-correctness R3 bugs are live and in the data/view layer (moduleViews.ts:223-232 revenue+spend conflation; ModuleScreens.tsx:236 AR/AP netting; :1194 float banner) — a visual-only redesign would inherit and polish them. Expense neutral-tone rule is currently HONORED (Amount default tone neutral, expense=text-muted-foreground; primitives.tsx:38-54) — no alarm-red on expenses found in this cluster. No gradients, no emoji, no purple-AI, no glassmorphism, and no unicode-as-icon found in ModuleScreens.tsx for these surfaces.

### Reports + Settings

Re-verified all report-cited anchors for the Reports + Settings cluster against current code. Almost every anchor is VALID; the meaningful drifts are: (1) the docked AI panel moved to AppShell.tsx:467-469 (report cited :463-469/:466-479) and is now 'sticky top-0 hidden h-screen w-[380px] shrink-0 ... lg:flex' — still a width-stealing flex sibling, so the squeeze cause is intact and the overlay fix is NOT applied; (2) BusinessesSection magenta moved from :45 to :46; (3) ReportsScreen teal moved from :140 to :141. The Settings subnav width is w-[190px] (matches the report's 8.5 anchor; section 6.10 prose says 'wrap the desktop nav' — both consistent). 'switch.tsx' DOES exist in ui/ (so 'Switch not imported' is the real deviation — the primitive is available but unused); there is NO 'checkbox.tsx' in ui/, so the Checkbox primitive must be added in Epic 1 before the raw-checkbox fixes. Two confirmed correctness bugs beyond pure styling: the quarterly drill-down year-leak (ReportsScreen.tsx:502/521-524) and the silently-dropped period= dashboard drill (ReportsScreen.tsx:1135 + CoreScreens.tsx:172). Drill-through to the register remains 'Refuted' — Reports only routes to /reports, never to /transactions|/income|/expenses, and the register (CoreScreens.tsx:720) reads only ?focus, so the account/category/date param contract does not exist yet. DrillSheet and the Rules RuleEditor Dialog are both default-CLOSED (correct per IA). Vendor-name leak 'Bedrock' appears in 3 places in ModuleScreens (:1568 badge, :1580 copy, :525 receipt label). Epic ownership: Reports/Settings surface defects -> Epic 6; the shared AI-overlay restructure and the 'Bedrock' badge relabel -> Epic 2 (shell/AskAI); the missing Checkbox primitive + token layer -> Epic 1. No edits were made (read-only audit).

### Ask AI + Shell

DRIFT VERDICT: zero drift. Every file:line the report cites for the Ask AI + Shell cluster re-verifies exactly against current code (OpenBooksAIChat.tsx is still exactly 918 lines; AppShell.tsx 1040). All anchors = valid. The current files match the brief's expected line numbers (provider leak :758, status copy :804/:807, ai.ts :143, :: nonce :226/:644, <select> :762, docked panel :466-469, SUGGESTIONS dup :33 vs ai.ts:121).

EPIC 2 PRESERVATION CONTRACT (must NOT break): the Convex Agent streaming stack is intact and is the load-bearing backend — useUIMessages(api.aiThreads.listThreadMessages,...,{initialNumItems:40,stream:true}) at OpenBooksAIChat.tsx:570-574; useSmoothText(text,{startStreaming,charsPerSec:220}) :317; useMutation(api.aiThreads.sendMessage).withOptimisticUpdate(optimisticallySendMessage(...)) :560-562; message.status 'streaming'|'pending' :436. Thread lifecycle api.aiThreads.createThread/deleteThread/listMine and proposals api.proposals.listProposals/confirmProposal/dismissProposal are unchanged. This is @convex-dev/agent/react, NOT the Vercel AI SDK useChat — Epic 2 adapts only the view layer.

DEPENDENCY STATE: apps/web/package.json has cmdk ^1.1.1 (good for the Command/Combobox switcher) but NONE of ai / @ai-sdk/* / ai-elements / streamdown. The AI stack (ai ^6, @ai-sdk/amazon-bedrock ^4, @convex-dev/agent ^0.6.3) lives only in ROOT package.json:38-42. No apps/web/src/components/ai-elements dir exists. Epic 2 must add streamdown (+ ai for UIMessage types) to apps/web and pull the AI Elements registry.

TOKEN PREREQUISITE FOR EPIC 1: there is NO --ob-green ramp and NO --ai token. globals.css defines only --primary:#2ca01c (:61), --ring:#2ca01c (:72), --positive. The active/AI tint green (#f1f8ee / #17540f / #dcefd2 / #bbe0a9 / #1d6b12 / #63b347 / #92cc7a) has no semantic token to migrate to — Epic 1 must DEFINE the tint/AI tokens in globals.css BEFORE any hex replacement; 'use the token' currently has no target. This is why most token violations above are blocked on Epic 1 even though they live in Epic 2 files.

OWNERSHIP NOTE: the Settings-card provider leak (ModuleScreens.tsx:1568) and 'Settings in primary nav' (AppShell.tsx:679-697) physically live in Epic 6 surfaces but share the ai.ts:143 leak source and the shell nav respectively — flagged here so Epic 2's ai.ts relabel and shell rework coordinate with Epic 6.

CONTEXT-AWARENESS: confirmed ReportsScreen.tsx:1269 is the ONLY createAiRequestEvent/dispatchEvent call site in the codebase (grep-verified). Income/Expenses/Transactions/Bills do not push scope; the per-surface launch wiring is Epics 3/4/5 work but the receiving widget contract (structured {prompt,nonce} payload replacing the '::' nonce) is Epic 2.

