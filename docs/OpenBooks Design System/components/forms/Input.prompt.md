Standard 32px text input with green focus ring.

```jsx
<Input placeholder="Vendor name" />
<Input icon="search" placeholder="Search transactions" />
```

Use `icon="search"` for all search fields. Amounts entered by users still use this input (type="text"), right-aligned via style when in tables.
