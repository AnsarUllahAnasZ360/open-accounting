# Launch-sprint backlog reconciliation

Generated 2026-06-17 against branch `codex/real-world-testing` (HEAD 9b5154d) by a 15-agent per-epic reconciliation pass.

**Build agents: READ THIS.** For any ticket marked PARTIAL, the listed code already exists on this branch — EXTEND it, do not rebuild it. The prior failed run duplicated existing files (secretBox.ts, connections.ts, etc.) because it was on a stale base; you are now on the correct tip, so the evidence below is accurate.

## Summary: 0 done, 18 partial, 130 todo (of 147)

Nothing is fully done, so no tickets are skipped. The redesign/real-data commits (E5/E6/E7 in *redesign* numbering) overlap a handful of launch-sprint tickets but never satisfy a full Definition-of-Done.

**Partial tickets (extend existing work):** E1-T3, E1-T7, E1-T12, E2-T10, E3-T11, E6-T4, E6-T6, E7-4, E8-T6, E8-T8, E10-T1, E10-T5, E14-T1, E14-T2, E14-T3, E14-T4, E14-T6, E15-T1

---

## E1 — Accounting correctness & reconciliation engine

On branch codex/real-world-testing (HEAD 9b5154d), E1 is largely NOT done. The earlier E7 commit (c23dc4c) built the Stripe in-transit/clearing model + matcher and the descriptor-as-booster behavior, which partially overlaps E1-T3, and a period-close ("Close the books") UI exists which partially overlaps E1-T12 — but neither ticket's full DoD is met. The 12 tickets classify as: 9 TODO, 3 PARTIAL (T3, T7, T12), 0 fully done. Key unbuilt items: fixture-gating to demo entities (T1), opening-balance posting on bank connect (T2), the clearing-zeroes invariant + health query + residual drain (T4), report pagination by whole entry instead of flat .take(5000) (T5), cash-flow self-transfer/split classification (T6), the unreviewed-gap banner shared by Reports+Dashboard (T8), contactId on journal lines (T9), unifying dashboard cash to the ledger with a bank-vs-books comparison (T10), and removing the hardcoded 12-month window + raw-transaction cash-flow series (T11). The bank reconciliation surface (table, mutations, worksheet, UI) for T12 is entirely absent. No new E1-specific vitest tests were found for fixture-gating, opening balance, the clearing invariant, report pagination, cash-flow/customer double-count, or the unreviewed gap; the only related tests (stripe.test.ts, stripeSingleCounting.test.ts) cover the pre-existing E7 model.

- `E1-T1` todo — convex/stripe.ts:1851 fetchStripeProjection still passes includeFixturePayoutFallback:true UNCONDITIONALLY; both real-sync callers (convex/stripe.ts:2045-2048 saved-credential path and 2056 env-key path) route through fetchStripeProjection with no isDemo/allowFixtures gating. projectionFromStripeLists (stripe.ts:1824) still substitutes buildFixtureProjection().payouts whenever payouts list is empty. No isDemo parameter threaded; entity.isDemo (schema.ts:72) is never consulted in the projection/sync path. No fixture-gating vitest exists (convex/stripe.test.ts only tests the existing fixture+webhook behavior). DoD all unmet.
- `E1-T2` todo — convex/plaid.ts:1874-1945 upsertPlaidAccountsForItemCore creates the ledgerAccount (1915) and bankAccount with balanceMinor (1927-1939) then returns at 1944 — NO postLedgerEntryCore call, no Dr Bank/Cr 3900 opening entry, no findAccountByNumber('3900'), no 'opening:<plaidAccountId>' sourceId, no idempotency guard. The only postLedgerEntryCore in plaid.ts (694) is a Plaid-removed-transaction reversal, unrelated. coreViews.ts:180 dashboard override (from 059a71d) is still present and unreconciled. No opening-balance vitest. Equity still reads $0. DoD all unmet.
- `E1-T3` **PARTIAL** — DONE: descriptor demoted to booster — findMatchingStripePayout (convex/stripe.ts:1235,1251) accepts an exact-amount match even without a 'stripe' descriptor, and stripeSingleCounting.test.ts:166-172 proves a non-Stripe descriptor still matches on exact amount. MISSING: PAYOUT_MATCH_AMOUNT_TOLERANCE_MINOR is still 1 (stripe.ts:1197), not exact-net 0; PAYOUT_MATCH_DATE_WINDOW_DAYS is 5 symmetric CALENDAR days via isoDaysBetween (stripe.ts:1199-1205), not the −2/+5 BUSINESS-day window; no matchDepositToPayout(transactionId,payoutId) session mutation; no internalQuery listing candidate pending payouts for an Inbox Match picker; no 'two same-net pending payouts → route to manual' ambiguity guard. grep for matchDepositToPayout/businessDay/exactNet returns nothing in convex/.
- `E1-T4` todo — convex/stripe.ts:1069-1153 payout loop posts Dr1160/Cr1150 (clearingDrainMinor) and flags drift via a payout_mismatch inbox card (1125-1135), but there is NO running-clearing-balance computation, NO assertion that 1150 cannot cross negative, and NO clearing_drift card kind or invariant-blocks-commit logic. grep finds no stripeClearingHealth query, no drainResidualInTransit mutation, no clearing_drift kind anywhere in convex/. DoD (invariant test, health flips, drain reverse+repost) all unmet.
- `E1-T5` todo — convex/reportViews.ts:32 REPORT_LIMIT=5000 unchanged. Loaders (reportViews.ts:493-494) still do flat .take(REPORT_LIMIT): journalEntries via by_entity_and_date but journalLines via the non-date by_entity index — exactly the truncation that drops one side of an entry. Lines are NOT loaded by-entry. truncated flag (913-917) is still row-count based (length>=REPORT_LIMIT), not 'an in-range entry was excluded'. coreViews dashboard loaders likewise uncapped-by-entry. No large-book (>5000-line) integrity vitest. DoD all unmet.
- `E1-T6` todo — convex/reportViews.ts:626-635 still classifies each cash line off a SINGLE arbitrary related counter-line: lines.filter(...).map(...).find(...) then groupKey from related?.type. No iteration over the FULL set of non-cash counter-lines, no deterministic precedence, and no cash↔cash self-transfer net-out (a checking→savings transfer or payout deposit still shows both legs and inflates groups). No transfer/split cash-flow vitest. DoD all unmet.
- `E1-T7` **PARTIAL** — PARTIAL change present: the invoice/bill face-value addition (convex/reportViews.ts:707-732) is now guarded — invoice add only fires if !incomeCustomerTotals.has(invoice.contactId) (715) and bill add only if !expenseVendorTotals.has(bill.contactId) (728). BUT the DoD requires de-dup keyed on the DOCUMENT's entryIds (skip face-value if any of the doc's entryIds already contributed), not a coarse contactId-presence check; the current guard can both double-count (different contacts) edge cases and under-count a customer's second open invoice. No equality assertion that Σ incomeByCustomer === profitAndLoss.incomeMinor, and no vitest proving the posted+open invoice is counted exactly once.
- `E1-T8` todo — No shared unreviewed-gap helper. reportPack limits block (convex/reportViews.ts:890-918) returns reportLimit/rowCounts/truncated but no unreviewedCount/unreviewedAbsMinor. coreViews.ts:200 computes reviewedTransactions (count of NOT needs_review) but exposes no unreviewedCount/$ helper and takes no scope arg. grep for unreviewedCount/'unreviewed and excluded'/'are unreviewed' across convex/ and apps/web/src returns nothing. No banner in ReportsScreen.tsx or CoreScreens.tsx. No count vitest. DoD all unmet.
- `E1-T9` todo — convex/ledger.ts:322-327 LedgerLineInput = {accountId,debitMinor,creditMinor,currency?} — no contactId. The line insert (ledger.ts:413-422) writes only those fields, never contactId. schema.ts:221 has the optional contactId on journalLines but it is never populated. No posting caller (stripe invoice posting stripe.ts:1022-1046, invoices, bills, payroll, pipeline) threads contactId onto a journal LINE (pipeline.ts contactId is on the transaction, not the line). reportViews customer/vendor rollup still reads transaction.contactId (reportViews.ts:682), not line.contactId. No contactId-persistence vitest. DoD all unmet (fxRate correctly left unwritten per Q3).
- `E1-T10` todo — convex/coreViews.ts:180 bankBalances still sets amountMinor = bankAccount.plaidAccountId ? bankAccount.balanceMinor : ledgerBalanceMinor — the LIVE Plaid balance remains the primary cash figure whenever a Plaid account is linked, directly contradicting the report's ledger cash (reportViews.ts:598-611). No separate bankSaysMinor field, no {ledgerCashMinor,bankCashMinor,differenceMinor,unreviewedCount} comparison, no 'Bank says X / Books say Y' UI in CoreScreens.tsx. No dashboard-cash===report-cash vitest. DoD all unmet.
- `E1-T11` todo — convex/coreViews.ts:12-25 still hardcodes the literal months array 2025-07 … 2026-06. cashFlowByMonth (coreViews.ts:201-216) still maps that frozen array and sums RAW transaction amountMinor (inflow/outflow), not a ledger-derived monthly cash series, and does not net transfers. No server-clock 'today' window, no parity with the Cash Flow report. No windowing vitest. DoD all unmet.
- `E1-T12` **PARTIAL** — PARTIAL: the period-close half exists — apps/web/.../ReportsScreen.tsx has a 'Close the books' card (line 265) wired to api.ledger.setPeriodLock (229,242) and reads reportViews.reportPeriodLock (1607); postLedgerEntryCore already blocks posting into a locked range (ledger.ts:364-370). MISSING the entire reconciliation surface: no bankReconciliations table and no reconciliationId/clearedAt markers in schema.ts (only periodLocks at 227); no startReconciliation/toggleTransactionCleared/addAdjustingEntry/completeReconciliation mutations; no worksheet query; no reconcile-to-statement UI; no diff=$0.00-gate / reversible-adjusting-entry / locked-period vitest for the reconciliation flow. grep for bankReconciliations/startReconciliation/completeReconciliation returns nothing.

## E2

E2 (AI categorization engine & learning loop) is essentially NOT started on branch codex/real-world-testing (HEAD 9b5154d). 10 of 11 tickets are TODO; only E2-T10 (calibration) is PARTIAL. The three net-new files the epic mandates — convex/categorizer.ts, convex/embeddings.ts, convex/aiCredentialStore.ts — do not exist, and no E2 test files (categorizer.test.ts/embeddings.test.ts) were added. Concretely: the categorizer is still Bedrock-only and degrades-to-Inbox for any non-bedrock provider (bedrockCategorizer.ts:569-572,670-673); all callers still point at bedrockCategorizer (ai.ts:91, plaid.ts:243, CoreScreens.tsx:1653, ModuleScreens.tsx:2062); the chat + test-connection runtimes still hardcode createAmazonBedrock (aiChatRuntime.ts/aiSdkRuntime.ts); the 25-item batch cap (ai.ts:696) with no reschedule persists; the aiMemoryEmbeddings vector table is dead (no embed()/vectorSearch anywhere); candidates are still sign-locked income/expense (ai.ts:290,313-314); routeProposedCategory still returns hardcoded stage:'rule' (pipeline.ts:435) and assertCategoryAccount still throws on bad ids (pipeline.ts:265-275) with no decidedBy:'embedding'; plaidPriorAccountId is still never populated on the first pass (plaid.ts only writes PFC into rawDescription); the prompt has no business-context block (bedrockCategorizer.ts:441-490); and the Settings AI provider dropdown is still disabled with the dead 'set in Convex env' copy and no saveCredential/Test-connection/Top-N Inbox provenance. E2-T10 is partial only because the calibration math, conservative gate clamp, and rawGate/calibratedGate+ECE separation pre-exist from the earlier confidence-calibration commit (1484e72) — but the eval->fit->persist wiring is not connected (the eval merely returns a summary), aiCalibrations is workspace-keyed rather than the required per-entity, there is no refit cron/trigger, and no test proves the gate uses the calibrated probability after auto-persistence. NOTE: the backlog.json E2-T1 title ('Write & resolve BYO AI credentials') diverges from the epic markdown ('Consume the unified credential resolver'); credential STORAGE is owned by E3, and E3's resolveActiveProvider does not exist on this branch, which blocks the whole E2 chain. Key files: /Volumes/SSD/OpenBooks/convex/bedrockCategorizer.ts, /Volumes/SSD/OpenBooks/convex/ai.ts, /Volumes/SSD/OpenBooks/convex/pipeline.ts, /Volumes/SSD/OpenBooks/convex/plaid.ts, /Volumes/SSD/OpenBooks/convex/schema.ts, /Volumes/SSD/OpenBooks/convex/crons.ts, /Volumes/SSD/OpenBooks/apps/web/src/components/openbooks/settings/AiSection.tsx, /Volumes/SSD/OpenBooks/apps/web/src/components/openbooks/CoreScreens.tsx, /Volumes/SSD/OpenBooks/apps/web/src/components/openbooks/ModuleScreens.tsx.

- `E2-T1` todo — No unified E3 resolver consumed. There is no `resolveActiveProvider` anywhere in convex/ (grep returns nothing in source). The new file `convex/aiCredentialStore.ts` (backlog.json) does not exist. The categorizer still resolves provider via the legacy env-only registry: `bedrockCategorizer.ts:552` calls `bedrockRuntimeEnv(...)` and gates on `context.provider.activeProvider !== "bedrock"` (lines 569-572, 670-673). The dead per-field `aiCredentials` table (schema.ts:326) is still never written — only `aiProvider.ts:83 decryptCredentialRow(row: Doc<"aiCredentials">)` references it and has no caller. No active/degraded seam test exists. The categorizer does not read provider/model/credential through any unified `credentials` (kind:"ai") store.
- `E2-T2` todo — The new provider-agnostic module `convex/categorizer.ts` does not exist. The categorizer is still Bedrock-only: both categorize paths bail to mode:'degraded' when `context.provider.activeProvider !== "bedrock"` (bedrockCategorizer.ts:569-572 and 670-673) and build the model via env-only `bedrockRuntimeEnv` (line 552/658), never `buildModelForProvider`. All callers still point at bedrock: `ai.ts:91` (`bedrockCategorizer:categorizeAndRouteTransaction`), `plaid.ts:243` (`bedrockCategorizer:categorizePendingTransactionsForImportInternal`), `CoreScreens.tsx:1653` and `ModuleScreens.tsx:2062` (`api.bedrockCategorizer.categorizePendingTransactions`). The three-runtime migration is not done either: `aiChatRuntime.ts:3,151` and `aiSdkRuntime.ts:3,70` still hardcode `createAmazonBedrock` from env. No `categorizer.test.ts`.
- `E2-T3` todo — The 25-item cap is intact and there is no self-rescheduling drainer. `ai.ts:696` still does `Math.min(25, ...)` and `ai.ts:700` `.take(500)` with no `runAfter`/reschedule. No `drainCategorizationBacklog` or `startCategorizationBacklog` symbol exists anywhere in source (grep empty). The Plaid import trigger still fires a single pass at `plaid.ts:1297-1298` (runAfter once, no pass chain), and the CSV/import buttons (`CoreScreens.tsx:1867`, `ModuleScreens.tsx:2137`) call `categorizePendingTransactions` once. No backlog-drain test.
- `E2-T4` todo — The aiMemoryEmbeddings vector table (schema.ts:358, by_embedding 1024-dim) is still dead. No `convex/embeddings.ts` module, no `embedText` action, and no `embed(`/`embedMany`/`vectorSearch` call anywhere in convex source (grep empty). `pipeline.recordCorrectionMemory` still only upserts `aiCorrectionMemories`; no embedding row is scheduled/written on confirm/recategorize. No embeddings.test.ts.
- `E2-T5` todo — No embedding/k-NN recall stage exists. No `ctx.vectorSearch('aiMemoryEmbeddings',...)` call in source. The cascade in `pipeline.ts routeTransactionCore` still goes exact-memory (stage 'memory', line 812) -> plaid_prior (831) -> ai (850) with no 'embedding' stage inserted. The schema `transactions.decidedBy` union (schema.ts:441-450) does NOT contain `"embedding"`, and the route stage unions are `'memory'|'plaid_prior'|'ai'` (pipeline.ts:392) / `'memory'|'ai'` (pipeline.ts:509) — the additive 'embedding' widening was never applied. Depends on T4 which is also todo.
- `E2-T6` todo — Candidate set is still hard-locked by sign. `buildCategorizationContext` (ai.ts:290) computes `accountType = amountMinor >= 0 ? "income" : "expense"` and `ai.ts:313-314` filters `candidateAccounts` to `account.type === accountType` only — so inflows get ONLY income accounts and outflows ONLY expense, forcing refunds into revenue. No contra/equity/liability/transfer candidates are added; the prompt builder (bedrockCategorizer.ts:441-490) has no refund/transfer guidance. No direction-aware-candidate test.
- `E2-T7` todo — Both correctness fixes are absent. (1) Stage attribution is still corrupted: `routeProposedCategory` returns the hardcoded literal `stage: "rule"` on the posted branch (pipeline.ts:435) even though its arg `stage` is `'memory'|'plaid_prior'|'ai'`. (2) `assertCategoryAccount` (pipeline.ts:265-275) still unconditionally `throw new Error(...)` on a missing/foreign/archived id with no catch-to-needs_review boundary in the categorizer — an AI/recall-hallucinated id would throw, not abstain. No 'embedding' stage in the decidedBy union. No pipeline.test.ts cases for per-stage attribution or bogus-id abstention.
- `E2-T8` todo — `plaidPriorAccountId` is wired through the pipeline (accepted at pipeline.ts:42, auto-posts at 818-834) but is still never populated on the Plaid first pass. In `plaid.ts` the PFC is only stitched into `rawDescription` text (mapPlaidTransactionToPipeline, lines 432-440 `Plaid prior: primary/detailed`); the first-pass `routeArgs` (plaid.ts:817-849) never sets `routeArgs.plaidPriorAccountId`. There is no personal_finance_category->ledgerAccount mapping table/helper. No plaid.test.ts case for a mapped prior.
- `E2-T9` todo — The primary work is absent. `buildCategorizationPrompt` (bedrockCategorizer.ts:441-490) emits only Entity name, date, amount, merchant, raw description, and candidate accounts — there is NO 'Business context' section, no approved-revenue-stream settings field read, no recent-vendor hint. (The contactId portion of the ticket is incidentally already satisfied by pre-existing plumbing: bedrockCategorizer.ts:494 forwards `contactId` and pipeline.ts:693 stores it on the transaction — but that pre-dates this ticket; the merchant->contact resolution this ticket adds is absent.) DoD item #1 fails, so not done.
- `E2-T10` **PARTIAL** — The calibration MATH and gate clamp pre-exist (commit 1484e72): `calibration.ts` fitCalibration/applyCalibration, `fitWorkspaceCalibration` mutation (ai.ts:983) persists a row, and `summarizeHoldoutCalibration` (ai.ts:1176) DOES report rawGate vs calibratedGate precision + ECE before/after separately, returned by `runHoldoutCategorizationEval` (ai.ts:1150). MISSING (not done): (a) no production caller chains the eval to fit-and-persist — `runHoldoutCategorizationEval` only RETURNS a calibration summary, it never calls `fitWorkspaceCalibration`, so no aiCalibrations row is auto-written; (b) calibration is WORKSPACE-keyed not per-entity — `aiCalibrations` schema has only `workspaceId`/`by_workspace` (schema.ts:407-419) and `getEntityCalibration` reads `by_workspace` (pipeline.ts:233-241), contradicting the ticket's per-entity + workspace-fallback requirement; (c) no refit cron (crons.ts has only Plaid sync + payroll); (d) no settings/onboarding trigger; (e) no test proving the gate compares the calibrated probability after auto-persistence. So the dormant loop is still dormant in production.
- `E2-T11` todo — Settings AI panel is unchanged: `AiSection.tsx:105` still renders `<Select ... disabled>`, line 121-123 still shows the dead 'set in Convex env · never shown' key copy, line 68 hardcodes `provider: "bedrock"`, and the dropdown lists only the legacy 5 providers (not 14 from listProviderCatalog). No `saveCredential` call, no API-key input, no per-provider Test connection. Inbox provenance/Top-N: no 'Matched your rule' / 'Same as your last N AWS charges' / 'AI 0.82 - review' provenance strings and no Top-N accept-to-confirm suggestion UI in CoreScreens/ModuleScreens (only a pre-existing AiReasoningChip showing decidedBy for ai rows, e.g. CoreScreens.tsx:2190-2196). No drainer progress surface. Depends on T1/T7, both todo.

## E3

E3 (Integrations & BYO-keys: AI/Plaid/Stripe/Plunk unified credential layer) is essentially unstarted on codex/real-world-testing (HEAD 9b5154d). The orphaned state described in the epic's "Current state" still holds verbatim: the unified `credentials` table does not exist, the dead per-field `aiCredentials` table (schema.ts:326) is unchanged and still referenced only by schema.ts + aiProvider.ts (zero write callers), the AI provider Select is still a disabled 5-option dropdown with "set in Convex env" (AiSection.tsx:105/123), setConfig still hardcodes provider:"bedrock" (ai.ts:68/853) with a 5-literal validator (ai.ts:31-37), all three runtimes (testProviderConnection aiSdkRuntime.ts:70, bedrockRuntimeEnv bedrockCategorizer.ts:150-157 gated on AI_PROVIDER==="bedrock", agent.ts:88) still hardwire createAmazonBedrock, Plaid writes every account to one entityId (upsertPlaidAccountsForItemCore plaid.ts:1878/1916/1928), Stripe sets webhookStatus "listening" the instant a secret is saved (connections.ts:791/845, no pending_verification in the schema union at 723/751), Plunk is env-only everywhere (auth.ts:53, requestAccess.ts:91, team.ts:88, plunk.ts:33), there is no connections.health query, ConnectionsSection.tsx shows only Banks+Stripe cards (no AI/Plunk), and none of the new test files (credentials/aiResolve/plunk/secretSafety.test.ts) exist. The recent E5/E6/E7 commits did not touch any E3 surface. The ONLY partial movement is T11: AGENTS.md's sandbox/test-only rule has been replaced with "Live connectors must work locally" (AGENTS.md:82), but the actual OPENBOOKS_REAL_TEST_LIVE_CONNECTORS gate still hard-blocks live keys in code (connections.ts:135-136, 223-224; plaid.ts:333-335), so T11 is partial, everything else is todo. NOTE: backlog.json carries the OLD per-field-aiCredentials framing and lists only T1-T10; I classified against the epic markdown's authoritative DoD (T1-T11, unified-table framing).

- `E3-T1` todo — Unified `credentials` table NOT built. No convex/credentials.ts exists (ls confirms). grep for saveCredential/credentialStatus/getActiveCredential/deleteCredential across convex/ returns nothing (only the unrelated connections.ts getActiveCredentialForEntity:897, which operates on connectionCredentials). The dead per-field aiCredentials table is unchanged at schema.ts:326 (still apiKeyCiphertext/accessKeyIdCiphertext/secretAccessKeyCiphertext/baseUrl/region/lastFour) and is still referenced ONLY by schema.ts + aiProvider.ts (grep -rln aiCredentials), i.e. zero insert/patch callers. No KDF/HKDF fix, no credentials.test.ts. None of the 6 DoD items met.
- `E3-T2` todo — No convex/aiResolve.ts and no resolveActiveAiModel symbol anywhere (grep). The setConfig validator aiProviderValidator (ai.ts:31-37) still lists exactly 5 providers (bedrock/anthropic/openai/google/ollama), NOT the 14 from aiCatalog — not widened. providerStatus (ai.ts:608) still reads only bedrockEnvironmentStatus() (ai.ts:618) and defaults configuredProvider to 'bedrock' (ai.ts:628). aiProviderRegistry.ts still forces active = ready && id === 'bedrock' (line ~144). setConfig still defaults provider to 'bedrock' (ai.ts:853). No validate-on-save ping. No aiResolve.test.ts.
- `E3-T3` todo — All three runtimes still hardwire Bedrock-from-env. aiSdkRuntime.testProviderConnection (line 42) still constructs createAmazonBedrock (line 70) — imports it at line 3. bedrockCategorizer.bedrockRuntimeEnv (line 150) still requires process.env.AI_PROVIDER === 'bedrock' (line 157) and is the categorize path (callers at 552/658); no resolveActiveAiModel/buildModelForProvider/generateObject import. agent.ts still createAmazonBedrock at line 88 (import line 1). No provider-agnostic categorize entrypoint, no integration test proving non-bedrock categorize/chat.
- `E3-T4` todo — AiSection.tsx still has the dead UI: the provider <Select> at line 105 is `disabled` and lists only 5 hardcoded SelectItems (bedrock/anthropic/openai/google/ollama, lines 111-115); the key box at line 123 still reads 'set in Convex env · never shown'. setConfig is still called with hardcoded provider:'bedrock' (line 68). No api.aiCatalog.list query, no saveCredential wiring, no model Select / custom-model input, no credentialKind-conditional fields. None of the 5 DoD items met.
- `E3-T5` todo — upsertPlaidAccountsForItemCore (plaid.ts:1874) takes a single args.entityId and writes every included account's ledgerAccount (line 1916) and bankAccount (line 1928) under that one entityId — no per-account assignment. No assignPlaidAccountsToBusinesses action exists (grep). AddBankSheet.tsx has a single pre-link top-level BusinessSelect (line 167, 'Choose the business this bank belongs to, then connect through Plaid Link') — no per-account picker after Link success. No 2-entity-split convex test. None of the 5 DoD items met.
- `E3-T6` todo — upsertConnectionCredential still sets webhookStatus 'listening' the instant webhookConfigured is truthy at connections.ts:791 and :845 — the exact false-green. The schema webhookStatus unions (schema.ts:723, 751) are still {not_configured, listening, failing, unknown} with NO pending_verification state. No public verifyStripeWebhook action (only the pre-existing internalAction verifyStripeWebhookCredential:986). StripeConnectSheet does not make the webhook secret required-and-verified. (Note: event.id dedupe at stripeWebhook.ts:105/129 pre-dates this from E7 and does not satisfy this ticket.) None of the 5 DoD items met.
- `E3-T7` todo — Plunk is still env-only everywhere: no convex/plunk.ts, no savePlunkCredential/deletePlunkCredential/plunkStatus (grep finds none). sendPlunkEmail uses requiredEnv('PLUNK_SECRET_KEY') (packages/email/src/plunk.ts:33) with no BYO resolution. auth.ts:53, requestAccess.ts:91, team.ts:88 (emailDeliveryConfigured), profile.ts:99 all read process.env.PLUNK_SECRET_KEY directly. No kind:'plunk' credential, no PlunkSection/Email card in the settings UI, no plunk.test.ts. None of the 5 DoD items met.
- `E3-T8` todo — No connections.health (or connectionsHealth) query exists (grep 'export const health' returns nothing). No unified per-provider {status, detail, lastValidatedAt, action} normalizer covering all four kinds — only pre-existing per-provider probes (plaid.testWorkspacePlaidApp:1651, the internal verifyStripeWebhookCredential:986). Depends on T1/T3/T6/T7 which are all todo. AI and Plunk have no validate/re-link affordance in the UI. None of the 4 DoD items met.
- `E3-T9` todo — ConnectionsSection.tsx renders only TWO ProviderCards — 'Banks' (Plaid, line 288) and 'Stripe' (line 311) — inside a md:grid-cols-2 grid (line 287). There is NO AI card and NO Plunk/Email card, so the surface is not the four-provider 'all your keys in one place' redesign the ticket requires; status is not sourced from a (nonexistent) health query. No 360px responsive redesign evidence/screenshot, no empty-first-run-to-onboarding card. None of the 5 DoD items met.
- `E3-T10` todo — No convex/secretSafety.test.ts (ls confirms absent) and no shared redaction helper extracted from aiSdkRuntime.redactEnvValues:24. No epic-wide enumeration test asserting all integration status/list queries omit ciphertext, no wrong-key-error redaction test, no grep gate, no secret-flow doc note. (Pre-existing connections.test.ts:162-163 and plaid.test.ts:699-700 assert encrypt-at-rest for the existing Plaid/Stripe paths only — they do not cover the new AI/Plunk/credentialStatus surfaces this ticket audits, since those surfaces don't exist yet.) Depends on T1/T6/T7/T8, all todo.
- `E3-T11` **PARTIAL** — Doc half DONE: AGENTS.md no longer carries the 'only Plaid sandbox / Stripe test-mode keys' rule — AGENTS.md:82 now reads 'Live connectors must work locally. Live Plaid (production/development) and live Stripe keys are permitted...'. Code half NOT done: the OPENBOOKS_REAL_TEST_LIVE_CONNECTORS gate still HARD-BLOCKS live keys (it is defaulted closed, not open/removed) — requireRealDataFlag throws at connections.ts:135-136, Stripe live throws at connections.ts:223-224, Plaid dev/prod is blocked at plaid.ts:333-335 (and gated in stripeWebhook.ts:135). Encryption-at-rest (plaid.ts:341) and the live-key HTTPS-redirect requirement (connections.ts:248) are correctly still present, but DoD item 2 (gate no longer blocks across the four files) is unmet and there is no test asserting a live-shaped key is accepted-and-encrypted.

## E4

All 10 E4 (launch-sprint "Guided onboarding & done-for-you books") tickets are TODO on branch codex/real-world-testing (HEAD 9b5154d). The branch's recent commits implement OTHER epics — the redesign sprint (E1-E5: config-driven workbench, Insights, Income/Expenses/Contacts sub-tabs), E6 confidence calibration, E7 Stripe/Plaid double-count fix, and real-data Plaid/Stripe connectors — none of which touch guided onboarding. The launch-sprint E4 surface is still in its documented pre-epic state: onboarding is the original in-memory single-business stepper (OnboardingScreen.tsx, 355 lines) with no-op 'Skip for now' IntegrationStep placeholders; bootstrapWorkspace creates exactly one business; onboardingChecklists has only the 5 original booleans (no state-machine fields); setConfig still ignores AI keys and forces bedrock; the AiSection provider Select still carries disabled; no unified credentials table, no setOpeningBalances (equity stays $0), no invited-teammate branch, no generateOnboardingProposals/incomeStream kind, no proposal-review screen, no scoped resetWorkspaceData, and no /demo route. Note: git commits labeled 'E4' (e.g. 0280a36) belong to the redesign sprint's epic numbering, not this launch-sprint E4. Caveat on dependencies: E4-T3/T4 are blocked on E3's unified credentials table+resolver, which also does not exist on this branch — so even the prerequisite infra for the highest-leverage ticket (BYO AI key) is absent."


- `E4-T1` todo — Onboarding state machine not built. convex/schema.ts:56-65 onboardingChecklists still has only the 5 original booleans (bankConnected/aiConnected/stripeConnected/firstInboxZero/firstReportViewed) + createdAt/updatedAt — NONE of the required additive fields (currentStep, completedSteps, skippedSteps, plunkConnected, teamInvited, openingBalancesSet, historyReviewed, proposalsReviewed, phase). grep in convex/onboarding.ts finds no getProgress/markStep/setPhase/STEP_ORDER const; the only mutation is the original markChecklistStep (onboarding.ts:351) toggling the 5 booleans. convex/onboarding.test.ts is the original file (single bootstrap + idempotency) with no markStep/getProgress/resume/skip cases.
- `E4-T2` todo — Multi-business onboarding not built. convex/onboarding.ts:242 bootstrapWorkspace still takes a single business (businessName/businessType/currency + skippedAi/skippedBank/skippedStripe) and calls createFirstBusinessForWorkspace once — no businesses[] array, no createBusinessForWorkspace rename, no addBusinessDuringOnboarding mutation. OnboardingScreen.tsx step 0 collects one business (single onboarding-business-name field, no add/remove rows). convex/onboarding.test.ts and tests/e2e/onboarding.spec.ts both still assert exactly 1 entity/1 business; the e2e never adds a second business or checks active-business-menu for two.
- `E4-T3` todo — BYO AI key (the activation blocker) is not wired. convex/ai.ts:838 setConfig args are still only {workspaceId, provider, autonomy} — no apiKey/chatModel/categorizeModel, and it still defaults provider to 'bedrock' (line 853). No unified E3 'credentials' table exists in schema.ts (grep 'credentials:' empty; the dead aiCredentials table at schema.ts:326 is untouched). aiProviderRegistry.ts still lists only 5 providers (AI_PROVIDER_IDS line 1), all non-bedrock have v1Enabled:false, and 'active' is hard-gated to id==='bedrock'&&v1Enabled at line 141. testProviderConnection (aiSdkRuntime.ts:42) only exercises Bedrock (createAmazonBedrock), never a configured non-bedrock key. AiSection.tsx:105 provider Select STILL has the disabled attr, lists only 5 providers, and the API key field (line 121) is read-only text with no Input/Save.
- `E4-T4` todo — Inline real-work steps not built. OnboardingScreen.tsx still renders IntegrationStep placeholders at lines 210/225/240 whose only action is the no-op skip (onboarding-ai-skip/onboarding-bank-skip/onboarding-stripe-skip in the e2e at spec lines 34-36); no AI/Plunk/Plaid/Stripe real-config step components, no webhookConfig copyable-URL panels, no guide links, no Plaid account->entity mapping UI. No Plunk credential store (grep 'plunk' only hits a schema comment). No unified credentials table for kind:'ai'/'plunk'. markStep(step,'skipped') does not exist (see E4-T1).
- `E4-T5` todo — Opening-balances step not built. No setOpeningBalances mutation exists (grep across convex/*.ts is empty). The Opening Balance Equity account 3900 exists only as a chart-template row in ledger.ts:53 — nothing books an opening journal entry into it, so equity is still $0 as the audit found. No opening-balances UI in OnboardingScreen.tsx and no first-of-month/USD opening-entry unit test in onboarding.test.ts.
- `E4-T6` todo — Invite-team step and self-host-vs-invited branching not built. AppShell.tsx:437 unconditionally renders OnboardingScreen for viewer.status==='needs_onboarding' OR zero businesses — no invited-teammate branch ('you've joined {workspace}' / route to role landing). grep for invited/fromInvite/joinedViaInvite in session.ts and AppShell.tsx is empty, so viewer exposes no owner-first-run-vs-invited-join flag. OnboardingScreen has no invite-team step calling team.invite/surfacing inviteUrl.
- `E4-T7` todo — AI bulk-setup proposal engine not built. No generateOnboardingProposals action (grep across convex/*.ts empty). proposals.ts PROPOSAL_KIND (line 31) is still the original 5 kinds (categorize/rule/invoiceDraft/bill/journalEntry) — no incomeStream kind/table. No user-chosen history window control, no clarifying-questions persistence, no income-stream taxonomy field, and no unit test seeding history to assert proposals are generated.
- `E4-T8` todo — Proposal review/approve screen not built. There is no onboarding review step in OnboardingScreen.tsx (it has no review/proposal UI) and no onboarding proposals to review (E4-T7 absent). The generic proposals.confirmProposal/dismissProposal and ai.createConfirmedRule exist as pre-existing infra but nothing wires an onboarding income-stream/category/rule review gate to them; proposalsReviewed/phase fields don't exist (see E4-T1).
- `E4-T9` todo — Finish-and-land-populated flow not built. OnboardingScreen.tsx finish() still just calls bootstrapWorkspace then router.push('/dashboard') (lines 73-82) — no bulk categorize/post pass enqueued on finish, no 'your books are being set up' transitional state, no first-run summary surface (businesses/accounts/feeds/posted-vs-inbox/proposals-approved). The e2e (onboarding.spec.ts) asserts only the empty-shell checklist card, not populated dashboard counts > 0.
- `E4-T10` todo — Owner reset/re-onboard and public demo not built. No resetWorkspaceData mutation/action (grep across convex/*.ts empty). DataSection.tsx:84 still reads 'Permanent deletion is intentionally not exposed here' with only the destructive realTestReset panel — no scoped, typed-confirmation 'delete all data & re-run onboarding' control. No /demo route exists (apps/web/src/app has no demo directory; grep for /demo in app is empty), no server-slug demo guard surfaced, no clone-CTA, and no scoped-reset isolation unit test.

## E5 — Multi-entity, workspace↔business layer & Portfolio/consolidation

Epic E5 (Portfolio / multi-entity consolidation) is entirely unstarted on branch codex/real-world-testing (HEAD 9b5154d). All 10 tickets are TODO. None of the new files the epic specifies exist: convex/entityScope.ts, portfolioMoney.ts, portfolioViews.ts, entityMetrics.ts, intercompany.ts, and convex/portfolio.authz.test.ts are all absent. The hardcoded 'acme-studio-llc' default-entity fallback (T1's central target) is still live in coreViews.ts:34, reportViews.ts:46, incomeViews.ts:20, expensesViews.ts:25, moduleViews.ts:24, plus aiThreads.ts:58 and performance.ts:17. The schema has none of the additive fields the epic needs — no entities.isDefault, no workspaces.defaultEntityId, no intercompanyPairId, no intercompanyLinks table — and the dead journalLines.fxRate field (T4 should remove) is still present (schema.ts:220). The frontend has no scope concept: active-entity.tsx exposes only selectEntity (no scope/selectScope), AppShell's BusinessSwitcher is single-select with no 'All businesses' item, and CoreScreens still uses entityArg() with no scopeArg. createEntity is not USD-locked (accepts any 3-letter ISO code). The merged work mentioned in the prompt (UI redesign, Plaid/Stripe connectors, double-count fix, AI calibration, payroll) did NOT touch any E5 surface. The one near-overlap is E5-T9: the Connections UI already shows each connection's business and lets you pick a business at CREATION, but there is no re-map mutation or post-creation edit, so even T9 has zero DoD items satisfied. Note: the prompt's claim that 'many E5 tickets may already be implemented by prior work' does not hold — E5 is the multi-entity/portfolio epic and is genuinely greenfield here.

- `E5-T1` todo — No deterministic default-business work exists. convex/entityScope.ts is absent (ls confirms). The hardcoded 'acme-studio-llc' slug fallback is still live in every targeted resolver: convex/coreViews.ts:34 (getActiveEntity), convex/reportViews.ts:46, convex/incomeViews.ts:20, convex/expensesViews.ts:25, convex/moduleViews.ts:24, plus convex/aiThreads.ts:58 and convex/performance.ts:17. schema.ts has NO entities.isDefault (entities table schema.ts:66-88) and NO workspaces.defaultEntityId (workspaces table schema.ts:28-33). No setDefaultBusiness mutation exists (grep in convex/ returns none) and no 'entity.default.set' audit event. AppShell.tsx:154-156 still uses the workspaceLabel/.includes() name-match for default selection. No entityScope unit test exists.
- `E5-T2` todo — No scope contract exists. apps/web/src/lib/openbooks/active-entity.tsx (read in full) exposes only {workspaceName, role, activeEntity, entities, selectEntity} — no scope, no selectScope, no isPortfolio. AppShell.tsx has only activeEntityId state + ACTIVE_ENTITY_STORAGE_KEY ('ob:active-entity-id'); no portfolioScope state and no ACTIVE_SCOPE_STORAGE_KEY (grep). CoreScreens.tsx:151 still defines entityArg(entityId) returning {} or {entityId} — there is no scopeArg helper. convex/entityScope.ts (which would export scopeValidator) does not exist. No 'all' literal in any scope union anywhere.
- `E5-T3` todo — BusinessSwitcher (AppShell.tsx:999-1042) is unchanged single-select: it maps entities to DropdownMenuItem with onSelect(entityId) and has NO leading 'All businesses' item, no separator, no selectScope('all') wiring, no data-scope attribute. There is no 'Manage businesses / Set default' footer. The business-type filter removal is not applicable yet since no scope switcher exists to replace it. data-testid='active-business-switcher' is preserved but that predates E5.
- `E5-T4` todo — convex/portfolioMoney.ts does not exist (ls confirms), so there is no sumUsdMinor helper. createEntity (convex/entities.ts:139-159) accepts ANY valid 3-letter ISO code (validates /^[A-Z]{3}$/ at entities.ts:147-149) — it does NOT reject or coerce non-USD, so the USD lock is absent. The dead journalLines.fxRate field is still present in convex/schema.ts:220 (v.optional(v.number())). No non-USD-create-rejected test and no sumUsdMinor test exist.
- `E5-T5` todo — convex/intercompany.ts does not exist (ls confirms). No intercompanyPairId field and no intercompanyLinks table in convex/schema.ts (grep returns nothing for either). No 1300/2300 reciprocal accounts in CoA seed. No confirmIntercompany/rejectIntercompany mutations and no listIntercompanySuggestions query (grep). The only 'intercompany' hits in the repo are a plain category-label string in convex/calibration.ts:264/271, unrelated to detection. No detector unit test.
- `E5-T6` todo — convex/portfolioViews.ts and convex/entityMetrics.ts both do not exist (ls confirms). There is no portfolioDashboard query (grep). coreViews.dashboard has not been refactored to extract a shared per-entity metric helper. Depends on T2/T4/T5 which are all todo. No portfolio rollup unit test exists.
- `E5-T7` todo — reportViews.reportPack has not been extended for scope=all: it still resolves a single entity via the acme-studio-llc fallback (reportViews.ts:46) and there is no scopeValidator import (convex/entityScope.ts absent). No consolidation/elimination logic, no consolidatedFrom/eliminatedMinor fields, no intercompanyPairId-keyed read-time filter (intercompanyPairId does not exist in schema). No consolidation or single-entity-unchanged regression test.
- `E5-T8` todo — No Portfolio dashboard UI. CoreScreens.tsx dashboard still calls api.coreViews.dashboard via entityArg(activeEntity.id) (CoreScreens.tsx:245-248) with no branch on scope==='all' and no api.portfolioViews.portfolioDashboard call (that query does not exist). No by-business tile grid, no consolidation banner in ReportsScreen. The cash-source reconciliation is not done — coreViews dashboard still derives cash from the live Plaid balance path; entityMetrics.ts (the place T8 would fix it) does not exist. Depends on T6/T7 which are todo.
- `E5-T9` todo — No re-map mutations exist: grep for reassignBankAccountEntity/reassignStripeAccountEntity in convex/ returns none, and convex/connections.ts (read in full) contains save/oauth/disconnect/credential logic but no entity-reassignment mutation. bankAccounts.entityId / stripeAccounts.entityId remain fixed at creation. No future-syncs-only immutability guardrail or test. Partial overlap only (does NOT satisfy any DoD item): connections.list already returns each connection's entityName (connections.ts:329) and the Add/Stripe sheets let a user pick a business at CREATION time via defaultEntityId (ConnectionsSection.tsx:143/535/541, AddBankSheet.tsx:59, StripeConnectSheet.tsx:45) — but there is no UI to CHANGE the business after creation and no backing mutation, so every DoD item is unmet.
- `E5-T10` todo — convex/portfolio.authz.test.ts does not exist (ls confirms) and there is no assertScopeAuthorized helper (convex/entityScope.ts absent; grep returns none). There is no scope=all read path to harden — portfolioViews and the consolidated reportPack path do not exist (T6/T7 todo). No authz contract doc comment block for multi-entity scope. Per-entity reads in a portfolio context cannot be audited because no such reads exist yet.

## E6 — Reports — correctness-aware UI, redesign, responsiveness, export parity & drill-down

E6 (Reports correctness-aware UI redesign) is essentially UNSTARTED on codex/real-world-testing (HEAD 9b5154d). The three core files are byte-for-line at the epic's documented 'current state' baseline (ReportsScreen.tsx 1788 lines, reportViews.ts 974, reports-export.ts 511) and the last commit touching them is fa4ecb3 'land prior uncommitted workbench foundation as building baseline' — i.e. before any E6 work. Verdict per ticket: 8 todo (T1, T2, T3, T5, T7, T8, T9, T10) and 2 partial (T4, T6). The two 'partial' classifications rest only on PRE-EXISTING scaffolding the epic itself acknowledges, not on E6 work: T4 has the original StatementTable MoneyButton drill (but cash-flow/aging/income/payroll/GL drills and all additive backend drillDown fields are missing); T6 has the original never-future clamp in report-periods.ts (but no prior/delta columns, no backend prior-pass, no CSV columns, and not even the mandated no-future unit test). None of the named E6 symbols exist in source: BasisBadge, cashBasisExcluded, reportPackForScope, useActiveScope, intercompanyPairId, 'Intercompany eliminated', unreviewed, TruncationBanner, per-report skeletons/empty/error states, the reports-export-parity test, or a Reports InsightsBand. The CSV parity gaps (cash-flow group-totals-only, balance-sheet flat-rows-not-sections, income-by-customer no %-share) are all still present. The Stripe/Plaid double-count fix (c23dc4c) and AI-calibration (1484e72) work cited in the brief touched the pipeline/ledger, not the Reports UI — they do not advance any E6 ticket. Net: this epic needs to be built from scratch except for the small amount of legacy drill/clamp scaffolding noted."

- `E6-T1` todo — CashFlow() at ReportsScreen.tsx:895-956 is unchanged from the 'current state' baseline: it takes only `{ pack }` (no isMobile prop), ActiveReport dispatches `<CashFlow pack={pack} />` at :1501 with no isMobile, uses unconditional `grid gap-4 xl:grid-cols-[1.4fr_1fr]` (:905), fixed `h-36` bridge with `text-[10px]` per-step labels (:938-948), and renders cash-flow rows as a single justify-between truncated line `{row.date.slice(5)} · {row.memo}` (:917-921) with no isMobile stacked-card branch. File is still 1788 lines (matches baseline). The only scrollWidth e2e assertion is reports-payroll.spec.ts:119, which targets the payroll/Monthly-Review surface and predates E6 — there is no cash-flow no-overflow test. None of the four DoD items met.
- `E6-T2` todo — ViewerToolbar at ReportsScreen.tsx:384-549 is the un-redesigned `flex flex-wrap items-end gap-3` single row (:422) with fixed-width selects `w-40`/`w-36`/`w-32` (:426,475,491) that never go full-width on mobile and no separate action-button row — Explain/Export sit inline (:539-546). ReportsHome (:335-361) and ReportHeader (:839-849) are unchanged. Subtitles still print raw lowercased `pack.controls.basis` (e.g. ProfitAndLoss :816, 'cash basis') instead of a basis badge. No grep-clean/screenshot evidence pack. PayrollSummary still renders FX/byCurrency literals contrary to the redesign intent. None of the DoD items met.
- `E6-T3` todo — reports-export.ts cash-flow CSV (:360-374) still emits only `pack.cashFlow.groups.map(group => [group.label, ...group.totalMinor])` — group TOTALS only, NO per-row line items. Balance-sheet CSV (:343-359) serializes the flat `pack.balanceSheet.rows` via `statementCsv('Accounts', pack.balanceSheet.rows, pack)` (:358), NOT the `sections` the screen renders. Income-by-customer CSV (:393-403) emits name+amount only, no % share column. No parity test exists: `apps/web/src/lib/openbooks/__tests__/` directory is absent and no file matching *parity* exists anywhere. All four DoD items unmet.
- `E6-T4` **PARTIAL** — Only StatementTable numbers drill (P&L/balance-sheet/expenses via MoneyButton, :692-758). CashFlow rows render raw `<Amount amountMinor={row.amountMinor} signed />` (:920), not MoneyButton — though backend cashFlow.groups[].rows is DrillLine[] (data exists, UI unwired). AgingReport cells use raw `<Amount>` (:1008) with no click. IncomeByCustomer amounts use raw `<Amount>` (:1078). PayrollSummary period totals use raw `<Amount>` (:1171). GL/TB/Journal numbers are static. Backend ReportPack type (reports-export.ts:113-147) has NO additive drillDown on arAging/apAging rows, incomeByCustomer.rows, or payrollSummary.rows. No e2e drill-parity assertion (no `drill-total == clicked` test for cash-flow/aging/income). Partial only because the pre-existing StatementTable drill (one of the report families) already works; the rest of the ticket is unimplemented.
- `E6-T5` todo — No `BasisBadge` component exists (grep of apps/web/src + convex returns nothing). Subtitles still print raw lowercased basis: ProfitAndLoss `${pack.controls.basis} basis` at ReportsScreen.tsx:816. No `cashBasisExcluded` field on the ReportPack type (reports-export.ts:63-185) and no such computation in reportViews.ts (grep empty). No exclusion note rendered. CSV basis header at reports-export.ts:251 exists but predates this ticket. All DoD items unmet.
- `E6-T6` **PARTIAL** — ComparePanel (ReportsScreen.tsx:829-837) is still only the passive text line 'Comparing against {range}.' — no prior-period value column or signed delta in StatementTable (:647-765 has no prior/delta columns). No `priorTotalMinor`/`priorMinor`/`delta` fields on StatementRow or in reportViews.ts (grep empty; InsightsBand.tsx deltaMinor is dashboard-only). No prior/delta columns in statementCsv (reports-export.ts:244-271). The ONLY satisfied sub-item is the pre-existing no-future guard (custom inputs have `max={today}` at :445/456 and clampRange at report-periods.ts:28-31), which the epic itself notes was already correct — but the mandated report-periods unit test asserting no preset yields a future endDate across multiple 'today' inputs does NOT exist (no report-periods.test.ts file; reportViews.test.ts has only 2 tests). Core deliverables (prior column, delta, backend second-pass, CSV columns, new tests) all absent.
- `E6-T7` todo — No consolidated scope path. reportViews.ts getEntity (:34-57) resolves exactly ONE entity (demo-slug 'acme-studio-llc' fallback then first). No `reportPackForScope` query (grep empty). active-entity.tsx exposes only single-entity `selectEntity`/`activeEntity` — no `'all'` scope, no `useActiveScope` (grep empty). ReportsScreen.tsx queryArgs (:1588-1599) calls `api.reportViews.reportPack` with a single entityId, never a scope. No `intercompanyPairId` read-time elimination and no 'Intercompany eliminated' line anywhere (grep empty). All DoD items unmet.
- `E6-T8` todo — Loading is still the bare `<section>Loading {meta.name}…</section>` text line at ReportsScreen.tsx:1755-1756 — no per-report shaped skeletons. Empty states are still the generic StatementTable fallback (:657-658) / GL EmptyState (:1193-1194) / cash-flow 'No cash movements.' (:915) — no report-specific tailored copy map. No error boundary around ActiveReport: reportViews.ts:457-458 throws a raw `Error('Report start date must be before the end date.')` with no friendly inline card, no 'Reset range' action, and no client-side range guard before query. All DoD items unmet.
- `E6-T9` todo — The `limits.truncated` flag is computed in reportViews.ts:913 (and typed on ReportPack.limits at reports-export.ts:181-184) but NEVER surfaced in any UI file — grep for `pack.limits`/`reportLimit` in apps/web/src/**/*.tsx returns nothing. No `TruncationBanner` or `UnreviewedGap` component (grep empty). No `unreviewed:{count,amountMinor}` field on the pack and no banner copy ('Showing the first 5,000 rows' / 'aren't categorized yet'); the only 'Showing the first' string is an unrelated TransactionsDrillDrawer:152. No /inbox link banner on report or home. All DoD items unmet.
- `E6-T10` todo — No insight banner on the Reports surface: `InsightsBand`/InsightsStrip is not imported in ReportsScreen.tsx (grep empty) and homePack (:1608-1619) is consumed only for the Close-the-books readiness checks, not an insight strip. The e2e specs (tests/e2e/reports.spec.ts has a single test at :39; reports-export-h2.spec.ts; reports-payroll.spec.ts) gained no cash-flow-mobile-no-overflow, drill-parity, export-parity, or basis-badge assertions (grep for drill-total/Cash basis/Intercompany/insight in these specs returns nothing). No docs/finishing/evidence/ screenshot pack for this redesign. This capstone ticket depends on T1–T9, all of which are todo/partial.

## E7 — Transactions register & Mercury-grade workbench: density, responsiveness, provenance, and edit clarity (launch-sprint)

Launch-sprint epic E7 (Transactions register Mercury-grade workbench) is essentially NOT STARTED on branch codex/real-world-testing (HEAD 9b5154d). All 11 tickets are todo except E7-4, which is partial only because one of its sub-items (unposted rows reading 'Not posted') is incidentally satisfied by pre-existing rowAttention/AttentionState code rather than by any E7 work. Every net-new artifact the epic requires is absent: no convex/lib/provenance.ts + describeProvenance + provenance row field (E7-1), no ProvenanceChip component (E7-2), no compact-merchant expand toggle — rawDescription still a permanent second line at CoreScreens.tsx:2188 (E7-3), no provenance-aware status/filter de-dup (E7-4), no mobileMeta/mobileHidden ColumnDef or compact mobile card or 390px /transactions no-scroll e2e (E7-5), bulkRecategorize still hardcodes otherIncomeCategoryId and the register has no J/K/Enter/E keyboard model — that handler lives only in the Inbox (E7-6), no grouped 'Correct this entry' section and no split amount-sum validation (E7-7), no InsightBanner component or mounted banner (E7-8), the Date/Amount filter duplication is unresolved (E7-9), and the '2026-06-30' hardcoded demo dates plus InsightsScreen TODAY_ISO remain with no shared today/asOf helper (E7-10). IMPORTANT distinction: commit c23dc4c 'E7 — close the Plaid/Stripe double-count' is the REDESIGN-SPRINT E7 (a separate epic numbering), not this launch-sprint Transactions-workbench E7; the redesign work that landed (E0–E5, AI calibration E6, double-count E7) did not implement any of these launch-sprint E7 tickets. The current tree matches exactly the 'CONFIRMED GAPS' state the epic document itself described. Key files inspected: convex/coreViews.ts (613,829-913), convex/schema.ts (decidedBy enum), apps/web/src/components/openbooks/CoreScreens.tsx (1473-1479,1627-1638,1830,1937-1955,2168-2285,2400-2422,2634,2667-2768), apps/web/src/components/openbooks/workbench/OpenBooksDataTable.tsx (35-66,500-571), apps/web/src/components/openbooks/workbench/AttentionState.tsx, apps/web/src/components/openbooks/workbench/WorkbenchSurface.tsx, apps/web/src/components/openbooks/workbench/index.ts, apps/web/src/components/openbooks/InsightsScreen.tsx:32, tests/e2e/core-screens.spec.ts (63-66,160-180).

- `E7-1` todo — No provenance work exists. `convex/lib/provenance.ts` does not exist (ls confirms absent); `grep -rni provenance convex/` returns nothing. The transactions query row map (convex/coreViews.ts:844-912) still emits only raw `decidedBy`/`confidence`/`reasoning` (lines 877-879) and NO `provenance: {kind,label,confidence,count}` object. No `describeProvenance` helper and no unit test for it. The server `insights` aggregate (coreViews.ts:829-843) is unrelated to per-row provenance.
- `E7-2` todo — `apps/web/src/components/openbooks/workbench/ProvenanceChip.tsx` does not exist (ls confirms absent); `grep -rn ProvenanceChip apps/web/src` returns nothing, and it is not exported from workbench/index.ts. The register still uses the AI-only AiInsightBadge gated on `row.decidedBy === 'ai'` (CoreScreens.tsx:2190-2199, and drawer 2634). No multi-kind chip, no render test.
- `E7-3` todo — Merchant column (CoreScreens.tsx:2177-2202) still renders `row.rawDescription` as a permanent muted second line (line 2188) with no expand chevron/toggle and no inline detail strip. No `expandedIds`/`renderExpanded` capability added to OpenBooksDataTable. The e2e merchant cell index is still `td.nth(2)` (core-screens.spec.ts:66), consistent with no column restructuring. DisplaySettingsMenu density toggle predates this ticket.
- `E7-4` **PARTIAL** — One sub-DoD is pre-satisfied by existing code (not by E7): unposted rows DO read 'Not posted' because rowAttention returns 'unposted' for entryId-null non-excluded rows (CoreScreens.tsx:1476) and AttentionState maps 'unposted'->label 'Not posted' (AttentionState.tsx:72-76). BUT the core ticket intent is unstarted: the Status column (CoreScreens.tsx:2270-2283) shows AttentionState or a Posted/Excluded dot with NO provenance-vs-AI de-duplication (there is no provenance chip to de-dupe against — E7-1/E7-2 absent), and the AI facet (CoreScreens.tsx:2134) filters the raw `ai` array, not `provenance.kind==='ai'`; no Rule/Memory provenance-aware facet was added. Cannot be 'done' without E7-1/E7-2.
- `E7-5` todo — ColumnDef (OpenBooksDataTable.tsx:35-66) has only `mobilePrimary`/`mobileTrailing`; no `mobileMeta`/`mobileHidden` field added. The mobile card path (OpenBooksDataTable.tsx:556-567) still dumps ALL non-primary columns (category combobox, contact, account, status) into a verbose `<dl>` label/value list with no compact meta line and no expand. The register column config (CoreScreens.tsx:2168) is unchanged. The 390px expectNoHorizontalScroll e2e assertion runs on /dashboard, not /transactions with rows (core-screens.spec.ts:168-171).
- `E7-6` todo — bulkRecategorize (CoreScreens.tsx:1937-1955) still hardcodes the single `otherIncomeCategoryId` target (line 1945) with no category picker. No register keyboard model: the only J/K/Enter/E handler (CoreScreens.tsx:879-910) lives inside the InboxScreen (operates on orderedItems/selectItem/confirmBatch/excludeSelected/selectedReceipt) — TransactionsScreen has no keydown handler and no key-hint footer. InboxKeyHint (line 1063) is Inbox-only.
- `E7-7` todo — TransactionDetail has no grouped 'Correct this entry' section; correction controls are loose (CoreScreens.tsx:2667-2739). Quick Recategorize still targets the hardcoded `otherIncomeCategoryId` (line 2671-2672). Split Post is disabled only on missing category ids (`!splitFirstCategoryId || !splitSecondCategoryId`, line 2732) with NO amount-sum-equals-original pre-validation. Exclude button reads just 'Exclude' (line 2766-2768), not 'Exclude (reverses any posted entry)'. The split copy at line 2696 mentions reverse+repost but is per-control, not the required grouped immutable-history section.
- `E7-8` todo — No `InsightBanner` component exists anywhere (`grep -rn InsightBanner apps/web/src convex/` empty) — E8-T3 dependency unmet. No banner is mounted above the register: TransactionsInsights is only wired as the separate Insights sub-tab (CoreScreens.tsx:2359), and the WorkbenchSurface `banner` slot (WorkbenchSurface.tsx:39,110) is used for the status message, not a page insight banner. The server insights aggregate (coreViews.ts:829-843) exists but is not consumed by any compact above-table banner.
- `E7-9` todo — Filter rail still exposes Date and Amount in BOTH the FilterPanelButton panel (filterFacets has date at CoreScreens.tsx:2127 and amount at 2129) AND as standalone pills (DateRangeControl 2414, AmountFilterPill 2415); no single-source decision comment (the 2406-2407 comment is about global search). AI facet (2134) is not provenance-aware. Sort/group/display menu derivations (2288-2304) are unchanged because the E7-3/E7-5 column changes never landed. Also depends on E7-3/E7-5/E7-10, all todo.
- `E7-10` todo — Hardcoded '2026-06-30' literals remain as silent defaults: manualDate useState (CoreScreens.tsx:1627), CSV sample (1630), dateRangeValueToISO fallback (1638), and manual-add post date fallback (1830). No shared today/asOf helper exists (`apps/web/src/lib/openbooks/today.ts` absent; no serverToday/asOf in apps/web/src/lib) — E8-T1 dependency unmet. InsightsScreen.tsx:32 also still hardcodes `TODAY_ISO = '2026-06-30'` (the co-owned centralization target in the ticket note).
- `E7-11` todo — This is the closeout/evidence ticket depending on E7-3..E7-10, none of which are done, so it cannot be complete. No E7 register evidence set (compact 1440px rows + provenance chips, 390px /transactions card no-scroll proof, expanded row, 'Correct this entry' drawer section, insight banner) can exist since those features are unimplemented. The e2e specs run but assert the pre-E7 surface; convex/ledger.ts being unchanged is trivially true only because no E7 work touched the tree.

## E8

Epic E8 (Insights everywhere: per-page banners + redesigned insights screens on real ledger data) is essentially NOT STARTED on branch codex/real-world-testing (HEAD 9b5154d). None of the new deliverable files exist: apps/web/src/lib/openbooks/today.ts (T1), workbench/InsightBanner.tsx + workbench/page-insights.ts + page-insights.test.ts (T3), tests/e2e/redesign-e8-insights-banners.spec.ts (T9). The confirmed RC6 hardcoded dates are all still present verbatim: InsightsScreen.tsx:32 `const TODAY_ISO = "2026-06-30"` (used at 77/284/406/609/639/873/903/1074/1102/1305) and isDormant `2026-04-01` (1324); ModuleScreens.tsx 2026-06-13/2026-06-30 (345/348/1268) + PAYROLL_CURRENT_PERIOD "2026-06" (1221); CoreScreens.tsx 2026-06-30 (1627/1630/1638/1830); coreViews.ts literal months array (12-25, starts "2025-07") and due-soon cutoff `<= "2026-06-30"` (456). The Bills section still routes to SectionInsightsStub ("Coming in this pass") in the dispatcher (InsightsScreen.tsx:389). There are zero InsightBanner/page-insight-banner references anywhere in apps/web/src or tests, and no useActiveScope portfolio wiring. Two tickets earn PARTIAL because pre-existing (pre-epic) infrastructure overlaps their DoD: T6's fixture gate already throws when OPENBOOKS_DEV_AUTH_BYPASS!=="1" (insightsFixtures.ts:59-60), and T8's deterministic AI fallback + disclaimer + "do not invent numbers" prompt already exist in aiInsights.ts — but aiInsights is still Bedrock-only (invokeBedrockPayload/extractBedrockResponseText), not routed through the provider-agnostic aiProvider/aiProviderRegistry runtime that T8 requires, and no Explain affordance or banner exists. Legacy components InsightsBand/MiniCashflowStrip/InsightsDashboard that T7 must retire still exist and are still exported from workbench/index.ts:53-56. A separate detached worktree (.claude/worktrees/wf_6a7135ae-d36-5) contains page-insights.ts and a page-insights.test.ts, but that work is NOT on this branch and does not count.

- `E8-T1` todo — apps/web/src/lib/openbooks/today.ts does not exist (ls fails; lib/openbooks/ contains active-entity.tsx, report-periods.ts, etc. but no today.ts). InsightsScreen.tsx:32 still has `const TODAY_ISO = "2026-06-30"`, passed to resolveScope at lines 77/406/639/903/1102 and InsightsPanel todayISO at 284/609/873/1074/1305. isDormant still hardcodes `2026-04-01` at line 1324. grep for useTodayIso/todayIso in InsightsScreen.tsx returns nothing. DoD 'grep for 2026-06-30 returns zero matches' fails.
- `E8-T2` todo — All targeted literal dates still present: ModuleScreens.tsx:345 `dateRangeValueToISO(range, "2026-06-13")`, :348 `to: "2026-06-30"`, :1221 `PAYROLL_CURRENT_PERIOD = "2026-06"`, :1268 `"2026-06-13"`. CoreScreens.tsx:1638 `dateRangeValueToISO(range, "2026-06-30")`, :1830 `manualDate || "2026-06-30"`. convex/coreViews.ts:12-25 still a static literal `months` array starting "2025-07"; line 456 still `bill.dueDate <= "2026-06-30"`. coreViews.dashboard has no optional `asOf` arg threaded in. No server-clock hook to consume (depends on T1, also todo).
- `E8-T3` todo — apps/web/src/components/openbooks/workbench/InsightBanner.tsx and page-insights.ts do not exist (ls fails). page-insights.test.ts not present under apps/web. workbench/index.ts has no InsightBanner/page-insights/PageInsight export (grep empty). No data-testid='page-insight-banner' anywhere in apps/web/src.
- `E8-T4` todo — grep -rn 'InsightBanner|page-insight-banner|buildPageInsight|page-insights' across apps/web/src and tests returns zero matches. No banner mounted on Transactions/Income/Expenses/Dashboard. No useActiveScope() usage in apps/web/src (grep empty), so the All/Portfolio scope wiring required by the DoD is absent. Blocked on T3 (todo).
- `E8-T5` todo — SectionInsights dispatcher (InsightsScreen.tsx:373-390) handles transactions/income/expenses/contacts/payroll and falls through to `return <SectionInsightsStub label=... />` at line 389 for bills; SectionInsightsStub (364-368) still renders description 'Coming in this pass'. No BillsInsights component defined (grep finds none). No InsightBanner on Contacts/Payroll/Bills (no InsightBanner exists at all). DoD 'Bills renders a real panel, not the stub' fails.
- `E8-T6` **PARTIAL** — Pre-existing gate satisfies one DoD item: convex/insightsFixtures.ts:59-60 throws ConvexError when `process.env.OPENBOOKS_DEV_AUTH_BYPASS !== "1"`, and seeds only a DISPOSABLE entity (comment line 8). MISSING: no dedicated unit test asserting seedInsightsEntity throws with the env unset (only tests/e2e/redesign-e1-insights.spec.ts relies on the fixture end-to-end; no convex/*.test.ts covers the throw); no documented provenance comment block was added; page-insights.ts source-confirmation cannot exist because the file is not built (T3 todo). Net: the gate predates this epic and the epic-specific deliverables (provenance doc + fixture-gate unit test) are absent.
- `E8-T7` todo — The three legacy components the ticket must RETIRE still exist on disk (workbench/InsightsBand.tsx, MiniCashflowStrip.tsx, InsightsDashboard.tsx) AND are still exported from workbench/index.ts:53-56 (`export { InsightsBand ... }`, `export { MiniCashflowStrip }`, `export { InsightsDashboard }`); workbench-config.ts:92 still references InsightsDashboard. No Bills section exists to make 'all six' panels consistent (T5 todo). DoD 'legacy components removed and references cleaned' fails. No epic-attributable responsive/redesign change to the insights panels found.
- `E8-T8` **PARTIAL** — Pre-existing pieces overlap DoD: aiInsights.ts has a deterministic fallback (189-220), disclaimer at line 193 `${DEFAULT_DISCLAIMER} (Computed without AI: the AI provider was unavailable.)`, and prompt 'Do not invent numbers that are not in the data.' (251). MISSING the epic's core changes: aiInsights.ts still calls Bedrock directly (imports invokeBedrockPayload/extractBedrockResponseText at lines 11-12, invoked ~204-206) and is NOT routed through the provider-agnostic aiProvider.ts/aiProviderRegistry.ts runtime that T8 mandates; no InsightBanner 'Explain' affordance exists (no InsightBanner at all); no narrative-fallback unit test added for the no-resolvable-key path. So the fallback/disclaimer plumbing predates the epic and the epic's deliverables are not done.
- `E8-T9` todo — tests/e2e/redesign-e8-insights-banners.spec.ts does not exist (ls fails). redesign-e0-subtabs.spec.ts only tests the Transactions section sub-tabs (cases E0.1-E0.6, all on /transactions); its insights-dashboard assertions (lines 85/106/186) are for Transactions Insights, with no Bills→Insights case. No e2e asserts page-insight-banner presence/distinctness or an injected-today resolved-date label. Blocked on T1/T4/T5 (all todo).

## E9 — Dashboard enhancement + AI CFO / Financial Advisor + weekly digests

All 9 E9 tickets are TODO on branch codex/real-world-testing (HEAD 9b5154d). The recently merged work (UI redesign, Plaid/Stripe connectors, E7 double-count fix c23dc4c, E6 confidence calibration 1484e72, payroll) touched none of the E9 surface. Hard evidence: (1) the entire AI-CFO layer is absent — no convex/aiCfo.ts, aiCfoAggregate.ts, aiCfoAnomalies.ts, weeklyDigest.ts, and a repo-wide grep for aiCfo/CfoSignals/generateAdvisories/getRunway/taxSetAside/digestLog/streamTag/cashReconciliation/AdvisorPanel/revenueByStream returns ZERO hits across convex, apps/web/src, and packages. (2) The two correctness tickets (T1, T2) still have every original defect: coreViews.ts:180 uses the live Plaid balance as the cash source, coreViews.ts:201-216 sums raw transaction.amountMinor for the cash-flow chart with the derived sparkline at 470-473, the literal months array sits at coreViews.ts:12-25, the due-soon cutoff hardcodes '2026-06-30' at coreViews.ts:456, InsightsScreen.tsx:32 freezes TODAY_ISO='2026-06-30' (plus the '2026-04-01' dormant threshold at 1324), and agentToolQueries.ts:35-36 defaults to a calendar-2026 window. (3) Cross-epic dependencies are also unmet: the unified E3 `credentials` table (kind:'ai'/'plunk') that T4/T6 require does not exist — schema still uses the per-field aiCredentials table (schema.ts:326). crons.ts has only Plaid + payroll crons (no digest), agentTools read set has no advisor tools, and there are no new E9 test files (no cashflow/window/cfo/digest/revenueByStream/anomaly tests).

- `E9-T1` todo — Dashboard cash/cash-flow correctness NOT done. convex/coreViews.ts:180 still reads the live Plaid balance as the cash source: `const amountMinor = bankAccount.plaidAccountId ? bankAccount.balanceMinor : ledgerBalanceMinor;`, and cashPositionMinor (coreViews.ts:249-251) sums those bankBalances, so the tile diverges from the Balance Sheet. No `cashReconciliation` block exists (grep for cashReconciliation across convex/apps/packages returns nothing). cashFlowByMonth (coreViews.ts:201-216) still sums raw `transaction.amountMinor` with no transfer/intercompany exclusion, and cashSparkline (coreViews.ts:470-473) is derived from that raw sum. CoreScreens.tsx has no 'Bank says / Books say / N to review' line (grep returns nothing). No convex/coreViews.cashflow.test.ts exists (ls convex/*.test.ts has no cashflow test).
- `E9-T2` todo — Hardcoded time windows all remain. coreViews.ts:12-25 still has the literal `months` array '2025-07'…'2026-06'; coreViews.ts:456 still hardcodes due-soon cutoff `bill.dueDate <= "2026-06-30"`. apps/web/.../InsightsScreen.tsx:32 still `const TODAY_ISO = "2026-06-30";` (used at lines 77,284,406,609,…) and the dormant-contact threshold `lastActivityDate < "2026-04-01"` at line 1324. convex/agentToolQueries.ts:35-36 still `DEFAULT_START_DATE = "2026-01-01"` / `DEFAULT_END_DATE = "2026-12-31"` (calendar-2026 window, used at line 154). No asOf-driven window function and no convex/coreViews.window.test.ts.
- `E9-T3` todo — No CFO aggregate exists. There is no convex/aiCfoAggregate.ts (ls convex/ shows no cfo file). grep for CfoSignals/taxSetAside/getRunway across convex/apps/packages returns nothing. No `taxSetAsidePct` workspace setting in convex/schema.ts (grep returns nothing). No convex/aiCfoAggregate.test.ts. None of the seven signal families (runway/burn, income trend, expense creep, concentration, forecast, tax set-aside) are implemented as a typed CfoSignals object.
- `E9-T4` todo — No CFO advisory engine. convex/aiCfo.ts does not exist; generateAdvisories symbol not found anywhere (grep returns nothing). No deterministic-fallback advisory card builder, no convex/aiCfo.test.ts. Additionally the unified E3 `credentials` table this ticket depends on does not exist — schema still has the per-field `aiCredentials` table (convex/schema.ts:326) keyed by provider, with no `kind:"ai"` unified resolver, so even the dependency is unmet.
- `E9-T5` todo — No advisor surface. apps/web/src/components/openbooks/dashboard/ contains only DashboardViz.tsx — no AdvisorPanel.tsx. grep for AdvisorPanel/generateAdvisories in apps/web/src returns nothing; CoreScreens.tsx does not mount any advisor section (grep for AdvisorPanel/reconcil/Books say returns nothing). The reuse target coreViews.insightsDrill exists (coreViews.ts:984) but nothing consumes it for an advisor panel.
- `E9-T6` todo — No weekly digest. convex/weeklyDigest.ts does not exist (grep for weeklyDigest returns nothing). convex/crons.ts (lines 7-20) contains only the Plaid sync interval and the daily 'auto-draft payroll runs' cron — no weekly digest cron. No `digestLog` table in convex/schema.ts (grep returns nothing). packages/email/src/plunk.ts sendPlunkEmail exists but is not wired to any digest job. The unified Plunk credential (E3 `kind:"plunk"`) the ticket reads from also does not exist.
- `E9-T7` todo — No advisor Ask-AI tools. convex/agentTools.ts openBooksReadTools (lines 304-315) lists getReport, getBalances, queryTransactions, searchContacts, getPayrollRuns, and propose* tools — no getRunwayAndBurn or getAdvisories. grep for getRunway/getAdvisories across convex returns nothing. No matching internal query in agentToolQueries.ts, no convex/agentTools.cfo.test.ts, and aiChatRuntime.ts does not reference these tools.
- `E9-T8` todo — No revenue-by-stream. No `streamTag`/`streamGroup` field on ledgerAccounts in convex/schema.ts (grep returns nothing). No `revenueByStream` rollup in coreViews.ts or incomeViews.ts (grep returns nothing). No RevenueByStream widget in apps/web/src/components/openbooks/dashboard/ (only DashboardViz.tsx present). No convex/revenueByStream.test.ts and no stream-taxonomy mutation.
- `E9-T9` todo — No anomaly/duplicate detection signal. convex/aiCfoAnomalies.ts does not exist and aiCfoAggregate.ts (its alternative host) does not exist either. No duplicate-candidate / amount-spike / new-large-vendor logic feeding CfoSignals (grep for aiCfo/CfoSignals returns nothing). No convex/aiCfoAnomalies.test.ts. Depends on E9-T3 which is itself todo.

## E10 — Payroll — verify, fix & integrate

None of the 7 E10 tickets are 'done' on branch codex/real-world-testing (HEAD 9b5154d). The actual E10 implementation was authored on a separate branch — commit 32ab97e 'feat(payroll): E10 Integration, e2e, full gate-green & evidence', which exists only on feat/payroll-module and is NOT an ancestor of the current HEAD (confirmed with git merge-base --is-ancestor). As a result, every E10-specific deliverable is missing from this branch: the bank matcher still uses the date-blind /payroll|gusto|wise/i regex with no exact-minor/unique/window guard and is unwired from markLinePaid (T2 todo); payrollMath.ts still hardcodes PKR:278/INR:83 with no fetched day-of-pay rate, no settlementFxRate override, and no settledFxRateMicros schema field (T3 todo); Expenses still shows no first-class payroll group and the 'fewer vendor lines' comment remains (T4 todo); the PayrollScreen has no pay-schedule control and statements are a single combined CSV, not per-currency (T5 partial); there is no payroll insight banner and the 2026-06 hardcoded dates remain in moduleViews and ModuleScreens (T6 todo); and statement() still hardcodes the acme-studio-llc slug with first-checking bank selection (T7 todo). Two tickets are 'partial' only on pre-existing baseline scaffolding the epic itself documented as the starting point (T1: trialBalance helper + a single-currency D4 e2e; T5: the setPaySchedule/autoDraft backend + NO-OP safety test). What is on this branch is the epic's documented 'Current state', not its 'Definition of done'. To land E10 here, merge/cherry-pick feat/payroll-module (32ab97e) or re-implement all seven tickets.

- `E10-T1` **PARTIAL** — The E10 multi-currency lifecycle work is NOT on this branch. convex/payroll.test.ts (HEAD, 227 lines) contains only the original 5 tests, all using a SINGLE USD employee via setupPayroll (convex/payroll.test.ts:15-82). There is no setupPayrollMultiCurrency helper, no USD+PKR+INR lifecycle test, no Payroll-Payable-net-0 assertion, and no statementGroups-vs-currencyTotals reconciliation test. The trialBalance helper exists (payroll.test.ts:102) and the e2e D4 test asserts payroll-base-total visibility + writes a screenshot (tests/e2e/reports-payroll.spec.ts:87-97), but that screenshot is the pre-existing 2026-06-11-D4-payroll-run-detail.png (single-currency demo run); the required docs/finishing/evidence/<date>-E10-payroll-multicurrency-detail.png does not exist and no per-currency footer assertion was added. The committed E10 work (commit 32ab97e) lives on branch feat/payroll-module and is NOT an ancestor of HEAD 9b5154d (verified via git merge-base --is-ancestor).
- `E10-T2` todo — findMatchingBankTxn (convex/payroll.ts:501-519) is unchanged from the epic's documented defect state: it still filters on /payroll|gusto|wise/i (token-narrow), never reads txn.date or applies a ±5-day window (date-blind), has no exact-base-minor equality nor uniqueness guard (sorts by closest amount and returns candidates[0]), and is currency-blind. It is still invoked ONLY from markRunPaid (payroll.ts:664), NOT from markLinePaid (payroll.ts:597-630, which only consumes a caller-supplied matchedTxnId). No settlementTxnId-double-match guard beyond the in-loop usedTxnIds set. No regression tests for exact/unique/in-window match, keyword-less match, per-line consumption, or no-double-match exist in payroll.test.ts. The pre-existing baseline does mark a matched txn review:'confirmed'+categoryAccountId=payable in settleLine (payroll.ts:582-591), but that is the starting scaffolding, not the T2 rewrite.
- `E10-T3` todo — The hardcoded rate constant DEFAULT_LOCAL_PER_BASE = { PKR: 278, INR: 83 } is still present (convex/payrollMath.ts:16-24) — no fetched day-of-pay rate, no Convex action, no persistence of a fetched micro-unit rate. markLinePaid/markRunPaid do NOT accept a settlementFxRate / per-currency override (payroll.ts:597-601, 643-644). settleLine still derives settlementBaseMinor from line.fxRateMicros (the accrual rate) at payroll.ts:539, so the FX path stays dead on real data. Schema payrollRunLines (convex/schema.ts:683-705) has NO settledFxRateMicros field. No unit test settles a PKR line at a different pay-day rate to assert a signed FX line + payable clearing to 0. Pre-existing baseline only: the FX_GAIN_NUMBER/FX_LOSS_NUMBER branching and settledBaseMinor field already exist (payroll.ts:31-32, 546-559; schema.ts:702), which is the documented starting point, not the T3 deliverable.
- `E10-T4` todo — convex/expensesViews.ts still carries the exact admission the ticket targets — a comment at :260 saying payroll accruals 'simply show fewer vendor lines.' There is no synthetic 'Payroll' expense group sourced from journal lines on account 5000, no source:'payroll' provenance tag, and no run linkage (grep for payroll/5000/source/provenance in expensesViews.ts finds nothing relevant). Account 5000 is only swept into generic categoryTotals because it is type 'expense'. No reconciliation test asserting Expenses payroll base == reportViews Payroll Summary base total exists (convex/expensesViews.test.ts has no payroll/5000/reconcile case). reportViews already has a siloed Payroll Summary (reportViews.ts:734-771), which is the pre-existing baseline, not the cross-lens integration this ticket requires.
- `E10-T5` **PARTIAL** — Backend scaffolding exists and is the documented starting point: paySchedule (payroll.ts:738), setPaySchedule (payroll.ts:761), autoDraftScheduledRuns (payroll.ts:810) with the NO-OP/idempotency safety test still green (payroll.test.ts:187-226), and the cron is wired. But the T5 deliverables are absent: ModuleScreens.tsx PayrollScreen has NO pay-schedule control (no setPaySchedule/paySchedule calls in the file). Statements are NOT per-currency — moduleViews.ts builds a single statementRows + single statementCsv (moduleViews.ts:659-681) and the UI exports one combined CSV via downloadCsv('openbooks-payroll-statement.csv', data.payroll.statementCsv) (ModuleScreens.tsx:2006). No per-currency block/export, no e2e toggling a schedule + per-currency CSV, and no multi-currency auto-draft unit test.
- `E10-T6` todo — Hardcoded dates remain throughout the payroll read/UI path: convex/moduleViews.ts:7 today='2026-06-11' (plus numerous .startsWith('2026') filters), ModuleScreens.tsx:1221 PAYROLL_CURRENT_PERIOD='2026-06', :1268 and :345 dateRangeValueToISO(range,'2026-06-13'), :348 to:'2026-06-30'. The moduleViews payroll block (moduleViews.ts:776-808) returns NO insight object — no run-rate, headcount-cost, or FX-exposure metric. The PayrollScreen body (ModuleScreens.tsx:1376-1429) renders NO AiInsightBadge/InsightsBand (the import at :43 is used only by the Income/Expenses screens at :586 and :721). No unit test for run-rate/FX-exposure in convex/moduleViews.test.ts. No single now/clock source was introduced for payroll.
- `E10-T7` todo — The statement query still hardcodes the demo slug: convex/payroll.ts:701 resolves the default entity via by_workspace_and_slug with slug 'acme-studio-llc'. resolveSettlementBankAccount (payroll.ts:484-494) still just picks the first 'checking' account (or bankAccounts[0]) with no documented deterministic per-entity precedence and no payroll-mapped-account preference. No test asserts cross-workspace/non-member rejection of payroll queries or that statement(entityId) resolves the correct entity by id (payroll.test.ts has only period-lock and double-approve rejection tests at :153 and :166, both same-workspace). All payroll fns do re-check authz via requireWorkspacePermission, but the slug coupling, deterministic bank selection, and the new authz/resolution tests this ticket requires are absent.

## E11

Epic E11 (data lifecycle — per-workspace factory reset, isolated no-login public demo, read-only guard, demo-bleed fix, full-account export, cron, e2e/docs) is entirely UNSTARTED on branch codex/real-world-testing (HEAD 9b5154d). All 10 tickets are todo. Every net-new file the epic requires is absent: convex/activeEntity.ts, convex/demoWorkspace.ts, convex/workspaceReset.ts, convex/publicDemo.ts, convex/publicDemoSession.ts, convex/exportAccount.ts, and apps/web/src/app/demo/page.tsx all do not exist. The confirmed root-cause demo-bleed bug is still live: the acme-studio-llc demo-slug fallback remains in all five view files (coreViews.ts:34, incomeViews.ts:20, expensesViews.ts:25, moduleViews.ts:24, reportViews.ts:46), each with its own copy-pasted getActiveEntity. The workspaces table (schema.ts:28-33) has no isDemo/demoKind fields (the isDemo at schema.ts:72 is on entities, as the ticket states). No isDemoWorkspace/getPublicDemoWorkspace/requireWorkspaceRead/assertNotDemoWrite helpers exist. realTestReset.ts writes only a realTestResetJobs row and no auditEvents. DataSection.tsx renders only the global RealTestResetPanel and exports only the report pack over a hardcoded 2026 range. app/page.tsx demo CTAs all point to /dashboard; line 544 still claims 'No tracking, no account required'. crons.ts has only Plaid sync + payroll auto-draft. No OPENBOOKS_PUBLIC_DEMO_ENABLED flag, no no-bleed test, no lifecycle e2e, no data-lifecycle doc. Note: entities.ts:96 sorts isDemo entities FIRST and entities.create hardcodes isDemo:false, reinforcing the demo-bias the epic should remove. None of the recently-merged work (redesign, Plaid/Stripe connectors, c23dc4c double-count fix, 1484e72 AI gate, payroll) touches any E11 surface.</parameter>
</invoke>


- `E11-T1` todo — Demo-slug fallback fully intact and not extracted. grep 'acme-studio-llc' convex/*Views.ts returns 5 matches: coreViews.ts:34, incomeViews.ts:20, expensesViews.ts:25, moduleViews.ts:24, reportViews.ts:46. No shared convex/activeEntity.ts (file absent); getActiveEntity copy-pasted in coreViews.ts:27-44, incomeViews.ts:13, expensesViews.ts:18, moduleViews.ts:17 with demo-slug lookup ranked ABOVE the workspace-first fallback. No no-bleed regression test (grep bleed/noBleed returns nothing).
- `E11-T2` todo — workspaces table (schema.ts:28-33) has only name/slug/createdAt/updatedAt + by_slug index — no isDemo, no demoKind. The isDemo:v.boolean() at schema.ts:72 is on the entities table, not workspaces. convex/demoWorkspace.ts does not exist; isDemoWorkspace/getPublicDemoWorkspace helpers absent everywhere. No backfill internalMutation.
- `E11-T3` todo — convex/workspaceReset.ts does not exist. No resetWorkspace action, no per-workspace preview, no workspace-name re-type confirmation, no 'workspace.reset.factory' auditEvents write. DataSection.tsx renders only RealTestResetPanel (lines 76, 94), the global dev nuke; no two-panel split, no owner-facing factory-reset panel. onboarding.ts not wired to any reset.
- `E11-T4` todo — convex/publicDemo.ts does not exist; no ensurePublicDemoWorkspace, seedPublicDemo, or resetAndSeedPublicDemo. seedDemo.ts still seeds into the caller's own workspace under the acme-studio-llc slug (no shared seed routine extracted). No isolated public demo workspace; grep publicDemo/public-demo/demoKind returns nothing.
- `E11-T5` todo — apps/web/src/app/demo/page.tsx does not exist (no /demo route). convex/publicDemoSession.ts does not exist. session.ts has no demo handling. AppShell still redirects unauthenticated users to sign-in with no /demo bypass. Landing demo CTAs (app/page.tsx:166, 184, 380, 519) still point to /dashboard. requireWorkspaceRead helper absent.
- `E11-T6` todo — assertNotDemoWrite does not exist in convex/demoWorkspace.ts (file absent). No write mutation (ledger.postEntry, pipeline.routeTransaction, rules, connections, onboarding, entities.create, invoices/bills/payroll, seedDemo, workspaceReset) calls any demo write-guard. No read-only enforcement test. No demo source of truth (workspaces.isDemo) for a guard to consult.
- `E11-T7` todo — realTestReset.ts writes only a realTestResetJobs row (db.insert line 359) and recreates Z360 (inserts at 257/288/315) — NO auditEvents row; 'auditEvents' appears at line 50 only as a table it DELETES. No 'workspace.global_reset' action. DataSection.tsx copy still describes one global reset with no global-vs-factory distinction. No runbook note.
- `E11-T8` todo — convex/crons.ts has only 'sync Plaid transactions' (4h) and 'auto-draft payroll runs' (09:00 UTC) — no demo reset cron, no internal.publicDemo.resetAndSeedPublicDemo (target absent). OPENBOOKS_PUBLIC_DEMO_ENABLED flag appears nowhere. No idempotency/determinism test.
- `E11-T9` todo — convex/exportAccount.ts does not exist. reports-export.ts exposes only settingsDataExportFiles (report pack, line 483) — no full-account snapshot of entities/accounts/journal/transactions/contacts/invoices/bills/employees/payroll/rules/connections. DataSection.tsx export (lines 38-69) emits only report-pack CSV/JSON/GL over the hardcoded 2026 REPORT_ARGS range (lines 20-26) — no 'Export everything' button, no full-history range, no 'workspace.exported' audit, no secret-free test.
- `E11-T10` todo — No Playwright e2e for reset/demo-read-only/export (no playwright config or *.spec.ts under apps/web outside node_modules; grep factory//demo/Export everything/resetWorkspace in tests returns nothing). No server no-bleed invariant test. No data-lifecycle doc in docs/finishing. app/page.tsx:544 'No tracking, no account required' copy unchanged. Blocked: all upstream deps (T3/T5/T6/T9) are todo.

## E12 — Settings & app-shell UI overhaul (make all 11 sections real, on-brand, responsive, + wire scope-switcher hook)

All 10 E12 tickets are TODO on branch codex/real-world-testing (HEAD 9b5154d) — the Settings & app-shell overhaul has not begun. Concrete absences verified in code: (T1) no apps/web/src/components/openbooks/settings/_shell.tsx and zero imports of SettingsCard/SettingsSaveBar/etc.; (T2) no entities.updateProfile mutation and no edit-business dialog in BusinessesSection.tsx; (T3) no categories.moveGroup, and accountant mode still shows only number·type with no normal-side or move control (the pre-existing baseline); (T4) no rules.previewAll/condition-group support — rules remain flat AND-only with single-rule preview at rules.ts:300; (T5) NotificationsSection email is display-only with a static Plunk chip, no editable email/cadence/Connections deep-link, and `digest` is a boolean toggle not an off/weekly/monthly cadence (no cadence field/setter); (T6) convex/team.ts has no changeRole/removeMember, TeamSection.tsx is read-only and never surfaces the existing revokeInvite; (T7) no convex/audit.ts, audit still .take(200) at moduleViews.ts:177 and filtered client-side via useMemo in AuditSection.tsx with no Load-more; (T8) active-entity.tsx has no scope/selectScope/useActiveScope and AppShell still uses the flat 'Viewing books for' BusinessSwitcher (line 999) with no 'All businesses' option; (T9) blocked by T1+T8 and shell is unchanged; (T10) settings.spec.ts has only the 3 pre-existing tests and no T2–T8 cases or a11y assertion. The redesign/connector/AI-calibration work that landed on this branch touches E1/E2/E3/E6/E7/E8/E9 surfaces but did NOT implement any E12 Settings or scope-switcher work. Note: backlog.json does not carry per-ticket Definition-of-Done text (only id/epic/title/files/dependsOn/size/risk); DoD was taken from docs/launch-sprint/epics/E12-settings-app-shell-ui-overhaul-make-all-11-secti.md. Key files inspected: apps/web/src/components/openbooks/settings/{TeamSection,AuditSection,CategoriesSection,NotificationsSection,BusinessesSection,RulesSection}.tsx, apps/web/src/lib/openbooks/active-entity.tsx, apps/web/src/components/openbooks/AppShell.tsx, convex/{entities,categories,rules,settings,team,moduleViews,schema}.ts, tests/e2e/settings.spec.ts.

- `E12-T1` todo — Shared shell primitives do not exist. `apps/web/src/components/openbooks/settings/_shell.tsx` is absent (ls of apps/web/src/components/openbooks/settings/ shows only the *Section.tsx files + connections/, no _shell.tsx). A repo-wide grep for `settings/_shell`, `SettingsCard`, `SettingsSaveBar`, `SettingsEntityPicker`, `SettingsSectionShell`, `SettingsEmptyState` across apps/web/src returns ZERO hits, so Tax/Notifications/Data still use ad-hoc per-section card markup (e.g. raw `rounded-[14px] border bg-card ... shadow-xs` strings inline in TeamSection.tsx:43, AuditSection.tsx, etc.). No DoD item met.
- `E12-T2` todo — No `updateProfile` mutation in convex/entities.ts — exported mutations are only create:135, archive:191, updateTaxSettings:255 (grep for `updateProfile` returns nothing). BusinessesSection.tsx has no Edit action/dialog: grep for `updateProfile|Edit|EditBusiness` in BusinessesSection.tsx returns no matches. No e2e 'edit business' rename case (settings.spec.ts has only 3 tests, none for edit-business). Every DoD item unmet.
- `E12-T3` todo — No `moveGroup` mutation in convex/categories.ts (exports are list:21, rename:79, setArchived:104, createCategory:155, recategorizeTransaction:227). CategoriesSection.tsx accountant mode (line 130-135) shows only `{cat.number} · {cat.type}` with NO normal side (debit/credit) and NO per-row 'Move to…' Select — this accountant-mode toggle + number·type display is the pre-existing baseline the epic 'Current state' describes, not new E12 work. No category-move e2e case. All DoD items unmet.
- `E12-T4` todo — convex/rules.ts has no `previewAll` and no condition-group support — exports are list:62, save:150, reorder:219, setActive:252, remove:274, preview:300 (single-rule only); grep for `previewAll|conditionGroups|groups` returns nothing. RulesSection.tsx has no add/remove condition group UI nor 'test all active rules' (grep for `conditionGroups|test all|previewAll|addGroup` returns nothing). No convex/rules.test.ts matcher test for group-OR/condition-AND. Rules remain flat AND-only. All DoD items unmet.
- `E12-T5` todo — convex/settings.ts exposes setNotificationEmail:85 (pre-existing) but the `digest` field is a boolean notification toggle (settings.ts:14 DEFAULT_NOTIFICATIONS.digest + literal at line 24), NOT an off/weekly/monthly cadence; no setNotificationCadence and no cadence field in schema.ts (only payroll cadence at schema.ts:651). NotificationsSection.tsx email is display-only with a static chip 'Email delivery wired to Plunk when configured' (line 65) — no editable email input bound to setNotificationEmail, no cadence Select, no deep-link to /settings/connections (grep for `cadence|Connections|setNotificationEmail` in the section returns no usage). emailDeliveryConfigured is still just Boolean(process.env.PLUNK_API_KEY) at settings.ts:50. All DoD items unmet.
- `E12-T6` todo — convex/team.ts has no changeRole or removeMember (exports: list:41, invite:99, lookupInvite:153, revokeInvite:180). TeamSection.tsx (read in full) renders members read-only: members map to a row showing avatar/name/role pill with no per-member role Select and no remove action; the only Select (line 141) is the invite-modal role picker. revokeInvite exists in team.ts but is never imported/used in TeamSection.tsx — pending rows show only an 'Invite sent' badge (line 65) with no revoke control. No last-owner guard added, no team-management e2e case. All DoD items unmet.
- `E12-T7` todo — No convex/audit.ts file and no paginated query — grep for `.paginate(`/`paginationOpts` in moduleViews.ts/audit.ts returns nothing; auditEvents are still fetched via moduleViews.ts:177 `.withIndex('by_workspace').order('desc').take(200)`. AuditSection.tsx still filters CLIENT-SIDE: a `useMemo` (line 40) over `data?.settings.audit.rows` applies text/actor/since filters in the browser, with no cursor/'Load more' control (grep for `Load more|usePaginatedQuery|cursor` returns nothing). All DoD items unmet.
- `E12-T8` todo — apps/web/src/lib/openbooks/active-entity.tsx has no scope concept: ActiveEntityContextValue (line 20) lacks scope/selectScope; only useActiveEntity is exported (line 48) — no useActiveScope, no `kind:'all'|'entity'` shape, no E5 contract comment. AppShell.tsx still renders the old flat `BusinessSwitcher` (function at line 999) with a `DropdownMenuLabel` 'Viewing books for' (line 1024) and no 'All businesses'/portfolio option (grep for `ScopeSwitcher|All businesses|scope-switcher|useActiveScope` returns nothing). SettingsScreen.tsx has no scope='all' fallback hint. All DoD items unmet.
- `E12-T9` todo — Depends on E12-T1 and E12-T8, both of which are todo, so the T9-specific fixes cannot exist. The shell is still in its pre-E12 baseline state (BusinessSwitcher 'Viewing books for', AppShell.tsx) with no scope-switcher to collide with #ob-topbar-page-actions and no shared-shell mobile drill-in to match (no _shell.tsx). No evidence of any E12-scoped responsiveness/overflow fixes or new viewport assertions tied to this work. Todo.
- `E12-T10` todo — tests/e2e/settings.spec.ts contains only 3 tests (line 103 nav/add-business/audit-filter, line 159 AI autonomy persist, line 178 rule reorder) — these are the pre-existing E1/E2/E4/E5 cases the epic cites as the baseline. None of the T2–T8 cases exist: grep for `edit business|move category|rule group|role change|remove member|load more|scope` finds no matching tests, and there is no axe/a11y assertion (grep `axe|a11y` returns nothing). Since T2–T8 themselves are unimplemented, their regression coverage is absent. All DoD items unmet.

## E13 — Self-host setup skill + deployment + security posture pages

Epic E13 (self-host setup skill, deployment runbook, and public security/setup pages) is entirely TODO on branch codex/real-world-testing (HEAD 9b5154d). All 9 tickets are unstarted — zero E13 artifacts exist and no E13/self-host commit appears on any branch.

Hard evidence of absence: no `skills/` directory and no `skills/openbooks-self-host/SKILL.md` (T1); no `scripts/setup.mjs` and no `setup` package.json script (T2); `scripts/dev-full.mjs` has no `--setup` mode and `README.md` still points self-hosters at Ansar's shared `ceaseless-mandrill-524` Convex deployment (T3); `scripts/preflight.mjs` is the unchanged Bedrock-only version that hard-requires AWS_* env for every provider, throws on non-sandbox Plaid (lines 142-144) and on non-test Stripe keys (line 171), so live keys still FAIL (T4); no `docs/security/security-posture.md` and no `apps/web/src/app/security/page.tsx` route (T5); no `docs/self-host/` directory, no env-checklist, no `scripts/check-env-docs.mjs` (T6); no `apps/web/src/app/setup/page.tsx` and no landing-page link to /setup or /security (T7); no `docs/self-host/deploy.md`, no `scripts/scan-secrets.mjs`, and the Ansar-specific deployment docs are unrefactored (T8); and no validation-evidence artifact exists (T9).

The unrelated merged work (UI redesign, Plaid/Stripe connectors, double-count fix c23dc4c, AI calibration) touched none of E13's surfaces. The only E13-adjacent code that already exists is pre-existing infrastructure the epic explicitly lists as read-only verification targets: `convex/connections.ts` webhookConfig (line 414) + keyPreview/fingerprint return shape, `convex/secretBox.ts` AES-GCM, `convex/authz.ts`, and both `convex/aiCatalog.ts` / `convex/aiProviderRegistry.ts`. None of these are E13 deliverables; the docs, scripts, skill, pages, and gates that constitute the epic are all missing.

Relevant paths inspected: /Volumes/SSD/OpenBooks/scripts/preflight.mjs, /Volumes/SSD/OpenBooks/scripts/dev-full.mjs, /Volumes/SSD/OpenBooks/package.json, /Volumes/SSD/OpenBooks/README.md, /Volumes/SSD/OpenBooks/convex/connections.ts, /Volumes/SSD/OpenBooks/apps/web/src/app/page.tsx, /Volumes/SSD/OpenBooks/docs/security/, /Volumes/SSD/OpenBooks/docs/deployment/.

- `E13-T1` todo — No skill exists. `ls skills/` => 'NO skills/ dir'; `ls .claude/skills/openbooks-self-host` => missing. `git ls-files skills/` returns nothing. No skills/openbooks-self-host/SKILL.md or reference/env-map.md anywhere, and no E13/self-host commit across all branches. Also depends on E13-T2/T6, which are themselves todo.
- `E13-T2` todo — scripts/setup.mjs is MISSING. package.json scripts has no `setup` key (only dev/dev:full/build/lint/typecheck/test/seed:demo/receipts:fixtures/stripe:webhook:register/preflight/verify/convex:dev/convex:deploy). grep for 'setup.mjs' in package.json/scripts => none. No JWT-keypair/JWKS/OPENBOOKS_SECRET_ENCRYPTION_KEY minting script exists.
- `E13-T3` todo — scripts/dev-full.mjs still has assertCloudConvex (line 49) and no `--setup` path or call to scripts/setup.mjs (grep for 'setup'/'--setup' finds only the dry-run flag handling, no setup mode). README.md Local Setup (line 79) still says 'Convex runs in the cloud deployment ceaseless-mandrill-524; do not point...' — the Ansar-shared-deployment wording the ticket requires removing is intact. dry-run output does not mention a setup step.
- `E13-T4` todo — scripts/preflight.mjs is the unchanged Bedrock-only version: requiredEnv (lines 11-26) hard-requires AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY/AWS_REGION/AI_MODEL/AI_EMBEDDINGS_MODEL/PLAID_*/STRIPE_SECRET_KEY for ALL providers. checkPlaid throws unless PLAID_ENV==='sandbox' (lines 142-144); checkStripe throws unless /^((sk|rk)_test_)/ test key (line 171) — live keys still FAIL; checkBedrock is the only AI dispatch and throws if AI_PROVIDER!=='bedrock'. No provider split, no openai/anthropic/google/groq ping, no exported requirements JSON, absent Plaid/Stripe still 'missing required env'=FAIL not SKIP. (Note: backlog files-list points at convex/aiProviderRegistry.ts; both aiCatalog.ts and aiProviderRegistry.ts exist but neither is referenced by preflight.)
- `E13-T5` todo — docs/security/security-posture.md does NOT exist (`find docs -iname '*security-posture*'` empty; docs/security/ has only secrets.md). apps/web/src/app/security/page.tsx does NOT exist (no security route). Landing page apps/web/src/app/page.tsx has no link to /security (grep for href/Link to security => none). The cited code deps (secretBox.ts AES-GCM, connections.ts keyPreview/fingerprint at lines 49-50/338-339, authz.ts) pre-exist as the verification targets, but the doc, page, and landing link — the actual ticket deliverables — are absent. Note connections.ts HTTPS-redirect guard is at lines 246-251 gated on OPENBOOKS_REAL_TEST_LIVE_CONNECTORS, not exactly the cited ':248' for the live-key requirement.
- `E13-T6` todo — docs/self-host/ directory does not exist (`ls docs/self-host` => NO docs/self-host). No prerequisites.md or env-checklist.md. scripts/check-env-docs.mjs is MISSING. No `check:env-docs` script in package.json. Depends on E13-T4 which is todo.
- `E13-T7` todo — apps/web/src/app/setup/page.tsx does NOT exist (no setup route). Landing page has no 'Self-host / Run your own' link to /setup (grep => none; only static feature-table text 'Self-hosted, you own the data'). connections.webhookConfig query exists (convex/connections.ts:414) and ConnectionsSection copy-row pattern exists as read-only references, but the /setup page consuming them is not built. Depends on E13-T5/T6 (both todo).
- `E13-T8` todo — docs/self-host/deploy.md does NOT exist. scripts/scan-secrets.mjs is MISSING; no `scan:secrets` script in package.json. docs/deployment/vercel.md and convex-auth-plunk.md are unchanged Ansar-specific runbooks (still present, not refactored/labeled as personal reference). No secret-scan gate added. Depends on E13-T5 (todo).
- `E13-T9` todo — End-to-end validation depends on T1–T8, all of which are todo. No docs/self-host/validation-evidence.md and no docs/finishing/evidence/e13-* artifact (`find` => 'NO e13 evidence artifact'). None of the exercised scripts (setup.mjs, check-env-docs.mjs, scan-secrets.mjs) or pages (setup/security) exist, and no generalized preflight to run, so the dry-run cannot have been performed.

## E14 — Quality: test suite, accounting invariants, categorization eval & security audit

E14 (the quality net epic) is essentially unstarted on branch codex/real-world-testing (HEAD 9b5154d). NONE of the epic's named NEW deliverable files exist: no convex/ledgerInvariants.test.ts, convex/stripeClearingInvariant.test.ts, convex/authzCoverage.test.ts, convex/fixtures/categorizationGold.ts, scripts/authz-coverage.mjs, docs/finishing/security-audit.md, the five go-live e2e specs, the gitleaks config, or the CI workflow/`pnpm ci`/`typecheck:convex` scripts. Git history confirms the branch carries E5 (redesign consistency), E6 (AI calibration, 1484e72), and E7 (Stripe/Plaid double-count fix, c23dc4c) work but zero E14 commits.\n\nClassification: 0 done, 6 partial (T1, T2, T3, T4, T6), wait — 5 partial (T1, T2, T3, T4, T6) and 3 todo (T5, T7, T8). The 'partial' ratings are driven entirely by PRE-EXISTING infrastructure that incidentally satisfies SOME DoD bullets, not by any E14 work: ledger.test.ts already proves balanced-entry rejection + USD trial-balance-zero + a 40-iter fuzz + reversal-roundtrip (T1/T3); stripeSingleCounting.test.ts already proves single-payout clearing/in-transit zero-out + income single-count (T2); stripeWebhook.test.ts and plaidWebhook.test.ts already prove tampered-signature rejection (T6); the 0.8 target + aiEvalRuns persistence + ai-eval-h3 e2e exist (T4). What is uniformly MISSING across tickets: the dedicated named test files, the multi-payout/no-fixtures-on-real invariant, exact-inverse/immutability/>5000-row-truncation assertions (truncation cliff still live at reportViews.ts:494), the >=60-row committed gold dataset + accuracy-math unit test, the 241-function authz matrix + coverage script + cross-workspace rejection test, the security-audit document + gitleaks + pnpm audit, all five disposable-book e2e specs, and the extended verify/`pnpm ci`/convex-tsc CI gate.\n\nNotably, two upstream dependencies E14 assumes were also never built on this branch: the unified `credentials` table (E3 — schema.ts still has the old split aiCredentials@326 + connectionCredentials@764) and the portfolio All/Zikra/Z360 scope switch with intercompany elimination — which further blocks T5/T6/T7. The dead journalLines.fxRate field (schema.ts:220) also still exists, contradicting T1's drop-and-assert-gone requirement.

- `E14-T1` **PARTIAL** — Required deliverable convex/ledgerInvariants.test.ts does NOT exist (confirmed absent; convex/*.test.ts listing has no such file). The named DoD asks for a NEW file. However the PRE-EXISTING convex/ledger.test.ts already covers some of the same invariants: 'rejects unbalanced entries' (ledger.test.ts:103, asserts 'debits must equal credits' re-covering ledger.ts:387-389), 'posts balanced entries and keeps the trial balance at zero' (ledger.test.ts:122, asserts snapshot.trialBalance.differenceMinor===0 via api.ledger.accountingSnapshot at ledger.ts:650), and a deterministic random sequence keeping trial balance zero (ledger.test.ts:216) — but only 40 iterations, not the required N>=50, and it does NOT cross-check journalLines directly in t.run. MISSING outright: (a) the new ledgerInvariants.test.ts file itself; (b) Test 3 the no-mixed-currency/USD-only guard — grep for a non-USD rejection in convex/ledger.ts returns nothing and there is no entity currency lock asserted; (c) the dead journalLines.fxRate field is still present at convex/schema.ts:220 (DoD requires it dropped with no remaining dependency, and a test asserting its absence); (d) the inline doc comment noting GL is USD-only. So balanced + trial-balance invariants exist in spirit in the old file, but the ticket's actual new artifact, the 50-iter fuzz, and the USD-only/fxRate guards are not done.
- `E14-T2` **PARTIAL** — Required deliverable convex/stripeClearingInvariant.test.ts does NOT exist (confirmed absent). The PRE-EXISTING convex/stripeSingleCounting.test.ts DOES prove 1150 (Clearing) and 1160 (In-Transit) net to ~0 after reconcile — test 'counts revenue once, drains clearing/in-transit to ~0...' (stripeSingleCounting.test.ts:196) calls accountNetMinor for '1150'/'1160' and asserts afterMatch.inTransitNetMinor===0 (line 400), and includes the income double-count guard (single-counting expectations). The exported matcher helpers findMatchingStripePayout (stripe.ts:1222), reconcilePayoutWithDeposit (stripe.ts:1285), matchPlaidInflowToPayout (stripe.ts:1426) exist and descriptor is demoted to a booster (stripe.ts: filter accepts amount match even without descriptor). BUT this only exercises a SINGLE payout. MISSING: (a) the new dedicated stripeClearingInvariant.test.ts file; (b) the MULTI-payout fixture (3 charges->fees->payouts->deposits) including one with a non-'stripe' descriptor and one whose deposit arrives on a different day within the -2/+5 business-day window, asserting 1150/1160 net to 0 across ALL payouts; (c) the no-fixtures-on-a-real(non-demo)-entity assertion guarding stripe.ts:1824/1851 RC4 (red-until-fixed). The single-payout zero-out is proven; the generalized multi-payout invariant and the fixture-gating test are not.
- `E14-T3` **PARTIAL** — No convex/reversalInvariants.test.ts and no convex/ledgerInvariants.test.ts (both absent). The PRE-EXISTING convex/ledger.test.ts:143 'supports reversal and repost through postEntry' posts an entry, reverses via reversesEntryId, and asserts trial balance stays 0 and a reversing entry exists — but it does NOT assert the reversal lines are the exact line-by-line debit<->credit inverse, does NOT assert affected accounts return to exact pre-original balances, and does NOT assert the original entry+lines are byte-for-byte unchanged (immutability). MISSING entirely: (a) the exact-inverse + pre-original-balance assertions; (b) the immutability (original row/lines unchanged) assertion exercising assertReversalLines at ledger.ts:391; (c) the >5000-line report-balance truncation test — the .take(REPORT_LIMIT) truncation cliff is still live at convex/reportViews.ts:492-494, and no test posts >5000 lines and asserts the reported trial balance is still zero / tracks it red-until E1-T5. None of the three DoD assertions for this ticket exist.
- `E14-T4` **PARTIAL** — The committed label-safe gold dataset does NOT exist: no convex/fixtures/categorizationGold.ts, no tests/fixtures gold file (grep for categorizationGold/goldCategorization returns nothing; convex/fixtures/ contains only goldenReports.ts and receipts/). The eval is still the demo-seeded HOLDOUT, not gold: runHoldoutCategorizationEval (ai.ts:1088) reads holdoutTransactionResult (ai.ts:552); there is NO runGoldCategorizationEval. PARTIALLY present scaffolding: the 80% target constant exists (ai.ts:370/423 targetAccuracy=0.8 with meets_target/below_target/no_eval_rows status), results persist to aiEvalRuns (schema.ts:380, inserts at ai.ts:593/961), Settings>AI rendering + tests/e2e/ai-eval-h3.spec.ts exist and (per prior state) is green. MISSING: (a) the >=60-row committed no-PII gold dataset; (b) the gold eval runner scoring predictions vs gold; (c) the deterministic pure-unit vitest test asserting the accuracy/threshold math on a fixed pairing (no such unit test found); (d) the recorded/mock-provider CI path. The threshold machinery exists but the durable gold benchmark, its runner, and the accuracy-math unit test are not built.
- `E14-T5` todo — None of the three deliverables exist. scripts/authz-coverage.mjs is ABSENT (confirmed missing). docs/finishing/security-audit.md is ABSENT, so there is no 241-function authz coverage matrix classifying each function authorized/intentionally-public/FINDING. convex/authzCoverage.test.ts is ABSENT. The existing convex/authz.test.ts (4 tests at lines 47/55/72/85) only covers requestAccess.list reads, dev-auth-bypass, and one anonymous-read rejection — it does NOT cover a curated representative view/ledger-mutation/action/connections-mutation across categories nor cross-workspace (workspace A targeting entity B) rejection. The allow-list constant and the new-function-class classifications (unified credentials functions; demo read path) do not exist — and the unified `credentials` table from E3 itself was never built (schema.ts still has the OLD split: aiCredentials at 326 + connectionCredentials at 764). The GET /ai/chat _ctx authz triage (http.ts:96) is not documented anywhere. Not started.
- `E14-T6` **PARTIAL** — The primary deliverable docs/finishing/security-audit.md does NOT exist (confirmed absent), so none of the documented sections (secret handling/encryption-at-rest, webhook verification, gitleaks history scan, pnpm audit, findings table) are written. No gitleaks config or committed gitleaks tooling anywhere (grep for *gitleaks* returns nothing; no .gitleaks.toml). No recorded pnpm audit output. PARTIALLY satisfied underlying primitives: convex/secretBox.ts (AES-GCM) exists; webhook signature verification AND tamper-rejection tests already exist — convex/stripeWebhook.test.ts:36 'rejects changed payloads and stale timestamps' (verifyStripeWebhookSignature) and convex/plaidWebhook.test.ts:68 verifyPlaidWebhookSignature with the signed-hash path, so the DoD's 'tampered/absent signature rejected' test is effectively covered by pre-existing tests. .gitignore correctly ignores .env/.env.*. BUT the audited target — the unified `credentials` store — does not exist (E3 unbuilt; aiCredentials+connectionCredentials still split at schema.ts:326/764), and the document, gitleaks scan, and pnpm audit deliverables are all absent. Mostly todo with only the webhook-tamper tests and crypto primitives pre-existing.
- `E14-T7` todo — All five required e2e specs are ABSENT: tests/e2e/onboarding-golive.spec.ts, connections-byok.spec.ts, reset-reonboard.spec.ts, portfolio-scope.spec.ts, stripe-reconcile.spec.ts (confirmed none match in tests/e2e/). The disposable-book/fresh-workspace guard is NOT documented in tests/e2e/helpers.ts (grep for disposable/freshWorkspace/createWorkspace/guard returns nothing). The upstream features these specs would exercise are also unbuilt on this branch: in-UI BYO-key entry for the unified credentials store (E3 not built), and the portfolio All/Zikra/Z360 scope switch with 'Intercompany eliminated' roll-up (grep for 'Intercompany eliminated'/portfolio scope in apps/web/src returns nothing). Pre-existing onboarding.spec.ts and stripe.spec.ts are the OLD specs, not the new go-live flows. Not started.
- `E14-T8` todo — No CI gate work. package.json has NO `ci` script and NO `typecheck:convex` script (grep returns NONE); `verify` is still the old `pnpm typecheck && pnpm lint && pnpm build && pnpm test:unit` (package.json:28) which does NOT run convex tsc against convex/tsconfig.json (the file exists but is unused by verify) and does NOT run e2e. No .github/workflows directory exists at all. No README/docs CI section describing how to run the gate locally + required env. No cross-epic DoD->test coordination table in docs/finishing (none found). vitest.config.ts globbing convex/**/*.test.ts is pre-existing, but since the new invariant/eval/authz tests (T1-T5) were never created, there is nothing new to fold in. Not started.

## E15 — Docs, Help Center, Landing & GTM (make OpenBooks publishable and explainable)

E15 is essentially untouched on branch codex/real-world-testing (HEAD 9b5154d). The product-engineering epics that this branch merged (UI redesign, Plaid/Stripe connectors, the Stripe/Plaid double-count fix in c23dc4c, AI calibration) did NOT touch any E15 deliverable — E15 is a pure docs/landing/GTM/governance epic. Verified concretely: the root LICENSE still ships GNU AFFERO GPL v3 (LICENSE line 1), README.md line 62 still says "License: AGPL-3.0-only", docs/product/01-vision-and-scope.md lines 84/105 still reference AGPL, and AGENTS.md was checked — so the landing's existing "MIT licensed" claims (page.tsx ~432/538, FAQ lines 53/68) are currently FALSE/inconsistent rather than verified. No help/setup/why/demo/about/learn/guide routes exist under apps/web/src/app (confirmed by directory listing). The landing still points all demo CTAs ("Try the live demo" line 184/519, "Try the mobile demo" line 380, "Open the app" line 166) at /dashboard, still ships the Docker copy (page.tsx line 428 + FAQ "docker compose up"), still claims "up to 24 months of history" (line 24) and "$0/month, forever" (line 195), and still links GitHub to AnsarUllahAnasZ360/open-accounting (lines 187/540, no REPO-URL anchor, no openbooks rename). The docs/gtm/ directory does not exist (no why-openbooks.md, demo-video-script.md, outreach-templates.md, setup-instructions.md, github-publication-checklist.md). No CONTRIBUTING.md, SECURITY.md, CODE_OF_CONDUCT.md, or .github/ exist at repo root, and scripts/ has no issue-seeding script. README leads with the generic ledger thesis (not the portfolio/multi-LLC differentiator) and has no honest status table. git log shows zero E15-related commits. Note: a referenced source doc (docs/finishing/accounting-engine-blueprint.md) is also missing on this branch, which would block the GTM authoring tickets. Result: 10 tickets are todo and 1 (T1) is partial because the landing pre-anticipates MIT strings — but the actual relicense is not done. Every "done"-blocking DoD item is unmet across the epic.

- `E15-T1` **PARTIAL** — Relicense NOT done: LICENSE line 1 = 'GNU AFFERO GENERAL PUBLIC LICENSE Version 3' (still AGPL, not MIT). `grep -rni agpl` still returns hits in README.md:62 ('License: AGPL-3.0-only'), docs/product/01-vision-and-scope.md:84 and :105 (AGPL), and LICENSE:660. Only the partial pre-work exists: apps/web/src/app/page.tsx already shows 'MIT licensed' at line ~432 (why-free) and 538 (footer), and LandingPrototypeInteractions.tsx FAQ already says 'MIT-licensed' (lines 53, 68). DoD fails on every point: LICENSE is not MIT, grep does not return zero AGPL, and README does not state MIT — so the landing's MIT claims are currently INCONSISTENT with the LICENSE, not 'verified correct'. Also the FAQ still says 'fork it and keep it alive' (line 68) which the ticket wanted reworded for a permissive license. Marked partial only because the MIT landing strings the ticket says to 'verify' are already present.
- `E15-T2` todo — None of the corrections applied. page.tsx line 428 still lists 'Self-hosted, one Docker command'; LandingPrototypeInteractions.tsx FAQ still says 'A machine that runs Docker ... One `docker compose up`'. page.tsx line 24 still claims 'up to 24 months of history'. Line 195 still 'monthly $0/month, forever' with no honest footnote. Demo CTAs at lines 166, 184, 380, 519 all point to /dashboard (not /demo or interim /sign-in) — would bounce a logged-out visitor. GitHub links at lines 187 and 540 still hardcode 'github.com/AnsarUllahAnasZ360/open-accounting' with no <!-- REPO-URL --> anchor and no openbooks rename or custom-domain link.
- `E15-T3` todo — No help center exists. Directory check: apps/web/src/app/help is MISSING (and no /learn, /guide, /docs). grep for href="/help" / >Help< / >Guide< across apps/web/src returns nothing — no in-app or landing-footer Help link. Footer (page.tsx 530-545) has only 'MIT licensed', 'GitHub', 'No tracking' — no Help link. No new help components added.
- `E15-T4` todo — README.md (119 lines) still leads with the generic 'OpenBooks is not a dashboard ... ledger-first' thesis, not the portfolio/multi-LLC 'Portfolio CFO' differentiator. Line 62 still 'License: AGPL-3.0-only' (not MIT). No 'What works / what's beta / what's planned' honest status table (grep for 'status table'/'what works'/'beta' finds nothing). Quickstart (pnpm install / dev:full) exists but is the old structure. The referenced source docs/finishing/accounting-engine-blueprint.md is also MISSING on this branch.
- `E15-T5` todo — No setup page or doc. apps/web/src/app/setup is MISSING and docs/gtm/setup-instructions.md does not exist (docs/gtm/ directory absent). No security-posture page, no documented three manual steps (Plaid redirect, Stripe webhook, AI key) authored. No links from README/help/landing footer.
- `E15-T6` todo — Landing demo CTAs (page.tsx 166/184/380/519) all still target /dashboard — not /demo and not the safe interim /sign-in, and there is no TODO anchor. No apps/web/src/app/demo route exists. The demo is not described in README or any help center (neither exists). E11's /demo is not wired here; the CTA does not resolve to a safe destination for a logged-out user.
- `E15-T7` todo — docs/gtm/why-openbooks.md does not exist (docs/gtm/ directory absent). No /why route (apps/web/src/app/why MISSING). No founder narrative scaffold and no <!-- ANSAR INPUT: ... --> slots authored anywhere. grep for 'why-openbooks' only matches the backlog/epic files.
- `E15-T8` todo — docs/gtm/demo-video-script.md does not exist (docs/gtm/ absent). No scene table, shot list, captions, or recording checklist authored. grep for 'demo-video-script' matches only the backlog/epic planning files.
- `E15-T9` todo — docs/gtm/outreach-templates.md does not exist (docs/gtm/ absent). No Show HN post, social thread, owner DM, README blurb, subreddit variant, or 'do not say' guardrail list authored. grep for 'outreach-templates' matches only planning files.
- `E15-T10` todo — No governance files: CONTRIBUTING.md, SECURITY.md, CODE_OF_CONDUCT.md all MISSING at repo root. No .github/ directory (no ISSUE_TEMPLATE, no PULL_REQUEST_TEMPLATE.md). docs/gtm/github-publication-checklist.md does not exist. scripts/ contains dev/eval scripts (dev-full, seed-demo, register-stripe-webhook, etc.) but NO issue-seeding script that converts the E0–E15 backlog to labeled GitHub issues. No recorded secret-scan output.
- `E15-T11` todo — This is the final cross-audit/responsiveness/launch-readiness ticket; it depends on T1–T7 which are all unmet. No consistency-audit note, no responsive screenshots of landing/help/setup/why (the help/setup/why routes don't even exist), and no launch-readiness checklist produced. grep across public surfaces still finds AGPL inconsistencies (README, vision doc), so the 'single consistent MIT claim' DoD is failing. Nothing in this ticket has been started.

