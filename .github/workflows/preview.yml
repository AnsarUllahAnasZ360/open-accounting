name: Preview Deploy

# Ephemeral, on-demand previews. Two ways to trigger:
#   1. Comment "/preview" on an open PR  -> deploys that PR's head commit.
#   2. Actions -> "Preview Deploy" -> Run workflow  -> deploys a named branch.
# A closed PR triggers best-effort cleanup of its Vercel preview.
#
# Required repo secrets:
#   CONVEX_PREVIEW_DEPLOY_KEY  (Convex "Preview" deploy key)
#   VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID

on:
  issue_comment:
    types: [created]
  workflow_dispatch:
    inputs:
      branch:
        description: "Branch to deploy as a preview"
        required: true
        type: string
  pull_request:
    types: [closed]

permissions:
  contents: read
  issues: write
  pull-requests: write

env:
  VERCEL_CLI_VERSION: "50.28.0"
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
  # Stable-alias config (both optional repo Variables):
  #   PREVIEW_ALIAS_DOMAIN  — a wildcard custom domain on the Vercel project,
  #     e.g. "preview.openbooks.app" -> alias becomes br-stage.preview.openbooks.app
  #   PREVIEW_ALIAS_PREFIX  — prefix for the .vercel.app fallback (default "openbooks")
  #     -> alias becomes openbooks-br-stage.vercel.app
  PREVIEW_ALIAS_DOMAIN: ${{ vars.PREVIEW_ALIAS_DOMAIN }}
  PREVIEW_ALIAS_PREFIX: ${{ vars.PREVIEW_ALIAS_PREFIX || 'openbooks' }}

jobs:
  deploy:
    name: Deploy Preview
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: >-
      (github.event_name == 'issue_comment' &&
        github.event.issue.pull_request &&
        contains(github.event.comment.body, '/preview')) ||
      github.event_name == 'workflow_dispatch'
    concurrency:
      group: preview-deploy-${{ github.event.issue.number || github.event.inputs.branch }}
      cancel-in-progress: true
    steps:
      - name: React to preview comment
        if: github.event_name == 'issue_comment'
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.reactions.createForIssueComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: context.payload.comment.id,
              content: 'rocket',
            });

      - name: Resolve preview context
        id: preview
        uses: actions/github-script@v7
        with:
          script: |
            const { owner, repo } = context.repo;
            // Convex + Vercel preview names: lowercase alphanumerics + hyphens.
            const slug = (s) => s.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/(^-|-$)/g, '').slice(0, 40);

            // Deterministic, stable alias host for a preview key. Re-pointing this
            // same host at each new deployment is what keeps the URL constant.
            const aliasDomain = process.env.PREVIEW_ALIAS_DOMAIN;
            const aliasPrefix = process.env.PREVIEW_ALIAS_PREFIX || 'openbooks';
            const aliasHost = (key) =>
              aliasDomain ? `${key}.${aliasDomain}` : `${aliasPrefix}-${key}.vercel.app`;

            if (context.eventName === 'issue_comment') {
              const prNumber = context.issue.number;
              const { data: pr } = await github.rest.pulls.get({ owner, repo, pull_number: prNumber });
              if (pr.state !== 'open') {
                core.setFailed(`PR #${prNumber} is not open.`);
                return;
              }
              const previewKey = `pr-${prNumber}`;
              core.setOutput('pr_number', String(prNumber));
              core.setOutput('branch', pr.head.ref);
              core.setOutput('checkout_ref', pr.head.sha);
              core.setOutput('sha', pr.head.sha);
              core.setOutput('preview_key', previewKey);
              core.setOutput('alias_host', aliasHost(previewKey));
              return;
            }

            const branch = context.payload.inputs.branch;
            const { data: branchData } = await github.rest.repos.getBranch({ owner, repo, branch });
            const previewKey = `br-${slug(branch)}`;
            core.setOutput('branch', branch);
            core.setOutput('checkout_ref', branchData.commit.sha);
            core.setOutput('sha', branchData.commit.sha);
            core.setOutput('preview_key', previewKey);
            core.setOutput('alias_host', aliasHost(previewKey));

      - uses: actions/checkout@v4
        with:
          ref: ${{ steps.preview.outputs.checkout_ref }}

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install pinned Vercel CLI
        run: pnpm add -g vercel@${VERCEL_CLI_VERSION}

      - name: Typecheck
        run: pnpm typecheck && pnpm typecheck:convex

      - name: Pull Vercel project settings (preview)
        run: >-
          vercel pull --yes --environment=preview
          --git-branch="${{ steps.preview.outputs.branch }}"
          --token=${{ secrets.VERCEL_TOKEN }}

      # Convex deploys to a named preview backend, injects its URL as
      # NEXT_PUBLIC_CONVEX_URL, then runs `vercel build` so Next.js bakes in the
      # correct backend URL at build time.
      - name: Deploy Convex preview + Vercel build
        env:
          CONVEX_DEPLOY_KEY: ${{ secrets.CONVEX_PREVIEW_DEPLOY_KEY }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          set -euo pipefail
          pnpm exec convex deploy \
            --preview-create "${{ steps.preview.outputs.preview_key }}" \
            --cmd-url-env-var-name NEXT_PUBLIC_CONVEX_URL \
            --cmd "vercel build --yes --token=$VERCEL_TOKEN"

      - name: Deploy to Vercel preview
        id: deploy
        run: |
          set -euo pipefail
          URL=$(vercel deploy --prebuilt \
            --meta previewKey="${{ steps.preview.outputs.preview_key }}" \
            --meta previewBranch="${{ steps.preview.outputs.branch }}" \
            --token=${{ secrets.VERCEL_TOKEN }})
          URL=$(echo "$URL" | tail -n 1)
          echo "url=$URL" >> "$GITHUB_OUTPUT"

      # Re-point the stable alias at the deployment just created. The snapshot URL
      # changes every run; this host does not, so links and env values stay valid.
      - name: Point stable alias at this deployment
        run: >-
          vercel alias set "${{ steps.deploy.outputs.url }}"
          "${{ steps.preview.outputs.alias_host }}"
          --token=${{ secrets.VERCEL_TOKEN }}

      - name: Write job summary
        run: |
          {
            echo "### Preview Deployment"
            echo ""
            echo "| Field | Value |"
            echo "|---|---|"
            echo "| Stable URL | https://${{ steps.preview.outputs.alias_host }} |"
            echo "| Snapshot URL | ${{ steps.deploy.outputs.url }} |"
            echo "| Convex preview | ${{ steps.preview.outputs.preview_key }} |"
            echo "| Branch | ${{ steps.preview.outputs.branch }} |"
            echo "| Commit | ${{ steps.preview.outputs.sha }} |"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Comment preview URL on PR
        if: github.event_name == 'issue_comment'
        uses: actions/github-script@v7
        with:
          script: |
            const stableUrl = 'https://${{ steps.preview.outputs.alias_host }}';
            const snapshotUrl = '${{ steps.deploy.outputs.url }}';
            const sha = '${{ steps.preview.outputs.sha }}'.slice(0, 7);
            const branch = '${{ steps.preview.outputs.branch }}';
            const key = '${{ steps.preview.outputs.preview_key }}';
            const body = [
              '### Preview Deployment',
              '',
              '| | |',
              '|---|---|',
              `| **Stable URL** | ${stableUrl} |`,
              `| **Snapshot URL** | ${snapshotUrl} |`,
              `| **Convex** | \`${key}\` |`,
              `| **Commit** | \`${sha}\` |`,
              `| **Branch** | \`${branch}\` |`,
            ].join('\n');

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });
            const existing = comments.find(
              (c) => c.user.type === 'Bot' && c.body.includes('Preview Deployment')
            );
            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }

  cleanup:
    name: Cleanup PR Preview
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    concurrency:
      group: preview-cleanup-${{ github.event.pull_request.number }}
      cancel-in-progress: false
    steps:
      - name: Install pinned Vercel CLI
        run: npm i -g vercel@${VERCEL_CLI_VERSION}

      - name: Resolve stable alias host
        id: cleanup
        uses: actions/github-script@v7
        with:
          script: |
            const aliasDomain = process.env.PREVIEW_ALIAS_DOMAIN;
            const aliasPrefix = process.env.PREVIEW_ALIAS_PREFIX || 'openbooks';
            const previewKey = `pr-${context.payload.pull_request.number}`;
            core.setOutput(
              'alias_host',
              aliasDomain ? `${previewKey}.${aliasDomain}` : `${aliasPrefix}-${previewKey}.vercel.app`,
            );

      # Only PR previews are torn down here. Branch previews (e.g. stage) keep
      # their alias on purpose so their URL stays live between deploys.
      - name: Remove stable alias
        run: >-
          vercel alias remove "${{ steps.cleanup.outputs.alias_host }}"
          --yes --token=${{ secrets.VERCEL_TOKEN }} || true

      - name: Note cleanup
        run: |
          {
            echo "### Preview Cleanup"
            echo ""
            echo "PR #${{ github.event.pull_request.number }} closed."
            echo ""
            echo "- Removed stable alias \`${{ steps.cleanup.outputs.alias_host }}\`."
            echo "- Delete the \`pr-${{ github.event.pull_request.number }}\` Convex preview from the Convex dashboard if it lingers."
          } >> "$GITHUB_STEP_SUMMARY"
