Dropdown select matching Input metrics — for date ranges, category pickers, account filters.

```jsx
<Select options={["Last 30 days", "This quarter", "Year to date"]} defaultValue="This quarter" />
<Select placeholder="Choose category" options={[{ value: "6010", label: "Software & AI tools" }]} />
```
